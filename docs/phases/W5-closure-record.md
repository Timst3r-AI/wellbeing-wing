# W5 Closure Record

**Status:** Accepted by human reviewer, 2026-08-17. Not a build instruction.
**Phase:** W5 — Runtime Enforcement and Behavioural Evaluation
**Type:** Phase closure record / audit seal. Docs-only. Authorises nothing to be built. This record satisfies W5-D6.
**Date:** 2026-08-17
**Tier at landing:** J — full ceremony
**Governed by:** the W5 runway; the seven W5-D1 doctrine records and their five correction/qualification records; ADR-0035, ADR-0036 and ADR-0037; the seven W5-D2 milestones; the W5-D3 instrument; the W5-D4 execution event and its evaluation records; the W5-D5 maintenance record; the W2, W3 and W4 closure-record precedent

---

**W5 set out to make the declared walls real and then to watch them: a runtime that enforces at execution what W4 wrote in contracts, an instrument that observes without judging, one governed execution of twenty-three traps, and an evidence map updated to tell only the truth. It closes with exactly that, published — a bounded, observable, network-free runtime proven structurally; an instrument proven fit and proven unable to run by itself; a deterministic execution whose honest answer to every generative question was "unknown, not absent"; and a phase that measured its walls without once claiming they make anyone safe.**

## 1. Closure statement

**W5 — Runtime Enforcement and Behavioural Evaluation — is complete and closed.** W5 closes because **every obligation its runway addresses to the phase is satisfied, intentionally deferred under named doctrine, or recorded as an external dependency** — not because everything W5 named is finished, and emphatically not because anything is universally green. **W5-D6 is satisfied by this closure record, and W5-D6 is complete only by this closure landing.**

What remains undone is undone by design and stays visible: the generative era never opened — no model was ever contacted, and the twenty-six overt-channel probe questions recorded `unknown-not-absent` in the W5-D4 evaluation records are the generative era's precise work order, future-owned behind ADR-0034's first-contact gate. The nine pending-ledger stubs remain pending and visible. Lane C's deferred rows remain deferred exactly where their source basis did not exist. Closing W5 finishes none of these and authorises none of them. **W6 remains unopened.**

## 2. The deliverable chain

| Deliverable | Commit | What it delivered |
|---|---|---|
| W5-AR — runway | `893d39d` | Two governed eras under one phase, six deliverables, the seams (T12, AR-NIST-05 currency, the Tier 2/3 boundary, the two dependency strings), and the entry gate honoured |
| W5-D1 — ADR-0023 | `9008b1d` | AIAdapter import citation correction (Tier M) |
| W5-D1 — DR-W5-01 (ADR-0024) | `6f64382` | The processing-context boundary: one governed seam, grant-derived one-room contexts, four-surface observability by construction; resolves W0 OQ 10 |
| W5-D1 — ADR-0025 | `071cb00` | Provisional freshness-governance qualification — the exact non-clinical label carried into runtime |
| W5-D1 — ADR-0026 / ADR-0027 | `f9acc50` / `cb7738c` | Safety-relevant set citation correction and reconciliation |
| W5-D1 — ADR-0028 | `23bbffb` | W5-AR open-cell count correction (Tier M) |
| W5-D1 — DR-W5-02 (ADR-0029) | `17e7652` | Runtime freshness and unknown/stale behaviour — the engine's ladder stays the single label calculator |
| W5-D1 — DR-W5-03 (ADR-0030) | `f923c65` | Grant machinery, consent duration, re-authentication binding to grant identity |
| W5-D1 — DR-W5-04 (ADR-0031) | `2d2dadc` | Payload assembly and equality — granted scope is the whole payload; the parcel cannot be padded |
| W5-D1 — DR-W5-05 (ADR-0032) | `cf130cb` | Transmission and disclosure mechanics — the crossing that cannot be quiet |
| W5-D1 — DR-W5-06 (ADR-0033) | `3e850a0` | Model access — the door decision made in ink; local class doctrinally constructable only, hosted unopened |
| W5-D1 — DR-W5-07 (ADR-0034) | `6230d42` | Behavioural evaluation architecture — the watcher's charter; the two-value status vocabulary; the first-contact gate |
| ADR-0035 | `d1bd5ce` | Plaintext-residue applicability at the runtime boundary — the mandatory pre-W5-D2 gate, discharged |
| W5-D2-M01 … M04 | `c684865` `248a9c6` `50f46bc` `e5fe7b7` | Runtime foundation and fence; grant machinery; freshness wiring; the processing context with the first full residue tax paid |
| ADR-0036 | `a3dce11` | Revocation cascade for derived artefacts; resolves W0 OQ 2; the W5-D2-M05 gate |
| W5-D2-M05 … M07 | `ccd7774` `81d6777` `673ef6f` | Payload assembly and equality; transmission and disclosure with in-process doubles; isolation end-to-end, the stricter M2 runtime, and the stateless composition root |
| W5-D3 | `b3d6d3d` | The behavioural evaluation harness — the instrument, never the run — with its false-positive and false-negative guards and a structural corpus-execution interlock proven shut |
| W5-D4 — ADR-0037 and the run | `bf7f8c4` | Exactly one governed execution event, `W5-D4-RUN-01`: the corpus executed deterministically, evaluation records landed append-only under `governance/evaluation/`, all twenty-three statuses transitioned through ceremony, results never in fixture files |
| W5-D5 — `W5-D5-LCM` | `3086982` | The one governed Lane C maintenance act: eleven Tier 2 rows reviewed individually, four evidenced on their own cited artefacts, seven deferred with the reassessment recorded, `AR-NIST-05` currency reassessed as assigned |
| **W5-D6 — this record** | *this landing* | **The phase closure record: criteria assessment, verification, pending-ledger review, incident log, honest findings, the W6 gate named without opening, and the deferred inventory** |

## 3. Criteria assessment against the runway

| Runway obligation | Disposition |
|---|---|
| **North star** — a bounded, observable runtime and an honest measurement of it, not a product | **Satisfied** — the runtime is seven proven modules behind a stateless composition root; the measurement is one deterministic execution whose records claim observation and nothing more |
| **W5-D1** — the doctrine set, landing individually through full ceremony, each accepted before what it gates | **Satisfied** — seven decision records (DR-W5-01…07) plus five correction/qualification records, each gating correctly; ADR-0035/0036/0037 added through the same ceremony as their gates required |
| **W5-D2** — milestone-based runtime with same-commit deterministic proofs; four-surface observability designed in | **Satisfied** — seven milestones, every capability with its structural and (where boundary-holding) residue proofs in the same commit; four boundary operations registered and kill-sweep clean; observability by construction proven at M04 and consumed by the W5-D4 captures |
| **W5-D3** — build and prove the instrument, including both calibration guards; the corpus not executed in this deliverable | **Satisfied** — twenty-one structural proofs including the shut interlock, calibration guards as fitness conditions, and corpus-still-unexecuted asserted after every self-test |
| **W5-D4** — execute only once D1–D3 prerequisites hold; results never enter fixture files; status transitions per accepted doctrine | **Satisfied** — ADR-0037 authorised exactly one run; calibration preceded corpus work by structural proof; results live only in governed evaluation records; all twenty-three transitions ceremonial, one field each, map and hash discipline intact |
| **W5-D5** — one governed maintenance act; explicit `AR-NIST-05` reassessment; never Tier 3; never applicability | **Satisfied** — eleven rows reviewed individually, the §6.4 currency item reassessed as assigned, no tier value changed, no applicability record touched |
| **W5-D6** — whole-phase closure; pending-ledger review; incident log; honest findings; deferred inventory; the W6 gate named without opening | **Satisfied by this record** |
| **§6.3 — the T12 seam** | **Intentionally deferred under named doctrine** — ADR-0034 C2 ruled amendment-through-ceremony and assigned the edit to the first W5-era landing that touches the pending ledger; no W5 landing touched it, so the assignment stands undischarged and visible, and no conversion occurred under the literal condition |
| **§6.4 — the `AR-NIST-05` currency item** | **Satisfied** — explicitly reassessed by W5-D5, superseded in place, the behavioural component kept honestly deferred |
| **§6.5 — the Tier 2 / Tier 3 boundary** | **Honoured** — eleven Tier 2 rows assessed row by row; eleven Tier 3 rows untouched; all four applicability records still `unresolved`; the eight Tier 1 honesty rows untouched |
| **§6.6 — the two dependency strings** | **Honoured** — Lane B's harness-bearing string and Lane C's runtime string were never collapsed, and no `DEP-` namespace was minted |
| **The runway non-goals** | **Honoured in full** — no review surface, no governed string catalogue, no label rendering, no UI, no CLI, no hosted mode, no sync, no vendor account, no SDK, no credential, no E10 activation, no Z4 discharge, no E12/Z5 activation, no real user data, no clinical example, no W6 implementation — and **no model contact, ever, in the entire phase** |

**No runway obligation is unresolved, and no obligation was converted into a failure because its named deferral remains open.**

## 4. Verification at phase closure

- **Deterministic suite: 446 passed, 9 skipped, 0 failures; 439 subtests.** The suite grew from 281 tests at W4 closure to 455 collected — seven runtime proof modules, the amended Lane B validator, the harness proofs, and the execution proofs, every one green and every deferral named rather than silently passed.
- **Public-safety scan at closure: 195 files, zero findings, 123 allowlisted suppressions** — zero new suppressions across every W5 code landing.
- **Registry: 70 entries before this closure; this record advances it to 71.** Every registered hash moved only with its own bytes in its own atomic commit; `W4-D6-PHDAR`, `W4-D6-EASR`, `W4-D6-BEF`, `ADR-0037` and `W5-D5-LCM` verified hash-current at this closure's gate.
- **Zero dependencies, still.** The runtime, instrument, runner and every proof are Python standard library only; no package was ever added, and the approved-manifest fence held all phase.
- **The corpus position is exact:** twenty-three fixtures, all `behaviourally_executed`, transitioned once through ceremony and untouched by this closure; twenty-four evaluation records (one manifest, twenty-three fixture records) byte-untouched since their landing, deterministic reproduction proven at their gate.

## 5. The pending ledger at closure

**Nine stubs remain, none converted by this closure, every one visible.** The ledger leaves W5 in a changed but honest state:

- **The six formerly W5-era stubs remain pending — and the reason is stated plainly.** Their substance is *generative-era behavioural proof*: repetition resistance, authority laundering, cross-room isolation behaviour, in-room silent inference, and the payload-equality and grant-merge behaviours at their dormant limbs. W5 built the deterministic halves of their prerequisites — the runtime exists, the grant machinery exists, the harness exists — but W5 deliberately never opened the generative era, and a stub converted against a deterministic-only capability would be exactly the honest-looking pass the ledger exists to prevent (ADR-0034 decisions 43 and 45 govern this in terms). **They pass forward as generative-era work behind the ADR-0034 first-contact gate.**
- **The T12 condition amendment remains assigned and undischarged** — ADR-0034 C2's ruling stands: the first W5-era-or-later landing that touches the pending ledger through ceremony must amend the condition to state both prerequisites. No W5 landing touched the ledger, by design.
- **The three W6-owned stubs** (language-law grading, no-bulk-approve, label legibility) unblock on surfaces and the governed string catalogue, exactly as recorded at W4 closure.

**No condition was rewritten, no conjunct hidden, and no stub cleared by closing the phase.**

## 6. Incident log

Five incidents occurred in W5. All are closed with final dispositions; **none remains open, and none created a closure blocker.**

1. **The M03 label-carriage catch.** A scratch draft wrapped the exact provisional-governance label across comment lines, breaking the byte-exact carriage ADR-0029 requires; **the draft's own proof caught it in scratch before any materialisation.** *Lesson: exact-phrase obligations are proven, not eyeballed.*
2. **The ADR-0036 materialisation anchor halt.** A materialisation script's README anchor did not match the live row tail; **the script's own assertion stopped the edit after three of five steps**, the anchor was re-measured from the live file, and the landing completed. Disclosed in the packet. *Lesson: fail closed; anchors are measured, never assumed.*
3. **The W5-D4 barred-vocabulary catch.** The runtime's own crossing event name `authority-checks-passed`, captured verbatim in a routing surface, tripped the new records' barred-token proof in scratch. Resolved as a **named mechanical exception written into ADR-0037 decision 11** — an event name, never an outcome — rather than a runtime rename. *Lesson: when a proof and a truthful capture collide, the exception is named in doctrine, not smoothed in code.*
4. **The transient cache fence trip, again.** A test-runner cache directory appeared at repository top level during the W5-D4 gate and **the directory fence rejected it exactly as designed** — the same incident class as W4's, now with the standing practice applied: the directory was removed and the suite runs with caching disabled. **No governed state was affected.**
5. **The W5-D5 scratch-path scan flags.** The landing-mode scan of a scratch draft reported three findings that were the assurance record's own pre-existing anti-claim sentences — byte-identical to the published file and covered by exact path-scoped allowlist entries that cannot match a scratch path. Verified pre-existing, disclosed, and confirmed suppressed at the true path with the normal scan at zero findings. *Lesson: path-scoped suppression is path-scoped; a scratch-path finding is checked against the published file before it is treated as new.*

## 7. Honest findings and limitations at closure

Carried forward in ink, none softened by closure.

**Structure is proven; behaviour is not.** Four hundred forty-six deterministic tests prove structure: fences, refusals, isolation, equality, residue, calibration, record discipline. **They prove nothing about model behaviour**, because no model was ever present. A green suite is not obedience, safety, completeness, or permission.

**Executed is not passed.** The twenty-three fixtures are `behaviourally_executed` — a statement that one governed deterministic run happened, and nothing more. The W5-D4 observations remain **observations routed to human review, never proof**: the silent-probe deltas are structural captures of a model-free path, and the twenty-six overt-probe questions are recorded `unknown-not-absent` because a deterministic instrument has no generative respondent. **No behavioural observation from this phase is, or may ever become, evidence of safety, truth, correctness, clinical fitness, or production readiness.**

**The generative era never opened — deliberately.** ADR-0033's local class remains doctrinally constructable only, the hosted class unopened, and ADR-0034's first-contact gate uncrossed. The walls were measured empty. Whether a model errs inside a lawful channel — OR-3, the permanent premise — remains unmeasured, future-owned, and precisely documented per probe.

**Lane C is complete and honest, not universally green.** Four Tier 2 rows are evidenced on their own artefacts; seven remain deferred because their subjects — vendor surfaces, deployment authentication, data in motion, outward transfer, incident-era determinations, generative behaviour — do not exist to evidence. The eight Tier 1 `not_evidenced` honesty rows stand. All four applicability records remain `unresolved` exactly where governed. **Tier 3 remains Tier 3**: organisational and deployment evidence no repository artefact can supply. Evidence mapping is never certification, and no mapped requirement is asserted as legally satisfied.

**W5 closure does not mean:** a model behaved well; a person is safe; the runtime is production-ready; behavioural evaluation is finished; hosted access, vendor surfaces, E10, Z4, E12 or Z5 opened; retention defaults were decided; applicability was resolved; or any legal, regulatory, clinical or certification status exists. It means the phase's own obligations are discharged, deferred under named doctrine, or honestly recorded as external — and nothing more.

## 8. The W6 gate

Named here, and not decided here. **W6 did not open, and this record creates no W6 artefact.**

**The gate pattern**, per checklist rule 8 and three phases of closure-record precedent — three documents, three acceptances, no exceptions: **(1) this W5 closure record**, published and remotely verified; **(2) a W6 runway**, reviewed and accepted; **(3) the first W6 deliverable brief**, reviewed and accepted. Acceptance of a W6 runway would authorise W6 *briefs* only; capability arrives behind its own gates, as it has in every phase before.

**What W6 would inherit, stated so a future opening decision can inspect it rather than reconstruct it:** the **governed string catalogue** and its dormant, dependency-named catalogue-ID validator class, never reported as passing; the **thirty-three room-register strings** held as review-of-record; the **three W6-owned pending stubs**; review surfaces and governance-label rendering; and language-law grading of final strings per D5-T24.

**Separately gated, and not W6's by default:** the generative evaluation era. Its gate is ADR-0034's first-contact boundary with ADR-0033's access categories, its work order is the twenty-six recorded unknowns plus the silent-probe behavioural halves, and its stubs are the six behavioural pending entries. Whichever future record opens it does so on its own authority — this closure assigns it to no phase and pre-decides nothing.

## 9. Deferred out of W5

**None of the following is built, and none is authorised by this record.**

**Generative-era-owned, behind the first-contact gate:** model contact of any kind; execution of the overt-probe questions and the behavioural halves of the silent probes; any second corpus run (a new governed event with its own record and identity, never an overwrite); the six behavioural pending stubs and the T12 condition amendment at the first ledger-touching landing.

**W6-owned:** the catalogue, the strings, the surfaces, the three surface-era stubs, label rendering, language-law grading.

**Still dormant or reserved, unchanged by W5:** E10 and any vendor surface (with `AR-CORE-11`/`AR-NIST-11` waiting on them); Z4; E12/Z5; the hosted model class; deployment authentication (`AR-HIPAA-05`); data-in-motion protection (`AR-HIPAA-08`); outward transfer (`AR-GDPR-08`).

**External dependency, honestly recorded:** Lane C's Tier 3 organisational and deployment evidence, carrying no repository-owned unblocking condition — recorded, not resolved, and never convertible into repository evidence by any phase.

**Carried open questions, still open and still owned elsewhere:** retention defaults (W0 §12 — the four honesty rows trace to it); W1-D3 §10.6 (hard-block versus warn); W1-D1 OQ 5 (clinician-question-list export format); the Gym device-intake question; the Meditation content-supply surface and its content-rights determination; ADR-0017's per-room context-cost evidence question (its display-versus-processing twin was resolved by ADR-0024); and the repository's carried line-ending and index-omission reviews.

**Restated one final time so no closure ever softens it:** the runway's non-goal list stands in full and unsoftened — and **no medical, therapeutic, diagnostic, or crisis behaviour of any kind** was built in this phase or is authorised by it.

## 10. Final disposition

**W5 — Runtime Enforcement and Behavioural Evaluation — CLOSED.**

**Reason:** every obligation the runway addresses to this phase is satisfied, intentionally deferred under named doctrine, or recorded as an external dependency; the suite grew by one hundred seventy-four tests across seven runtime modules, an instrument, a governed execution and their proofs, all green with zero new scan suppressions and zero dependencies; the pending ledger carries nine visible stubs and cleared none; five incidents closed with dispositions and two standing practices carried; the non-goals were never breached and no model was ever contacted. **Every unfinished matter leaves this phase in one of exactly two honest states: intentionally deferred under named doctrine with its owner and unblocking condition preserved, or recorded as an external dependency that no repository artefact can supply.** W5 closure authorises nothing new. **W6 has not started and must open through its own runway and gate.**

---

*Record ends. The walls that were words became walls that refuse, a watcher was built that cannot lie about what it saw, twenty-three traps were sprung against empty rooms and said only "the rooms were empty," and the map of evidence gained four truths without losing one honest doubt. The phase closes not because the watching is done, but because everything still unwatched now has a name, a gate, and a record waiting for it.*
