# Phases

**Build philosophy: capability follows governance, not the other way around.**

A phase is closed when its deliverables are complete, reviewed, and checked against the Constitution. No phase begins before the prior phase closes.

## W0 — Constitution

**Scope:** The governing document — purpose, non-goals, core laws, room boundaries, the Health Vault pattern, privacy-by-design principles, agent boundaries, risks and failure modes, open questions.

**Deliverable:** [`../constitution/W0-wellbeing-wing-constitution.md`](../constitution/W0-wellbeing-wing-constitution.md)

**Status:** Accepted by human reviewer, 2026-06-12 (v0.1.1, W0.1 cleanup pass applied) — the binding governance document for all subsequent phases.

**Closes when:** the Constitution is approved as the binding governance document for all subsequent phases.

## W1 — Governance Architecture and Data Boundary Design

**Scope:** Still no UI and no production code. Per Constitution §13, W1 delivers:

1. **Data boundary map** — every data category, its sensitivity class, its home (Vault / Profile / room records / audit), and every permitted flow, with Laws 4, 8, and 9 expressed as explicit edges that do or do not exist.
2. **Consent and scope model** — the grant object (who, what, why, how long), revocation behaviour, and resolution of Open Question 2 (revocation cascade).
3. **Authority and staleness model** — authority labels, last-reviewed timestamps, re-review triggers, and supersession (Laws 3 and 7), defined before any schema exists.
4. **Safety surfacing decision document** — resolution of Open Question 1 (Law 12 mechanics) as its own reviewed decision record.
5. **Threat model** — Vault-centred security analysis and the local-first vs hosted decision (Open Question 6), which should be decided **first**, since every other W1 deliverable inherits from it. *Keystone resolved: [ADR 0001](../decisions/0001-local-first-user-held-keys.md) (Accepted) settles the local-first vs hosted decision via user-held keys; remaining W1 deliverables inherit from it.*
6. **Evaluation plan skeleton** — which constitutional laws map to deterministic tests vs behavioural evaluation (Open Question 12).

**Status:** Closed — all six deliverables accepted by human reviewer, 2026-06-12 (D1–D3, D5, D6 in [`../architecture/`](../architecture/); D4 landed as [ADR 0002](../decisions/0002-safety-surfacing.md); ADR 0001 resolved the keystone). Closure criteria per W1-D6 §8.

## W2 — Governance Evaluation & Enforcement Foundations

**Scope:** Translate the W1 corpus into enforceable scaffolding and evaluation readiness — verification only, nothing user-facing. The only code W2 may contain is code that checks rules.

**Runway:** [`W2-alignment-report.md`](W2-alignment-report.md)

**Status:** Closed — all deliverables landed and published; sealed by [`W2-closure-record.md`](W2-closure-record.md), accepted by human reviewer, 2026-07-05.

**Deliverables:**

| Deliverable | Document | Status |
|---|---|---|
| W2-D1 — W1 Closure Record | [`W2-D1-w1-closure-record.md`](W2-D1-w1-closure-record.md) | Accepted by human reviewer, 2026-06-13 |
| W2-D2 — Governance Registry | [`W2-D2-governance-registry-brief.md`](W2-D2-governance-registry-brief.md) · registry at [`../governance/registry.md`](../governance/registry.md) | Accepted by human reviewer, 2026-07-05 |
| W2-D3 — Phase Entry Checklist | [`../governance/checklist.md`](../governance/checklist.md) | Accepted by human reviewer, 2026-07-05 |
| W2-D4 — Synthetic Fixture Strategy | [`../governance/fixtures.md`](../governance/fixtures.md) · seed set in [`../../fixtures/`](../../fixtures/) | Stages A and B accepted by human reviewer, 2026-07-05 (Stage B landed after the W2-D6 landing-mode scan) |
| W2-D6 — Public-Safety Scan | [`../../scripts/README.md`](../../scripts/README.md) (tooling; executes before W2-D5, IDs stable) | Accepted by human reviewer, 2026-07-05 |
| W2-D5 — Deterministic Test Skeleton | [`../../tests/README.md`](../../tests/README.md) (tooling; three tiers + skip ledger) | Accepted by human reviewer, 2026-07-05 |
| W2 Closure Record | [`W2-closure-record.md`](W2-closure-record.md) | Accepted by human reviewer, 2026-07-05 |

## W3 — Health Vault and Health Profile Foundations

**Scope:** The first product-spine phase: the Health Vault as evidence layer, the Draft Health Profile as derived review layer, the Approved Health Profile as working context — built on the no-AI authority path, with user review before anything becomes active. No adapters, no models, no clinical logic, no notifications, no hosted anything.

**Runway:** [`W3-runway-health-vault-profile-foundations.md`](W3-runway-health-vault-profile-foundations.md)

**Status:** **W3 — Health Vault and Health Profile Foundations — is complete and closed** (W3-D1 through W3-D6 sealed, W3-D7 phase closure published, 2026-07-06; see [`W3-closure-record.md`](W3-closure-record.md)). **W4 has not started and must open through its own runway/gate.**

**Deliverables:**

| Deliverable | Documents | Status |
|---|---|---|
| W3-D1 — Plaintext Residue and Vault Encryption Doctrine (pair) | [`0004-plaintext-residue-policy.md`](../decisions/0004-plaintext-residue-policy.md) · [`0005-vault-encryption-stack-doctrine.md`](../decisions/0005-vault-encryption-stack-doctrine.md) | Accepted by human reviewer, 2026-07-05 — one atomic landing; neither record authorises implementation on its own |
| W3-D1 — Platform Stack Doctrine and Development-Artifact Policy (pair, Landing A) | [`0006-runtime-platform-stack-doctrine.md`](../decisions/0006-runtime-platform-stack-doctrine.md) · [`0007-development-artifact-policy.md`](../decisions/0007-development-artifact-policy.md) | Accepted by human reviewer, 2026-07-05 — one atomic landing; the evaluation spike becomes runnable only after publication, outside the repo, under the landed rules |
| W3-D1 — Runtime Stack Final Selection | [`0008-runtime-stack-final-selection.md`](../decisions/0008-runtime-stack-final-selection.md) | Accepted by human reviewer, 2026-07-05 — engine-spine selection on first-pass evidence (local-process class); the first binding installation remains a future, separately authorised fence-crossing |
| W3-D1 — Import Boundary and Minimal Review Posture (pair, Landing B) | [`0009-import-file-boundary.md`](../decisions/0009-import-file-boundary.md) · [`0010-minimal-review-posture.md`](../decisions/0010-minimal-review-posture.md) | Accepted by human reviewer, 2026-07-05 — one atomic landing; the engine brief consumes the pair together; neither record authorises implementation on its own |
| W3-D1 — Backup Guidance and Key-Loss Wording (trust-sentences pair, Landing C) | [`0011-local-backup-guidance.md`](../decisions/0011-local-backup-guidance.md) · [`0012-key-loss-onboarding-wording.md`](../decisions/0012-key-loss-onboarding-wording.md) | Accepted by human reviewer, 2026-07-05 — one atomic landing; consequence and remedy in one breath; completes the W3-D1 cluster (8/8); neither record authorises implementation on its own |
| W3-D2 — Engine Milestone 1: store skeleton (first product landing) | [`../../engine/`](../../engine/) · [`../../requirements.txt`](../../requirements.txt) | Accepted by human reviewer, 2026-07-05 — Tier F: one product directory, one exact-pinned dependency set, self-testing fence amendment; test-supplied keys only; import, derivation, custody, and export deferred to their milestones |
| W3-D2 — Engine Milestone 2: import path | [`../../engine/`](../../engine/) · [`0009-import-file-boundary.md`](../decisions/0009-import-file-boundary.md) | Accepted by human reviewer, 2026-07-05 — Tier J: intake operation per ADR 0009 (bytes plus user-supplied provenance in, one sealed record out; type verification capped at magic-number/shape depth; provisional 25 MB operational cap, not doctrine); interpretation of every kind, vault layout/naming/indexing, envelope finalisation, key custody, and export remain deferred to their own decisions |
| W3-D2 — KDF, Custody, and Envelope Selection | [`0013-kdf-custody-envelope-selection.md`](../decisions/0013-kdf-custody-envelope-selection.md) | Accepted by human reviewer, 2026-07-05 — closes ADR 0005's two named opens: moderate KDF profile (evidence-based, micro-spike, review-dated provisional), passphrase-alone custody with no recovery path in any form, two-layer wrap architecture, per-record envelopes; the implementing milestone remains separately authorised |
| W3-D2 — Engine Milestone 3: key custody | [`../../engine/`](../../engine/) · [`0013-kdf-custody-envelope-selection.md`](../decisions/0013-kdf-custody-envelope-selection.md) | Accepted by human reviewer, 2026-07-06 — Tier J: ADR 0013 as code — versioned key envelope (own file, caller-chosen path), two-layer wrap, moderate-profile derivation, passphrase change re-sealing the envelope only; no keyfile, no recovery path in any form; first-run wording, strength posture, vault layout, and backup/export mechanics remain deferred |
| W3-D2 — Engine Milestone 4 (Landing 1): residue at scale and format seam | [`../../tests/`](../../tests/) · [`../../engine/README.md`](../../engine/README.md) | Accepted by human reviewer, 2026-07-06 — Tier J: synthetic vault at scale (all-ciphertext property proven at every checkpoint, including after a killed process), cross-format refusal matrices, and the engine's in-ink documentation (honest residuals, threat-register entries, non-atomic-write finding); atomic write remains a possible future decision; D2 closure follows as its own landing |
| W3-D2 — Closure Record (Landing 2) | [`W3-D2-closure-record.md`](W3-D2-closure-record.md) | Accepted by human reviewer, 2026-07-06 — Tier J: seals the engine spine — published chain cited by commit, verification state at closure, honest findings carried forward, deferred list preserved; authorises nothing to be built; W3-D3 not started |
| W3-D3 — Profile Object Model, Milestone 1: the grammar as shapes | [`../../engine/core/profile.py`](../../engine/core/profile.py) | Accepted by human reviewer, 2026-07-06 — Tier J: W1-D3's authority/staleness grammar as pure data structures — inseparable label pairs, bounded unknowns, contradiction and supersession shapes, the Approved layer as shape only (no instance path exists), ledger event shapes, staleness as a pure function over injected intervals (no clinical defaults); no persistence, no transitions, no review path — those remain W3-D3 M2 and W3-D4 matters under their own gates |
| W3-D3 — Profile Object Model, Milestone 2: sealed profile persistence | [`../../engine/core/profile_records.py`](../../engine/core/profile_records.py) | Accepted by human reviewer, 2026-07-06 — Tier J: profile items and bounded unknowns sealed through the store path unchanged; in-payload typing with evidence-by-absence (no record format v2); truth-label persistence refused at the only write path per the minimal-review-posture record; loading reconstructs through the object-model constructors; contradiction/supersession/ledger-event persistence and the transition engine remain deferred to W3-D4 under its own gates |
| W3-D3 — Closure Record | [`W3-D3-closure-record.md`](W3-D3-closure-record.md) | Accepted by human reviewer, 2026-07-06 — Tier J: seals the Health Profile object model — published chain and the seven planning rulings cited, verification state at closure, honest findings carried forward (including the completeness nuance: transition-era shapes exist but do not persist), deferred list preserved; authorises nothing to be built; W3-D4 not started |
| W3-D4 — Transition Engine, Milestone 1: catalogue, matrix, and validator | [`../../engine/core/transitions.py`](../../engine/core/transitions.py) | Accepted by human reviewer, 2026-07-06 — Tier J: all eight W1-D3 transitions catalogued (understanding, never permission); the runnable matrix as a whitelist with refusal by absence; a pure content-free validator classifying runnable/gated/dormant/illegal; T1 catalogued but dormant until grant machinery exists by its own authority; T2/T4/T7/T8 recognised as gated — no application, no user acts, no events, no persistence change; the applier and its structural guards remain W3-D4 M2 under its own gates |
| W3-D4 — Transition Engine, Milestone 2: applier with user-act gating | [`../../engine/core/transitions.py`](../../engine/core/transitions.py) | Accepted by human reviewer, 2026-07-06 — Tier J: per-transition appliers validating through the classifier, in-memory only, emitting exactly one data-only ledger event per application (emitted, never stored — durable ledger deferred to its own decision); user acts as data, constructible only in the test tree within repository enforcement (AST guard landed in the same commit); truth labels computable in memory during gated synthetic tests and still unpersistable; T7 composite implemented (supersede + mint correction, retained history); T8 supersession only, no reactivation; contradiction/supersession persistence remains deferred |
| W3-D4 — Closure Record | [`W3-D4-closure-record.md`](W3-D4-closure-record.md) | Accepted by human reviewer, 2026-07-06 — Tier J: seals the transition engine — published chain (M1, M2, and the pending-ledger cleanup) cited by commit, the planning-brief and in-flight rulings restated, verification state at closure, honest findings carried forward, deferred list preserved; authorises nothing to be built; W3-D5 and later deliverables not started |
| W3-D5 — Closure Record (mapping-based) | [`W3-D5-closure-record.md`](W3-D5-closure-record.md) | Accepted by human reviewer, 2026-07-06 — Tier J: seals residue rules as a deliverable satisfied by absorption — no separate D5 milestones existed; the expected work landed inside the published D2/D3/D4 evidence discipline, and the record carries the full ADR 0004 clause-by-clause mapping with proof names and commit anchors, two honest notes, and the forward gates; authorises nothing to be built |
| W3 — Durable-Ledger Doctrine (D6 gate) | [`0015-durable-ledger.md`](../decisions/0015-durable-ledger.md) | Accepted by human reviewer, 2026-07-06 — Tier J: the durable ledger exists as doctrine — sealed under the master-key custody boundary, independently sealed append frames, doctrine-wide event scope with v1 limited to existing transition events, backup-unit membership as sealed ciphertext, erasure as the user's knowing act only; the implementation milestone remains unstarted, and D6 remains gated until its sequencing is authorised; authorises no implementation |
| W3 — Durable-Ledger Store (implementation milestone) | [`../../engine/core/ledger_store.py`](../../engine/core/ledger_store.py) | Accepted by human reviewer, 2026-07-06 — Tier J: ADR 0015 as code — the WBWL versioned ledger file of independently sealed append frames under the master key; append order preserved and proven bytewise append-only; torn tail refused with intact history preserved (one-event blast radius); whole-ledger erasure as an explicit act; the appliers untouched (the writer is caller-side); import/custody emission remains a named future extension; backup/export mechanics remain D6 |
| W3-D6 — Milestone 1: backup export and restore symmetry | [`../../engine/core/backup.py`](../../engine/core/backup.py) | Accepted by human reviewer, 2026-07-06 — Tier J: the WBWB portable ciphertext file — public header, reachable key envelope, sealed payload holding manifest, records, and ledger with no structure visible outside it; restore is file-plus-passphrase into an empty target only, five-step validation before any write, no record plaintext decrypted to restore; member sets caller-supplied (the engine never discovers files); single-record export (M2) and the D6 closure (M3) remain under their own gates |
| W3-D6 — Milestone 2: export as a right | [`../../engine/core/export.py`](../../engine/core/export.py) | Accepted by human reviewer, 2026-07-06 — Tier J: single-record plaintext export at the data layer — the imported bytes leave byte-identical under no gate but the owner's act; provenance always returned as structured data, written as a sidecar only when explicitly requested; destinations validated before any write, existing destinations refuse, profile-class records refuse (their export deferred to its own governed path); no batch, no whole-vault plaintext export (own future record), no UserAct or review machinery; the D6 closure (M3) remains under its own gate |
| W3-D6 — Closure Record | [`W3-D6-closure-record.md`](W3-D6-closure-record.md) | Accepted by human reviewer, 2026-07-06 — Tier J: seals export/backup/restore — published chain cited by commit; the seven planning rulings (headlined by the WBWB circularity correction) and two M2 rulings restated; consumed ADR 0015 inputs named; the WBWB container shape, five-step restore validation order, and export-right contract carried in ink; honest findings and deferred list preserved; authorises nothing to be built; W3-D7 not started |
| W3-D7 — W3 Closure Record | [`W3-closure-record.md`](W3-closure-record.md) | Accepted by human reviewer, 2026-07-06 — Tier J: seals the phase — the six deliverable seals cited by commit, criteria assessed against the runway, verification state, the nine future-owned pending stubs named, the incident log (five entries with lessons), the honest-findings roll-up, the W4/W5 gate naming next-era draft material without pre-deciding it, and the deferred list; authorises nothing to be built; W4 not started |

## W4 — Room Contracts

**Scope:** The first jurisdiction phase: four room contracts — Wellness, Kitchen, Gym, Meditation — each a written, testable statement of what a room may read, may write, must never infer, and how it behaves when relied-on context is unknown, stale, or contradicted. Built on the sealed engine spine and the W1 data-boundary map. No adapters, no models, no review surfaces, no UI — the contracts constrain future capability; they are not capability.

**Runway:** [`W4-runway-room-contracts.md`](W4-runway-room-contracts.md)

**Status:** **The three-document W4 entry gate is complete:** the W3 closure record is gate document one, the W4 runway is gate document two, and the accepted DR-W4-01 first-deliverable brief is gate document three. **ADR 0016 (Room Contract Template) is the first landed W4 doctrine record, and W4-D2 (Wellness Room Contract) is the first landed room contract.** **W4 opened for governed doctrine, brief, and room-contract work only, and is now closed:** W4-D1 through W4-D7 are complete, with every runway obligation satisfied, intentionally deferred under named doctrine, or honestly recorded as an external dependency; the closure became effective on publication and authoritative remote verification of the W4-D7 closure record, under the same effective-on-publication rule stated below. Named W5, W6, and external matters remain carried and unresolved, and closure authorises nothing new. That opening authorised no scaffolding, no validator implementation, no adapter work, no review surfaces, no engine changes, and no W5/W6 implementation. Every later W4 record and deliverable still requires its own brief, review, and ceremony. *Opening is effective on publication:* while the ADR-0016 commit remains local it is not yet in force; W4's opening takes effect only once ADR-0016 is published on `origin/main` and verified. The deliverable sequence ran W4-D1 (six room-contract doctrine records) through W4-D7 (closure), with forward dependencies on the W5 adapter layer (isolation enforcement) and the W6 governed string catalogue (wording routing) named and carried, not blocking — and still carried, not resolved, at closure.

**Deliverables:**

| Deliverable | Documents | Status |
|---|---|---|
| W4-D1 — Room Contract Template (DR-W4-01) | [`../decisions/0016-room-contract-template.md`](../decisions/0016-room-contract-template.md) | Accepted by human reviewer, 2026-07-10 — Tier J: the fixed eleven-section room-contract template (fixed order and numbering, every section present and non-blank, "not applicable" written with a reason, a mandatory eleventh open-questions section reading "None at acceptance." when empty, and the Meditation reads-nothing case as the worked N/A example); decides the skeleton only — it fills no room's sections and gates the four room contracts (W4-D2…D5) and the conformance validator (W4-D6); no contract, validator, adapter, surface, or engine code; wording sections route to the future W6 governed string catalogue as review-of-record |
| W4-D1 — Room Isolation Model (DR-W4-02) | [`../decisions/0017-room-isolation-model.md`](../decisions/0017-room-isolation-model.md) | Accepted by human reviewer, 2026-07-10 — Tier J: the architectural room-isolation standard that fills every contract's Processing Boundary section — each room's processing interaction occurs in a context that has never contained another room's content, cross-room content is unreachable (not merely forbidden), one processing grant binds exactly one room, and filtering or output controls alone are insufficient; grant scoping and isolation are separate complementary walls; W5 later realises the property while W4 specifies no mechanism, behavioural evaluations still apply, and any exception needs its own accepted record with no silent downgrade; the display-vs-processing-context split and per-room context-cost evidence remain deferred to W5; no adapter, session, prompt, payload, validator, surface, or engine code — no implementation authorised; the Meditation Room receives the uniform standard, its no-outward-signal property left to DR-W4-04 |
| W4-D1 — Read/Write Scope Confirmation (DR-W4-03) | [`../decisions/0018-read-write-scope-confirmation.md`](../decisions/0018-read-write-scope-confirmation.md) | Accepted by human reviewer, 2026-07-10 — Tier J: W1-D1 is the sole source of truth for every room's read and write scope; contracts carry scope only through the paired form — an exact W1-D1 reference as controlling authority plus a complete verbatim quotation for legibility — and a quotation/reference mismatch fails review; paraphrase is prohibited, and no contract may widen, narrow, reinterpret, simplify, modernise, or clarify a D1 scope; scope amendments land in W1-D1 first and contracts follow, never lead; absence of a D1 edge means absence of authority; rooms write only their own authorised record classes with the D3-T3 label-on-entry by reference, and no room writes into another room's jurisdiction; records that direct reading of live W1-D1 exposed drift in the pack/runway summaries (D1 wins — those are planning materials, not scope authority); confirms and transmits scope, mints none; no adapter, validator, surface, or engine code — no implementation authorised |
| W4-D1 — Cross-Room Inference Prohibition Standard (DR-W4-04) | [`../decisions/0019-cross-room-inference-prohibition-standard.md`](../decisions/0019-cross-room-inference-prohibition-standard.md) | Accepted by human reviewer, 2026-07-11 — Tier J: a room must not derive a health-relevant or room-jurisdiction conclusion outside its granted context — forbidden inference is constitutionally prohibited (even when user-requested), and observable consequence is how a breach becomes testable, never permission to infer silently, with silent behaviour change the primary target; hedging, questions, and user attribution do not launder it; every future room contract requires a room-specific named-bait list (a floor, not a ceiling; each bait mapped one-to-one to a future evaluation fixture) — but ADR-0019 authors no room's bait list; named-bait lists are the highest public-safety surface, canonical grammar placeholders only; user-requested speculation inherits the settled refusal shape and mints no claim, restriction, recommendation, warning, or authority state; room processing has no pattern-flag or "for review" pathway; a confirmed breach is highest-defect with a permanent regression fixture and output filtering alone is never an adequate remedy; complementary to ADR-0017 (isolation) and ADR-0018 (scope), with the Meditation Room on the uniform standard; no fixture, adapter, validator, surface, or engine code — no implementation authorised |
| W4-D1 — Unknown/Stale/Contradicted Behaviour Standard (DR-W4-05) | [`../decisions/0020-unknown-stale-contradicted-behaviour-standard.md`](../decisions/0020-unknown-stale-contradicted-behaviour-standard.md) | Accepted by human reviewer, 2026-07-11 — Tier J: the one shared behaviour table for all four rooms when relied-on context is unknown, stale, expired, or contradicted, with W1-D3 as controlling authority — all six state labels (current, review due, stale, expired, superseded, unknown freshness) carried with their verbatim semantics; every contract carries the table by exact reference plus complete verbatim quotation, and rooms instantiate wording, not rules; unknown means unknown and absence is never confirmation; contradiction is held honestly and newer is not truer until the user says so; the safety-relevant inline uncertainty sentence is always shown, not grace-conditioned; acknowledgement-before-continuing for safety-relevant expired/contradicted/unknown context is a surfacing-order requirement, not a functional block; most-protective framing is surfacing/verbal only and mints nothing; block-versus-warn remains open under W1-D3 §10.6 with the §6.4 floor in every row; final strings and catalogue IDs belong to the future W6 governed string catalogue (review of record until then); no behaviour-table implementation, catalogue, fixture, adapter, validator, surface, or engine code — no implementation authorised |
| W4-D1 — Contract Validator Requirements (DR-W4-06) | [`../decisions/0021-contract-validator-requirements.md`](../decisions/0021-contract-validator-requirements.md) | Accepted by human reviewer, 2026-07-15 — Tier J: the sixth and final W4-D1 doctrine record — decidability, not implementation cost, draws the validator line: a contract term is mechanically checkable if and only if its correctness is decidable from deterministic repository artefacts without interpretation, and every term is mechanically checkable, review-only, or explicitly decomposed — no ambiguous middle; judging validators are prohibited (a judging validator is doctrine minting by machinery); contract validators inspect deterministic repository artefacts while behavioural and runtime checks remain evaluation territory; failure evidence is minimal and public-safe; a mechanical pass does not equal acceptance; validators extend the existing deterministic suite with phase-named tests; DR-W4-06 (doctrine) and W4-D6 (plan/build) remain separate, and validator implementation waits until all four room contracts are accepted; the W6 catalogue-ID class remains dormant and dependency-named; no implementation authorised — with this record's publication the W4-D1 doctrine set is 6 of 6, and W4 remained open at this landing for governed contract work only, not implementation |
| W4-D2 — Wellness Room Contract | [`../rooms/wellness-room-contract.md`](../rooms/wellness-room-contract.md) | Accepted by human reviewer, 2026-07-22 — Tier F: the first of four room contracts (Wellness); all eleven ADR-0016 sections present and non-blank; read and write scope carried only through ADR-0018's paired form (exact W1-D1 reference plus complete verbatim quotation); a five-item named-bait floor (WELL-B1…B5 — a floor, not a ceiling — each declaring exactly one future W4-D6 evaluation fixture that does not yet exist); ADR-0020's one shared behaviour table carried verbatim across all six W1-D3 states with contradicted context; ADR-0017 isolation declared, not operationally enforced (W5 realises it); wording routes to the future W6 governed string catalogue as review-of-record; W5 and W6 remain deferred and unread; no validator, fixture, adapter, surface, or engine code — no implementation authorised |
| W4-D3 — Kitchen Room Contract | [`../rooms/kitchen-room-contract.md`](../rooms/kitchen-room-contract.md) | Accepted by human reviewer, 2026-08-04 — the second of four room contracts (Kitchen); all eleven ADR-0016 sections present and non-blank; E6 read scope (allergies, confirmed dietary requirements only), C1 write scope (recipes/preferences, grocery/staples, meal plans), and the E10 outward vendor boundary carried in ADR-0018's paired form; category #11 embeds E6 under a C3-handling rule while remaining C1; a five-item named-bait floor (KITCH-B1…B5 — a floor, not a ceiling — each declaring exactly one future W4-D6 evaluation fixture that does not yet exist); ADR-0020's shared behaviour table carried verbatim across all six W1-D3 states with contradicted context, allergy the live safety-relevant read; ADR-0017 isolation declared, not operationally enforced (W5); the E10 payload is bare item identity only, declared as a boundary only — the contract establishes no connector or VendorAdapter, claims none has been implemented or verified, and authorises none; wording routes to the future W6 governed string catalogue; no implementation or transmission authorised |
| W4-D4 — Gym Room Contract | [`../rooms/gym-room-contract.md`](../rooms/gym-room-contract.md) | Accepted by human reviewer, 2026-08-09 — the third of four room contracts (Gym); all eleven ADR-0016 sections present and non-blank; E7 read scope (confirmed injury/physical notes only), mixed C1+C2 write scope (Workout plans #12 at C1, Movement/rest/recovery logs #13 at C2), and the User-reported label on entry carried in ADR-0018's paired form; user-reported non-clinical sleep accepted as a category-#13 instance; a six-item named-bait floor (GYM-B1…B6 — a floor, not a ceiling — each declaring exactly one future W4-D6 evaluation fixture that does not yet exist), GYM-B5 (injury-absence-to-all-clear) highest priority; ADR-0020's shared behaviour table carried verbatim across all six W1-D3 states with contradicted context, injury the live safety-relevant read with a retrospective framing (an expired injury note may still apply, never healed); ADR-0017 isolation declared, not operationally enforced (W5); manual-first with current device ingestion, provenance, adapters, and derived scores excluded and no device-integration authority; no outward edge; wording routes to the future W6 governed string catalogue; no implementation or transmission authorised |
| W4-D5 — Meditation Room Contract | [`../rooms/meditation-room-contract.md`](../rooms/meditation-room-contract.md) | Accepted by human reviewer, 2026-08-09 — the fourth and final room contract (Meditation); all eleven ADR-0016 sections present and non-blank; the CM contemplative jurisdiction, a sensitivity peer of C4 behind Law 9's structural wall and never a rung of the health ladder; read scope stated from the complete edge list M1–M2 plus the “no other edges” clause in ADR-0018's paired form, with the template's worked N/A case carried for inbound other-room reads — no inbound Profile or cross-room read edge exists, while M1 (the user and their own Meditation records) remains a right, not a grant; write scope own records only — Meditation practice records #14, Reflections & contemplative notes #15, Contemplative library & annotations #16 (User-curated), all CM, User-reported on entry; silent practice writes nothing by default and an explicitly logged silent sit is a #14 record; breath awareness only, with no wider breathwork authority; persistent preferences out of scope; no movement, rest, recovery, or sleep category acquired; a seven-item named-bait floor (MED-B1…B7 — a floor, not a ceiling — each declaring exactly one future W4-D6 evaluation fixture that does not yet exist), MED-B3 and MED-B7 making the Law 9 wall testable outward and inward, with MED-B7 preserving the lawfulness of the user's own free-text reflection whatever subject it mentions; ADR-0020's shared behaviour table carried verbatim across all six W1-D3 states with a structurally different instantiation stated as such — no source-authorised CM review interval, grace period, or hard limit exists, a date is not a freshness state, and therefore zero current Meditation-specific room-register strings are derived, a present finding and not a permanent claim; no live safety-relevant Profile context can enter, and that absence is not an absence of risk; M2 the only processing edge (user-initiated, nothing persisting outside the room, no output to any other room), ADR-0017 isolation declared, not operationally enforced (W5), and M2 output never represented as source material; the user-curated teaching or scripture library authorised while the content-supply surface remains deferred and no content-rights determination is made; wording routes to the future W6 governed string catalogue with zero currently routed; no implementation or transmission authorised — with this record the four room contracts are complete |
| W4-D6 — Contract Validator M10/M12 Decidability Correction (ADR 0022) | [`../decisions/0022-contract-validator-m10-m12-correction.md`](../decisions/0022-contract-validator-m10-m12-correction.md) | Accepted by human reviewer, 2026-08-09 — Tier J: the Lane A implementation correction to ADR 0021, raised by implementation rather than review. M12's anti-map clause could not be satisfied by any accepted room contract — every accepted section 8 names its granted boundary relationally (“beyond E7”, “beyond the E6 closed list”, “beyond M1 and M2”) rather than enumerating forbidden identifiers, so the empty-intersection pass condition failed all four; telling those apart would require reading prohibition semantics, which the decidability line excludes and R10 already owns. M12 is restated as M12a anti-map identifier fidelity against a frozen per-contract expectation (Wellness, Kitchen, Gym, Meditation) with missing and unexpected identifier as its mismatch classes, plus the unchanged M12b Meditation exact-edge rule; M10 becomes closed-set contract placeholder membership, since the canonical vocabulary is closed and no accepted source enumerates a rejected token — with a governed contract-token recognition grammar explicitly bounded away from fixtures. Corrected Lane A arithmetic: M10 4, M12 5, total 47 mechanical instances (set-level 2, Wellness 10, Kitchen 11, Gym 11, Meditation 13), review surface 37 unchanged. The discovery history is preserved: 46 provisional, 48 source-derived, implementation discovery, 47 corrected — implementation stopped rather than coding around doctrine. ADR 0021 keeps a non-semantic pointer and a logged erratum; no room contract is amended; no implementation is authorised, and Lane A remained suspended until this record was published |
| W4-D6 — Lane A Contract Conformance Validator | [`../../tests/test_w4_contract_conformance.py`](../../tests/test_w4_contract_conformance.py) | Accepted by human reviewer, 2026-08-09 — Tier J: the first W4-D6 build artefact — the Lane A contract-conformance validator and its frozen expectation data (`tests/grammar/w4-contract-expectations.json`, schema version 3), extending the deterministic suite with 47 mechanical check instances against the four accepted room contracts (set-level 2; Wellness 10; Kitchen 11; Gym 11; Meditation 13) under ADR 0021 as corrected by ADR 0022; every expected value frozen from the accepted baseline with dual provenance — immutable acceptance pairs and current governed-baseline pairs, both commit↔hash coherence-proven against repository history — so the validator never learns its expected answer from the artefact under test; M6 enforces strict byte fidelity after removal of blockquote transport syntax only; M10 is closed-set placeholder membership with no rejected-token literal anywhere; M12a is anti-map identifier fidelity and M12b the Meditation exact-edge rule; baseline drift is an orthogonal diagnostic against the governed hash, never a result state; quoted source regions are excluded from every structural extraction; 47 positive assertions, 27 negative mutation scenarios, 6 false-positive guards and 8 separately-counted provenance proofs, all runtime/in-memory with no persistent synthetic contract artefact and no behavioural fixture; execution precondition: Git CLI (already required by the suite's scan invocation) plus full repository history for the provenance proofs; a mechanical pass means document conformance only — human review and ceremony remain required, at Lane A closure no fixture or catalogue identifier existed; fixture identifiers later arrived under W4-D6-BEF, while no catalogue identifier exists, and no implementation or transmission is authorised |
| W4-D6 — Lane C External Assurance Source Register | [`../governance/external-assurance-source-register.md`](../governance/external-assurance-source-register.md) | Accepted by human reviewer, 2026-08-10 — Tier J: the governed provenance-and-currency register for the external authoritative sources consulted by privacy and health-data assurance work — fourteen primary official sources across the constitutionally named alignment families and the voluntary engineering baseline, under a fixed twelve-field schema separating what a source is from whether it currently stands, with immutable allocation identifiers, explicit null/empty semantics, visible supersession routing to dependent-row review, and re-verification-before-use maintenance rules; canonical primary-source locators are individually reviewed and narrowly allowlisted for this register alone, with external links remaining default-denied across the corpus; registration records evidence provenance only — it certifies nothing, determines no applicability, and the Wing makes no legal-certification claim; this row records the register artefact only — it does not claim Lane C closure or W4-D6 closure, and no implementation is authorised |
| W4-D6 — Lane C Privacy & Health-Data Assurance Record | [`../governance/privacy-health-data-assurance-record.md`](../governance/privacy-health-data-assurance-record.md) | Accepted by human reviewer, 2026-08-10 — Tier J: the governed evidence-mapping record for privacy and health-data assurance — sixty-five rows mapping thirteen stable primitives to repository evidence across a framework-neutral core, four alignment overlays (HIPAA-aligned, APP-aligned, GDPR-style, FTC-HBNR) and the voluntary NIST SP 800-122 engineering baseline; exactly sixteen stored fields per row with the assurance vocabulary derived at presentation and never stored; four applicability records all unresolved with no role or basis asserted — applicability unresolved is never not applicable and never a pass; three evidence-timing tiers separating repository evidence now, W5-dependent runtime evidence, and external organisational evidence that no repository artefact can green; honest states preserved — not-evidenced and externally dependent rows are stated as such, never inflated; in the same landing the Source Register's dependent-row fields were populated for the eleven cited sources through governed register maintenance, refreshing its registered content hash; evidence mapping only — the record certifies nothing, determines no applicability, and the Wing makes no legal-certification claim; the record and the External Assurance Source Register are both published and authoritatively remotely verified, the register's dependency rows populated with its registered content hash refreshed in the same commit; Lane C evidence mapping is complete and Lane C is closed under the accepted criterion — complete and honest, not universally green: closure does not mean every row is evidenced, all four regulatory/alignment applicability records remain unresolved with no role or basis asserted, evidence mapping is never certification and asserts no legal requirement as satisfied, and Tier 2 and Tier 3 dependencies remain exactly where recorded — W5 must not collapse Tier 3 evidence into repository evidence; Lane C closure closes Lane C only — Lane B remained separate and declaration-only at this closure (23 declarations, 0 fixtures, 0 fixture IDs) and W4-D6 as a whole was not declared closed at this closure; W5 and W6 remain unopened; no implementation is authorised |
| W4-D6 — Lane B Behavioural Evaluation Fixture Strategy | [`../governance/behavioural-evaluation-fixtures.md`](../governance/behavioural-evaluation-fixtures.md) | Accepted by human reviewer, 2026-08-11 — Tier J: the Lane B Stage A strategy extension that the Synthetic Fixture Strategy (W2-D4) names in its non-scope section — extends W2-D4, never replaces it; twenty-three fixture-reference obligations exist, one per named-bait declaration across the four sealed room contracts, and the accepted cardinality model derives twenty-three future fixture artefacts — derived, never a symmetry target; at this landing no fixture artefact exists, no FIX identifier has been allocated, and the authoritative live bait-to-fixture map is empty by authorised state; the sealed room contracts are never amended to allocate fixture references — this document's map is the authoritative live record of fixture existence; behaviourally_unexecuted is the sole W4 execution status, no runtime behavioural evaluation has occurred, and no behavioural success claim is made; Lane B is not closed by this landing, W4-D6 was not closed at this landing, Stage B (fixture authoring, FIX allocation, map population, corpus validator) remained separately gated on this document's publication and remote verification; W5 and W6 remain unopened; no implementation is authorised |
| W4-D6 — Lane B Behavioural Evaluation Fixture Corpus | [`../governance/behavioural-evaluation-fixtures.md`](../governance/behavioural-evaluation-fixtures.md) | Accepted by human reviewer, 2026-08-11 — Tier J: the Stage B corpus landing — twenty-three SYNTHETIC behavioural-evaluation fixtures, one per named-bait declaration across the four sealed room contracts, landed atomically with the twenty-three-row live bait-to-fixture map and the deterministic corpus validator (`tests/test_w4_lane_b_fixture_corpus.py`); fifty-one local probes at the derived floor; every fixture remains behaviourally_unexecuted with the named W5 execution dependency preserved; the validator proves corpus conformance and referential integrity only — no behavioural evaluation has occurred or passed, and static corpus validation is never behavioural evaluation success; the corpus, its map, and the validator are published and authoritatively remotely verified, and Lane B is closed under the accepted criterion — referenced, validated, and honestly unexecuted: twenty-three declarations resolve through twenty-three map rows to twenty-three published fixtures with bijection proven, the deterministic corpus validator is published and green, and closure means the behavioural-evaluation corpus is complete and statically validated — not that any behavioural evaluation has passed; every fixture remains behaviourally_unexecuted and the named W5 execution dependency is preserved; Lane B closure closes Lane B only — all obligations addressed to W4-D6 are discharged or intentionally deferred under named doctrine, and all three governed lanes are closed; W4-D6 is therefore closed — the validator plan and build delivered and green (Lane A: mechanical document conformance only), the behavioural-evaluation corpus referenced, validated, and honestly unexecuted (Lane B), and the assurance evidence disposition complete and honest, not universally green (Lane C); W4-D6 closure authorises nothing new — no W5 runtime, behavioural-evaluation execution, or downstream implementation, no conversion of deferred or external evidence into repository evidence, and no legal-certification claim; W4 remained open pending W4-D7 at that closure; W5 and W6 remain unopened; no W5 runtime, behavioural-evaluation execution, or downstream implementation is authorised |
| W4-D7 — W4 Closure Record | [`W4-closure-record.md`](W4-closure-record.md) | Accepted by human reviewer, 2026-08-12 — Tier J: the phase closure record, satisfying W4-D7 — the full W4 runway assessed with every obligation classified satisfied, intentionally deferred under named doctrine, or honestly recorded as an external dependency; the deliverable chain cited by commit; the suite measured at 281 total — 272 passed, 9 skipped, 0 failed, 439 subtests — extended by the phase's two phase-named validator surfaces rather than merely failure-free; the pending ledger reviewed with nine future-owned stubs and zero W4-owned items; four incidents closed with dispositions and the landing-hygiene practice carried forward; the W5 gate named with its inherited obligations and not decided; W6 remains unopened; **published and authoritatively remotely verified, and W4 is therefore closed — the closure took effect under the repository's publication rule when this record became authoritative on `origin/main` and that authority was verified; no later ledger entry creates it, this row records it**; nothing new is authorised |

## W5 — Runtime Enforcement and Behavioural Evaluation

**Scope:** The first runtime phase: realising at runtime the boundaries W4 declared — the processing-context boundary, grant machinery, payload assembly and equality, transmission and disclosure mechanics, architectural isolation, and the M2 runtime — and then measuring whether those boundaries hold, including on channels that produce no output. Two governed eras under one phase: adapter/runtime enforcement, and behavioural evaluation. No review surfaces, no governed string catalogue, no UI — those remain the surface era's.

**Runway:** [`W5-runway-runtime-enforcement-evaluation.md`](W5-runway-runtime-enforcement-evaluation.md)

**Status:** **W5 — Runtime Enforcement and Behavioural Evaluation — is complete and closed** (W5-D1 through W5-D5 sealed, W5-D6 phase closure published, 2026-08-17; see [`W5-closure-record.md`](W5-closure-record.md); closure effective on this record's publication). Complete and honest, not universally green: the runtime is structurally proven and the corpus deterministically executed with every generative-era question honestly `unknown-not-absent`; no model was ever contacted; the nine pending stubs remain visible and unconverted; observations remain observations, never proof, and no result anywhere is a safety, health, or certification claim. **W6 has not started and must open through its own runway/gate** (three documents, three acceptances — the closure record names the gate without opening it).

**Deliverables:**

| Deliverable | Documents | Status |
|---|---|---|
| W5-D1 — AIAdapter / Processing-Context Boundary (DR-W5-01) | [`../decisions/0024-aiadapter-processing-context-boundary.md`](../decisions/0024-aiadapter-processing-context-boundary.md) | Accepted by human reviewer, 2026-08-13 — Tier J: the first of seven candidate W5-D1 doctrine records and the AIAdapter ADR the sealed corpus has required since W1. Decides the processing boundary as a mechanism-independent property — one governed seam; no content accepted independently of a grant and no free-text interface; the boundary outside the sealed engine with separation both ways, a future external composition root permitted and orchestration left to W5-D2; grant-derived context construction with one-room binding structural and no two-room composition point; no hidden processing-state continuity while lawful retrieval under a fresh grant remains open; context lifetime bounded by the grant with no processing-side state surviving it; display context separated from processing context, answering ADR 0017's first carried question; four-surface observability by construction with propagation claims bounded at the recipient boundary and no claim about a recipient's internals; Z5 kept distinct while W5 activates no Z5 flow; all three E12 prerequisites settled at doctrine level (scoping, authentication, impersonation prevention) with **E12 expressly remaining reserved**; the content-free ledger obligation at the boundary; structural refusal with the audit distinction preserved; and ADR 0017's exception discipline extended to the whole record. Six architecture questions carried with named owners, including the ADR 0004 residue applicability check assigned to a W5-D1 unit before any W5-D2 implementation. Resolves W0 Open Question 10. No implementation, directory, dependency, model contact, payload, transmission, harness or fixture execution is authorised |
| W5-D1 — Runtime Freshness and Unknown/Stale Behaviour (DR-W5-02) | [`../decisions/0029-runtime-freshness-unknown-stale-behaviour.md`](../decisions/0029-runtime-freshness-unknown-stale-behaviour.md) | Accepted by human reviewer, 2026-08-14 — Tier J: the second of the seven W5-D1 doctrine records. Part A fixes freshness semantics — a freshness data type is a **governance policy key**, one key shape only, normative `R`/`G`/`H` with **`S = R + G` derived**, no per-field override, the fixed-duration day of exactly 24 elapsed hours, and **seven governed keys** (medication, allergy, condition, injury, pregnancy status, clinician instructions, preference). Every threshold is a **provisional governance default — not clinically validated**, and the six equal keys carry the **non-equivalence rule**: their equality is a uniform provisional governance simplification, **not evidence of clinical equivalence**. Freshness must be valid at every governed reliance; no background polling; acknowledgement is surfacing only and **never renewal**. Part B fixes runtime consequence — **expired remains the label while the assertion is consumed as unknown**, retaining assertion, provenance and age; bounded unknown never means absence, resolution or reassurance; **safeguard 14 is predicate-based** and stale can meet it; a fail-closed withholds **only the dependent operation or output**, never the user and never the room; three **derived, never-persisted** assertion postures; and ADR-0002 and ADR-0020 apply **independently within their own memberships**, with no precedence and no inferred non-member rule. **Resolves W1-D3 §10.1 and §10.6 in body**; the historical room-contract `Open (§10.6)` text is unchanged. Deterministic obligations are separated from behavioural proof; no implementation, directory, dependency, model contact, payload, transmission, harness or fixture execution is authorised |
| W5-D1 — Grant Machinery, Consent Duration and Re-authentication (DR-W5-03) | [`../decisions/0030-grant-machinery-consent-duration-reauthentication.md`](../decisions/0030-grant-machinery-consent-duration-reauthentication.md) | Accepted by human reviewer, 2026-08-15 — Tier J: the third of the seven W5-D1 doctrine records. Part A makes W1-D2's grant grammar **structurally normative** — all thirteen elements over one already-declared edge, and a missing element, undeclared edge, blanket scope or multiple purposes make a grant **unconstructable**. Lifecycle: **proposed / declined / active / awaiting required re-authentication / review-due / expired / revoked**; **no freshness vocabulary**, `stale` barred, `suspended` omitted, and `review-due` is **surfacing only**. **Re-affirmation creates a successor grant** with new identity, new validity interval and inspectable predecessor lineage — never an in-place extension; **expired and revoked never reactivate**; material change requires a **new grant**. Part B: E2 single-task with **fresh re-authentication at grant time**; profile scoped read standing to a **hard 180-day ceiling** after which it expires and **silence neither renews nor creates a successor**; AI processing single-task default, session maximum; E10 per transmission; E9 single-task; **no unbounded duration**. A session cannot silently revive, and **inactivity-based termination is non-operative** until a later governed decision supplies the threshold — which W5-D2 may not supply by discretion. Re-authentication proves **only present user control for the exact proposal**, in a fixed five-step order, and never becomes a reusable ambient flag. Part C: refusal withholds **only the dependent delegated operation** — never the person, never the room, never a right. **Resolves W1-D2 §8.1 and §8.2 / W1-D3 §10.7 in body.** Authorises no implementation, runtime directory, software/package dependency, UI, authentication mechanism, session-manager implementation, model access, vendor-hosted access, payload construction, transmission, fixture execution, pending-ledger change, Lane C, W6 or W5-D2 milestone |
| W5-D1 — Payload Assembly and Payload Equality (DR-W5-04) | [`../decisions/0031-payload-assembly-and-payload-equality.md`](../decisions/0031-payload-assembly-and-payload-equality.md) | Accepted by human reviewer, 2026-08-15 — Tier J: the fourth of the seven W5-D1 doctrine records. **Permission is not enough — the assembled payload must match the authorised scope.** A payload is lawful only if assembled from the exact authorised scope for the exact grant, matching edge, actor, recipient class, purpose, operation, duration, zones, plaintext status and vendor posture; **the granted scope is the whole payload** (W1-D6 §4.J), and nothing is added for helpfulness, safety, convenience, continuity, model quality or SDK default. **Forbidden:** SDK extras, device identifiers, helpful context, room state, history beyond the grant, cross-room or cached or adjacent context, inferred or derived enrichment, hidden instructions carrying state, and anything included **merely because it is already available**. Metadata is permitted **only where governed**; profiling, scoring and shadow-profile metadata are barred. **Equality** is governed canonical equality — byte-level **where applicable** at the **last controllable point, per boundary edge**; canonical, structural, field-level and deterministic otherwise. Similarity, intention-equivalence, output-equivalence and *close enough* **never** satisfy it, and representation differences may be ignored **only where the governed canonical form says so, never by runtime discretion**. **Three comparisons** are obligations: granted scope — assembled payload — last controllable point, plus pre/post-adapter where a transformation exists. **Z3 is governed now; Z4 remains DORMANT** — the Z4 limb stays **visibly pending**, **W5 may not claim it discharged**, E10 is not activated and no VendorAdapter ADR is authored. Content-free fingerprints permitted under five conditions, with **no hash, schema or storage prescribed**. Where equality cannot be proven the **dependent operation only** fails closed — never the person, never the room, never a right — and **a refusal is never resolved by narrowing the proof**. Prerequisite for the pending payload-equality stub but **does not unblock it**. Authorises no implementation, runtime directory, software/package dependency, payload assembler, serialisation, canonicalisation, hash, comparison mechanism, schema, storage, UI, model access, vendor-hosted access, transmission, disclosure, fixture execution, pending-ledger change, Lane C, W6 or W5-D2 milestone |
| W5-D1 — Transmission and Disclosure Mechanics (DR-W5-05) | [`../decisions/0032-transmission-and-disclosure-mechanics.md`](../decisions/0032-transmission-and-disclosure-mechanics.md) | Accepted by human reviewer, 2026-08-15 — Tier J: the fifth of the seven W5-D1 doctrine records. **Three separate gates** — grant permitted, payload equality proved, transmission authorised — and this record governs the third only: **a lawful payload does not authorise crossing**, and no granted, trusted or session-active flag is transmission authority. **Transmission** is any deliberate movement, handoff, send, export delivery, adapter handoff, model call, vendor call or metadata-bearing transfer out of the controlled processing context; **disclosure** is broader than network send and covers anything that becomes visible, retained or processable outside authorised assembly. Naming a category **does not activate it**. Recipient class must be **nameable, inspectable and matched to the grant**; zone, plaintext status and vendor posture must match at the moment of crossing. **W1-D5 OR-2 is carried unsoftened** — the Wing cannot verify vendor retention behaviour from outside, mitigation is **disclosure honesty, not technical control**, and eight comfort claims are barred by name. **W1-D2 §4's disclosure form is fixed and binding**, restated not redesigned. A **partial crossing is a crossing** and a failed outcome never unmakes an exposure. **Retries** are new attempts unless proven identical on seven attributes; no silent resend, no background retry, none after expiry or revocation. **Telemetry, logging and error reporting are disclosure surfaces and are not exempt because they are operational**; **SDK defaults create no authority**; **metadata-only transfer can be disclosure**. Refusal is structural and content-free, withholding **only the dependent operation** — never the person, never the room, never a right. **Z4 remains dormant by ADR-0031 and is not re-decided**; E10 unopened; no VendorAdapter ADR; model access is **DR-W5-06's**; **E9 export remains a right** and is classified, never gated. Authorises no implementation, network client, library, protocol, SDK integration, retry mechanism, payload assembler, model call, vendor-hosted access, schema, storage, UI, export-warning wording, hosted-mode language, fixture execution, pending-ledger change, Lane C, W6 or W5-D2 milestone |
| W5-D1 — Model Access (DR-W5-06) | [`../decisions/0033-model-access.md`](../decisions/0033-model-access.md) | Accepted by human reviewer, 2026-08-16 — Tier J: the sixth of the seven W5-D1 doctrine records — **the door decision**. **Model access is its own authority question**: not created by a valid grant, a lawful payload, payload equality, transmission authority, model availability, session activity, authentication or evaluation need — passing the ADR-0030/0031/0032 gates is never evidence of it. Model access occurs wherever a grant carries the **local model or vendor-hosted model recipient class**; every access is a **Z3 processing disclosure event through the single AIAdapter seam**, and every model call is a transmission and disclosure surface — including on the user's own device. **The local model class is doctrinally constructable** — the *"a model running on this device"* branch of W1-D2 §4 — lawful only under every prior gate, with no background processing and no ambient model permission; **doctrinally constructable ≠ runtime exists**, and every physical artefact (runtime, SDK, credential, directory, model binary) remains W5-D2's behind its own Tier F crossing. **Vendor-hosted model access is not opened**; the conditions any future opening must meet are stated in ink, with **OR-2 carried unsoftened**. **Z3/Z4 non-conflation is mandatory doctrine**: vendor-hosted model access is a recipient-class question, Z4 stays dormant, E10 unopened, no VendorAdapter ADR, and the Z4 equality limb stays visibly pending. **E12/Z5 remain reserved and unopened.** Model output is **agent-origin by construction — never evidence, never clinical truth, never authority, never ground truth for tests**; a proposal or suggestion only; it reaches stored state only through declared edges and the governed user or review acts they require, and no model-write edge is created. Evaluation-era instrument access is lawful **as a category only** — synthetic, no person, no user grant — under DR-W5-07's architecture. Acceptance satisfies **one conjunct of two pending conditions; both stubs stay pending** — no pending-ledger change, no stub conversion. Refusal is structural, content-free and dependent-operation-only — never the person, never the room, never a right. Authorises no implementation, model contact, provider selection, SDK, credential, runtime directory, transport, prompt, schema, fixture execution, pending-ledger change, Lane C, W6, E10 activation, Z4 discharge, VendorAdapter ADR, or E12/Z5 activation |
| W5-D1 — Behavioural Evaluation Architecture (DR-W5-07) | [`../decisions/0034-behavioural-evaluation-architecture.md`](../decisions/0034-behavioural-evaluation-architecture.md) | Accepted by human reviewer, 2026-08-16 — Tier J: **the seventh and final W5-D1 doctrine record, completing the W5-D1 doctrine set** — the watcher's charter. **Behavioural evaluation is governed observation of system behaviour under synthetic fixture conditions**: it observes behaviour and **does not prove truth, mint authority, certify safety, or prove clinical reliability**. Deterministic before generative; **structural unreachability is proven first or the behavioural result is not evidence of architectural closure**. **A fixture is a well-made trap, never a test result** — W4-D6-BEF carried whole; a fixture file never holds a behavioural result. **Every evaluation artefact is synthetic-only** — including harness prompts and execution transcripts, resolving W2-D4 §8's deferral — with no real user content, **no real grants** (fixture grant-shapes are data, never consents), and **no evaluation channel to a person**. The harness is a **boundary contract, not a build**: fixture data as sole scenario source; paired variants wherever the claim depends on contrast; **all four observed surfaces** (output-only evaluation is structurally insufficient); mechanical checks separated from judgments routed to human review; false-positive **and** false-negative controls; honest unknown preserved; **the harness never grades itself**. **Ground truth is the governed record and the fixture's stated expectation — never a model opinion**; model output is never evidence and never ground truth. **A pass means only that a bounded system path behaved as expected under a named fixture, on the observed surfaces, under stated conditions** — never safe, truthful, clinically reliable, therapeutic, certified, or authorised to persist, promote, transmit, diagnose, infer or act. Non-deterministic results are **bounded observational claims over stated repeated trials with disclosed variance**, never equality claims. A distinct governed **evaluation-record artefact class** is created (not C0; never in fixture files; schema and retention unprescribed) with permanent never-become rules. **`execution_status` is execution-state only, closed at exactly two values** (`behaviourally_unexecuted`, `behaviourally_executed`) with result-bearing values barred by name; outcomes live only in evaluation records; every transition is a W5-D4-era ceremony event; a re-run never overwrites an earlier record. **The T12 seam is dispositioned: the condition is incomplete as written and is to be amended through a later governed pending-ledger landing; no conversion under the literal condition in the interim.** Acceptance satisfies **no pending conjunct** — all 9 stubs remain pending and all 23 fixtures remain `behaviourally_unexecuted`. **No model contact**: any model-contacting harness proof requires W5-D2 runtime authority, Tier F crossings, and a named W5-D3 first-contact gate. Authorises no implementation, harness build, model contact, fixture execution, observation-record materialisation, pending-ledger edit, test conversion, Lane C, W6, E10 activation, Z4 discharge, VendorAdapter ADR, or E12/Z5 activation |
| W5 — Provisional Freshness-Governance Qualification (DR-W5-02 gate) | [`../decisions/0025-provisional-freshness-governance-qualification.md`](../decisions/0025-provisional-freshness-governance-qualification.md) | Accepted by human reviewer, 2026-08-13 — Tier J: the qualification that makes DR-W5-02 writable. W1-D3 §10.1 requested clinical-adjacent input for freshness numbers; that input was not available, and this record states so rather than treating three non-clinical inputs as a substitute. Thresholds may be set only as **provisional governance defaults — not clinically validated**, labelled wherever a threshold value is reproduced or summarised, ending only by supersession or explicit re-affirmation and never by silence. Ten binding safeguards constrain DR-W5-02: event-triggered review primary and calendar secondary; expired—unknown preserving assertion, provenance and age; unknown never collapsing into absence, resolution or reassurance; thresholds governed by data type; assertion posture derived and never stored; fail-closed where safe completion would require assuming an uncertain safety-relevant fact absent or resolved — a **partial disposition of W1-D3 §10.6** blocking only the Wing's assertion, never the user; renewal as meaningful review, never a timestamp reset; alert fatigue carried to the surface era; and no safeguard relaxable without its own record. Not a W5 deliverable and creates no eighth W5-D1 identity; sets no threshold value; claims no clinical validation, certification or endorsement; no implementation, directory, dependency, model contact, payload, transmission, harness or fixture execution is authorised |
| W5 — Safety-Relevant Set Reconciliation and Freshness Coverage (DR-W5-02 gate) | [`../decisions/0027-safety-relevant-set-reconciliation.md`](../decisions/0027-safety-relevant-set-reconciliation.md) | Accepted by human reviewer, 2026-08-14 — Tier J: the reconciliation that lifts the block on DR-W5-02. Four accepted sources hold four different safety-relevant / high-stakes memberships; this record fixes that they are **scoped by governance function and non-transferable**, that membership of one never implies membership of another, and that **no canonical universal set exists or is created**. It **adjudicates no membership** — ADR 0002's six are neither validated nor re-justified. It withdraws an earlier reading that ADR 0002's Kitchen/Gym row delegated the whole room layer to W1-D3 §6.3, leaving a **composition question**: ADR 0002's L2/L3 surfacing mechanism over six members and ADR 0020's always-shown mechanism over four both operate at room surfaces, and no accepted record states how they interact for **pregnancy status and clinician instructions**, covered only by the former. **Both records remain simultaneously valid; no precedence, supersession or winning set is created.** Reported and left open: those two items are in no accepted freshness taxonomy and can never age into review-due, stale or expired; and *freshness data type* has no accepted definition. **DR-W5-02 owns the composition question, freshness coverage and that definition.** Not a W5 deliverable and creates no W5 planning slot; amends no law and no record; no implementation, directory, dependency, model contact, payload, transmission, harness or fixture execution is authorised |
| W5 — Plaintext Residue Applicability at the W5 Runtime Boundary (W5-D2 gate) | [`../decisions/0035-plaintext-residue-applicability-w5-runtime-boundary.md`](../decisions/0035-plaintext-residue-applicability-w5-runtime-boundary.md) | Accepted by human reviewer, 2026-08-17 — Tier J: the applicability/finding record that discharges the mandatory pre-W5-D2 gate (ADR-0024 open question 6). **Finding: ADR-0004's plaintext-residue doctrine substantially reaches the Wing-controlled side of the transient Z3 boundary, and W5-D2 implements under it** — default-deny persistence over every Wing-written Z3 artefact; plaintext-free logs in every build configuration (a debug flag is never a grant); the forbidden artefact classes and ADR-0024's forbidden continuity list prohibiting the same object from two directions; user copies remaining rights, not residue. **Vendor-side retention stays outside ADR-0004 by its own non-goal — congruence, not gap**: disclosure honesty governs past the last controllable point, OR-2 carried unsoftened. **Local model process working memory is dispositioned as an ADR-0004 decision-5 class** — mitigated where available, honestly documented, never denied, never total-control claims — with any concrete model runtime behind its own Tier F crossing under ADR-0033. The task / processing-context / operation vocabularies are mapped and compose without conflict; residue obligations attach at any governing end condition; "session" stays non-controlling. **ADR-0034 is the decision-4-shaped record for evaluation artefacts** — no unrecorded exception. **W5-D2 inherits the residue-test obligations as acceptance conditions**: create/run/terminate-and-kill/sweep for every boundary operation transiently holding governed plaintext, abort and crash paths documented, failure output reporting location and category never content — **a boundary feature without its residue tests does not merge**. Clears ADR-0024 OQ 6 in body; `resolves: []`; W0 OQ 2 untouched. Not a W5 deliverable and creates no planning slot; **W5-D2 milestone-brief drafting may proceed after acceptance; implementation still requires its own brief, Tier F crossings and ceremony.** No implementation, directory, dependency, model contact, credential, telemetry, schema, fixture execution, pending-ledger change, Lane C, W6, E10, Z4, VendorAdapter or E12/Z5 movement is authorised |
| W5 — Revocation Cascade for Derived Artefacts (W5-D2-M05 gate) | [`../decisions/0036-revocation-cascade-derived-artefacts.md`](../decisions/0036-revocation-cascade-derived-artefacts.md) | Accepted by human reviewer, 2026-08-17 — Tier J: **resolves W0 Open Question 2** and discharges W1-D2 §5.5's deferral, unblocking W5-D2-M05. **Invalidation events are exactly revocation and expiry** (completion is not invalidation; succession invalidates nothing); identical forward consequences, distinct marks. **The taxonomy:** source records never targeted; **user-owned material — including anything whose chain holds the user's explicit adoption act — is rights territory the cascade never reaches** (revocation never reaches backwards through a completed user act); Wing-controlled derived artefacts without a user act are the targets; boundary-held material is residue, purged on every termination path; governance records are permanent. **The disposition: invalidate + quarantine, with the user deciding** — keep by explicit adoption or delete by the erasure right; **the Wing never cascade-deletes on its own initiative, never silently retains-as-usable, and never edits history.** Silent re-use is the named refused failure (the mark is load-bearing and travels); re-derivation from an invalidated artefact is refused; interrupted marking completes at next relevant use, never in background; **no vendor-side deletion claim is ever made.** W5-D2-M05+ inherit the proof obligations (refuse invalidated inputs; lineage at creation; marks C0; no deletion of user material). Authorises no implementation, no assembler, no marker store, no surface, no model contact, no fixture execution, no pending-ledger change |
| W5-D2 — Runtime implementation | — | **Milestones complete — W5-D2-M01 through W5-D2-M07 all landed 2026-08-17.** Opened 2026-08-17 by the **W5-D2-M01** foundation landing under the accepted W5-D2 milestone brief and the accepted W5-D2-M01 Tier F opening brief: the `runtime` top-level directory exists as a **capability-empty skeleton** with same-commit structural proofs (the directory fence admits exactly `runtime` and refuses any eighth entry; the skeleton owns no capability and imports nothing; the tree reaches no network facility; engine import caps and the three-pin manifest unchanged) and ADR-0035 residue/log scaffolding with **zero boundary operations registered**. **W5-D2-M02 (grant machinery) landed 2026-08-17** — ADR-0030 at runtime: thirteen-element constructability over one declared edge (missing element, undeclared edge, and blanket scope unconstructable); the seven governed lifecycle states with expired and revoked terminal forever; successor grants with new identity and inspectable, unaltering lineage; re-authentication bound to one exact proposal; consent carried with **no authority field anywhere, by design**; governed durations as doctrine-derived data (the 180-day standing ceiling as a pure injected predicate — no clock is read); and **a structurally proven absence of any inactivity-timeout code path** — nineteen same-commit structural proofs, zero scan findings, zero new dependencies. **W5-D2-M03 (freshness runtime wiring) landed 2026-08-17** — ADR-0029 at the boundary: the seven governed policy keys with complete `R`/`G`/`H` triples (**S derived as R + G only — no API accepts an S**); **label boundaries verified through the sealed engine's own public staleness function consuming the derived absolute triple — engine law consumed, never duplicated**; bounded unknown preserved with **absence never inferred**; the §6.4 floor structural (**stale and expired never assertable as stable truth**); **acknowledgement writes no freshness state** and renewal requires an explicit review act; the five re-review triggers consumed as pure governance signals that rewrite nothing; the **safeguard-14 predicate** expressed with its consulted set named and **no canonical set or precedence created**; every value carrying the exact label *provisional governance default — not clinically validated*; **no notification, reminder, or outward-push name anywhere in the module, structurally proven** — twenty-three same-commit structural proofs, zero scan findings, zero new dependencies (the module imports `dataclasses` alone). **W5-D2-M04 (processing context) landed 2026-08-17** — ADR-0024’s room made real: contexts constructable only from an active grant, with **the room a function of the edge — no room parameter exists**; **no free-content interface** (content enters only inseparably grant-bound and in-scope); no composition point, no reuse, **no fetch and no file or network I/O by AST proof**; lifetime bounded by the grant with end clearing everything and **absence demonstrable, not presumed**; revocation expressible (severability honesty deferred to the first real crossing at W5-D2-M06); content-free C0 ledger events produced at create and end under ADR-0015’s extension clause (appending is composition-root work); the **four observation surfaces present by construction**, including paired-variant support; **the first governed-plaintext boundary operation registered by record and carrying the full ADR-0035 residue tax — normal-termination and kill-termination sweeps both proven clean** — twenty-four same-commit structural proofs, zero scan findings, zero new dependencies. **W5-D2-M05 (payload assembly and equality) landed 2026-08-17** — ADR-0031 made real, under ADR-0036’s cascade obligations: assembly only inside a live processing context from its grant-bound holdings — **the granted scope is the whole payload, proven at the door** (subset, superset, duplicate and empty all refused); **invalidated inputs refused by content-free reference and grant lineage recorded at creation** (ADR-0036 decision 20); no deletion or re-derivation API exists, by proof; the **three governed comparisons** implemented over a deterministic canonical serialisation with **byte-level identity at the last controllable point**; similarity never satisfies equality, and **unproven refuses the dependent operation**; content-free fixed-length fingerprints under the five recorded conditions; **no standing payload** (no module container; each assembly a distinct payload); **the payload-assembly boundary operation registered by record with the full ADR-0035 tax paid — normal and kill terminations swept clean**; **the Z4 limb of the payload-equality obligation remains visibly pending and no crossing exists** — the deterministic pending stub’s conditions are now materially true at Z3 while conversion remains its own governed ceremony, deliberately not performed — twenty-five same-commit structural proofs, zero scan findings, zero new dependencies. **W5-D2-M06 (transmission and disclosure mechanics) landed 2026-08-17** — ADR-0032 made real: **the third gate enforced separately** — transmission authority proven per attempt from the live context, the assembled payload and a passing equality record, **never ambient and consumed by exactly one crossing**; recipient nameable in the grant’s own display terms; posture (recipient class, zones, plaintext, vendor, purpose, operation) matched at the moment of crossing; **invalidated payloads refused** (ADR-0036); **equality re-proved at the last controllable point before any hand-off**, with refusal-before-boundary producing **no disclosure at all**; **a partial crossing is a crossing** — recipient failure after hand-off records disclosure and abort honestly, never unmade; content-free C0 disclosure records carrying ADR-0001’s elements by reference with an injected as-of marker (no clock); the **seven-attribute retry identity** with any change a new attempt and retry after revocation or expiry refused; **crossings terminate at in-process structural doubles — provably network-free, no SDK, no vendor path, no comfort claim in source by proof**; the **transmission-crossing boundary operation registered by record with the full ADR-0035 tax paid — normal and kill terminations swept clean**; Z4 dormant, E10 unopened, the Z4 equality limb visibly pending — twenty same-commit structural proofs, zero scan findings, zero new dependencies. **W5-D2-M07 (isolation realisation, the Meditation runtime, and the composition root) landed 2026-08-17 — the accepted seven-milestone sequence is COMPLETE.** The stateless composition root (functions only, no state, nothing retained between operations — never a standing handle) is **the only place the runtime pieces meet and the only ledger-append path** (modules produce content-free events; only the root appends, via a caller-supplied callable); **isolation realised end-to-end**: one operation = one grant, one context, one payload, at most one crossing, ended always — no function anywhere accepts two contexts, no pool/cache/registry/merge name exists across the tree, one room’s content refuses another room’s context, and sequential operations share nothing; **Meditation stricter, not smoothed**: per W1-D1 the M2 edge carries **CM and only CM, and CM travels on no other edge** — enforced structurally with its own refusals — with M2’s only egress its own governed crossing and the no-output-elsewhere rule held by the global absence of any room-write path; the **composed-operation boundary registered by record (the fourth) with the full ADR-0035 tax paid — normal and kill terminations swept clean over the whole composed flow**; twenty same-commit structural proofs, zero scan findings, zero new dependencies. **W5-D3, W5-D4, W5-D5, W5-D6 and W6 remain unopened; no model was contacted; the nine pending stubs and twenty-three unexecuted fixtures stand untouched.** Milestone-based per the accepted brief (**W5-D2-M01 … W5-D2-M07**, bare M-names barred): every capability lands with its deterministic proof in the same commit; four-surface observability designed in, not retrofitted; each milestone gated on the W5-D1 records it depends on; **the W0 OQ 2 revocation-cascade record is required before W5-D2-M05**; no model contact is grantable by any W5-D2 milestone |
| W5-D3 — Behavioural evaluation harness | — | **Complete — landed 2026-08-17.** The instrument exists in the tests tree (`tests/evaluation_harness.py`) with 21 same-commit structural proofs: fixture-as-data loading with synthetic-discipline validation, paired-variant delta detection across the four observed surfaces, output-only observation refused, unknown honestly distinct from absent, false-positive and false-negative calibration guards, every delta routed to review, and a structural corpus-execution interlock proven shut. The twenty-three-fixture corpus was not executed; W5-D4 is the run |
| W5-D4 — Fixture execution and evaluation records | — | **Complete — landed 2026-08-17.** ADR-0037 authorised exactly one governed execution event, `W5-D4-RUN-01`: the twenty-three-fixture corpus executed under the deterministic in-process instrument (no model contacted; the generative era stays behind its own gate), calibration proven before any corpus work, evaluation records landed append-only under `governance/evaluation/` with the four-surface structure intact — silent probes as paired-variant composed operations with every delta routed to human review, overt probes honestly `unknown-not-absent`. Results never entered fixture files; all twenty-three fixtures transitioned `behaviourally_unexecuted` → `behaviourally_executed` through ceremony in exactly one field each, recorded in the W4-D6-BEF map with its hash refreshed in the same commit. Executed is not passed; observed is not proven; no result is a health or certification claim |
| W5-D5 — Lane C Tier 2 evidence maintenance | — | **Complete — landed 2026-08-17.** The one governed maintenance act (`W5-D5-LCM`): all eleven Tier 2 assurance rows reviewed individually against landed artefacts. Four moved to `evidenced` on their own cited artefacts, never on phase completion (AR-CORE-07, AR-HIPAA-04, AR-NIST-04, AR-NIST-06, tiers unchanged); seven stay deferred with the reassessment recorded in place, including the assigned `AR-NIST-05` currency reassessment — its behavioural component honestly deferred, because no evaluation observation is assurance evidence. No Tier 3 conversion, no applicability resolution, vocabulary closed and unchanged, register untouched, assurance-record hash refreshed same commit |
| W5-D6 — W5 closure record | [`W5-closure-record.md`](W5-closure-record.md) | **Complete — the phase seal, published 2026-08-17.** Whole-phase closure against the runway on the obligations basis; pending ledger reviewed (nine stubs visible, none converted); five incidents closed; honest findings carried in ink; deferred inventory named; the W6 gate named without opening W6 |

## W6 — Surface Era: Governed Presentation and Review Surfaces

**Scope:** The surface era: the phase where governed truth is finally shown to a person. The governed string catalogue, the thirty-three review-of-record strings, language-law grading (D5-T24), governance-label rendering, review surfaces, and the honest rendering of every state the records already hold — pending, deferred, unknown, stale, review-routed, executed, evidenced, not-evidenced, applicability-unresolved, and non-authority notices. North star: **governed truth must survive display without becoming authority** — visual language is language, and no word or pixel may claim more than the ink behind it.

**Runway:** [`W6-runway-governed-presentation-review-surfaces.md`](W6-runway-governed-presentation-review-surfaces.md)

**Status:** **The W6 entry gate is three documents complete of three:** the W5 closure record is gate document one, the runway — accepted 2026-08-17 — is gate document two, and the accepted W6-D1 surface-era doctrine brief ([`W6-D1-surface-era-doctrine-brief.md`](W6-D1-surface-era-doctrine-brief.md)) is gate document three, effective on publication and remote verification. **W6-D1 is doctrine-complete: all six decision records — ADR-0038 (display vocabulary and label law), ADR-0039 (the label-authority firewall), ADR-0040 (honest-state rendering law), ADR-0041 (human-routing presentation law), ADR-0042 (catalogue identity and catalogue-ID namespace law), ADR-0043 (review-surface boundary contract) — accepted and published through full ceremony, each before what it gates, with all fourteen runway doctrine questions answered and all ten risk bindings discharged. Doctrine complete is not capability begun: no catalogue, no catalogue ID, no graded string, no rendered label, and no surface exists. **W6-D2 — the Governed String Catalogue — is complete and closed** (all five landings of its accepted brief published — A shape/home, B schema, the merged C+D materialisation under the architect's ruling, and the E completion record; sealed by [`W6-D2-E-catalogue-completion-record.md`](W6-D2-E-catalogue-completion-record.md), 2026-08-17). The catalogue stands at [`../../governance/string-catalogue.json`](../../governance/string-catalogue.json): thirty-three governed entries, `CAT-0001`–`CAT-0033`, validated whole by the woken catalogue-ID validator — **ungraded, unrendered, and honest about both**: membership is necessary where later law requires it, never sufficient for display. **W6-D3 — Language-Law Grading — is complete and closed** (three landings under the standing merge discipline: [`W6-D3-ABC-grading-opening-scope-rubric.md`](W6-D3-ABC-grading-opening-scope-rubric.md) — opening, the thirty-three-entry scope lock, and the eight-criterion D5-T24 rubric with its four-value disposition vocabulary · [`W6-D3-DE-grading-execution-record.md`](W6-D3-DE-grading-execution-record.md) — every entry graded one by one, **lawful-as-worded 33 / 0 / 0 / 0**, catalogue hash proven unchanged, historical register items disposed with sealed records untouched · sealed by [`W6-D3-F-grading-results-closure-record.md`](W6-D3-F-grading-results-closure-record.md), 2026-08-17, with nothing unresolved, deferred, or correction-needed). The ceiling holds over every grade ever issued: **a grade is not approval, not display permission, not renderability** — the words are lawful and still unshown, exactly in that order. **W6-D4 is open for brief-governed work** under [`W6-D4-ABC-surface-opening-medium-contract.md`](W6-D4-ABC-surface-opening-medium-contract.md) (A/B/C merged, justification in place): ADR-0043 consumed whole, seven risks bound at opening, the state-family rendering directive carried (family reasoning explanatory, per-item traces mandatory), the medium decided — **a static generated review surface with no JavaScript, no forms, no interactive controls**, produced by a stdlib-only generator from **exactly six declared inputs** with undeclared input failing structurally, the three generated artefacts deliberately excluded from the registry with integrity by deterministic byte-identical regeneration. The materialisation landed under [`W6-D4-DEF-surface-materialisation-record.md`](W6-D4-DEF-surface-materialisation-record.md) (D/E/F merged — a surface and its proofs are same-commit-coupled): **the static review surface exists** at [`review-surface.html`](../surface/review-surface.html) — five sections (register · evidence map · evaluation posture · pending ledger · carried questions), no scripts, no controls, no links, no glyphs, neutral greys encoding nothing, every accompaniment inline, the four aphorisms rendered — with the fifteen ADR-0043 declarations as data, **the mandatory 188-row per-item trace** (33+65+23+26+25+9+7, family reasoning explanatory, every row individually proven), and twenty-six structural proofs green in the same commit including byte-identical regeneration and two-way trace bijection. Viewing it reviews nothing, decides nothing, approves nothing — structurally, because nothing on it can be pressed. **W6-D4 — Review Surfaces — is complete and closed**, sealed by [`W6-D4-G-review-surfaces-closure-record.md`](W6-D4-G-review-surfaces-closure-record.md) (2026-08-17): exactly one surface exists and it is exactly one kind, 188 rows rendered and individually proven, 26 structural proofs green, family reasoning explanatory and replacing nothing, nothing unresolved, deferred, or correction-needed — and no presentation assurance performed, because auditing this surface is W6-D5's whole purpose. **W6-D5 is open for brief-governed assurance work** under [`W6-D5-A-presentation-assurance-opening-brief.md`](W6-D5-A-presentation-assurance-opening-brief.md): asking whether the showing stayed honest, over a named clean baseline (surface + 188-row trace + 26 proofs + the published Tier M erratum), in four landings — opening · audit execution (rendered-state, implied-claim, trace, stub reassessment as one read-only session) · findings · closure, with findings kept separate from closure so anything correction-needed is visible before completeness is declared. The audit reads and alters nothing; assurance is **not** certification, safety, production readiness, clinical validity, legal conformance, correctness, or approval. The audit executed under [`W6-D5-BCDE-audit-execution-record.md`](W6-D5-BCDE-audit-execution-record.md) (B/C/D/E merged — one read-only session over one artefact set): **531 checks — rendered-state 188/188 match · implied-claim 14 satisfied + 1 observation, 0 defects · trace 321/324 match with the 188-row trace bijective both ways and family reasoning proven not to have replaced individual accountability · 3 W6-owned stubs identified, 0 converted, ledger byte-identical**. **Two findings stand**, neither a current misstatement, both accountability-path risks (the per-entry grading disposition rendering from a generator literal; the four applicability records rendering as an accurate family statement without individual rows) — recorded untouched for their own correction landing. The findings landed under [`W6-D5-F-assurance-findings-record.md`](W6-D5-F-assurance-findings-record.md): **the showing stayed honest** on the checks performed — bounded exactly as not certification, safety, clinical validity, legal conformance, correctness, production readiness, or approval — with **two correction-needed findings** (accountability repairs, not defect repairs; neither a misstatement), **three correction paths named and none taken**, the three W6-owned stubs recorded **eligible for future consideration and none converted** (ledger byte-identical), and the packet-reported versus independently-verifiable boundary stated precisely. **W6-D5 — Presentation Assurance — is complete and closed**, sealed by [`W6-D5-G-presentation-assurance-closure-record.md`](W6-D5-G-presentation-assurance-closure-record.md) (2026-08-17): closing because the deliverable's obligations are discharged, **not** because everything found is resolved — the two correction-needed findings remain **open by design**, visible, sized, and unexecuted, alongside the optional audit-as-proof-module path and the three ceremony-bound stubs. Every audited artefact, source, and ledger byte is identical to its pre-D5 state. **W6-D6 is open for brief-governed phase-closure work** under [`W6-D6-A-closure-authorisation-brief.md`](W6-D6-A-closure-authorisation-brief.md): asking whether W6 can close without hiding anything, over a baseline confirmed lawful for closure preparation (D1–D5 each published and independently verifiable, every W6 registry entry hash-current), in three landings — opening · closure assessment (inventory, runway obligations, pending ledger, incidents, deferred inventory) · the final closure record, separate. **W6 may close complete with open carried items if and only if each is named, classified, and never represented as resolved.** Closure is **not** certification, safety, clinical validity, legal conformance, correctness, approval, or production readiness. The closure assessment landed under [`W6-D6-BCDEF-closure-assessment-record.md`](W6-D6-BCDEF-closure-assessment-record.md) (B/C/D/E/F merged — one evidence-gathering act over one published corpus, with no closure declared among them): the whole-phase inventory (24 commits, 24 registry entries, six deliverables, the Tier M erratum in the chain), **all twelve runway obligations assessed — ten satisfied, two satisfied-with-carried-item, none unresolved**, non-goals mechanically verified across the whole phase, the pending ledger reviewed byte-untouched with nine stubs and three W6-owned ones eligible-not-converted, a nine-entry incident log naming the post-publication catch, and the deferred inventory carrying F-1, F-2, optional P-3, the ceremony-bound stubs, the generative era, the T12 obligation, and seven open questions forward with next gates named and unopened. **W6 — Surface Era: Governed Presentation and Review Surfaces — is complete and closed** (W6-D1 through W6-D5 sealed, W6-D6 phase closure published 2026-08-17; see [`W6-closure-record.md`](W6-closure-record.md); closure effective on this record's publication). **Complete, not perfect**: two correction-needed findings stand open with their smallest paths named and untaken, three W6-owned stubs wait on unauthorised ceremonies, seven carried questions are still carried, eleven Lane C rows remain external and four applicability records unresolved, and the generative evaluation era is assigned to no phase — every one named, classified, and represented as resolved nowhere. No model was contacted at any point in the phase; no dependency was added; no new scan suppression was created. **W7 has not started and must open through its own runway/gate** (three documents, three acceptances — the closure record names the gate without opening it) — every later W6 deliverable requires its own accepted brief through its own ceremony.

**Deliverables:**

| Deliverable | Documents | Status |
|---|---|---|
| W6-D1 — Surface-Era Doctrine Brief (opening record) | [`W6-D1-surface-era-doctrine-brief.md`](W6-D1-surface-era-doctrine-brief.md) | Accepted by human reviewer, 2026-08-17 — gate document three; opens W6-D1 only; six decision records planned |
| W6-D1 — Display-Vocabulary and Label Law (DR-W6-01) | [`../decisions/0038-display-vocabulary-and-label-law.md`](../decisions/0038-display-vocabulary-and-label-law.md) | Accepted by human reviewer, 2026-08-17 — Tier J: the display vocabulary closed at seven exhaustive classes; stored states verbatim-or-registered-derived with renaming barred as authority-by-thesaurus; derived labels derived-never-stored Wing-wide; the barred display list closed and growable-only (including the `certif-` and `complian-` stems); six prohibited visual classes stated in checkable terms with rendering artefacts obliged to declare glyph/colour/coverage/affordance inventories as data; every honest state family assigned its class with omission barred as overclaim; renders nothing, creates no catalogue, allocates no identifier |
| W6-D1 — The Label-Authority Firewall (DR-W6-04) | [`../decisions/0039-label-authority-firewall.md`](../decisions/0039-label-authority-firewall.md) | Accepted by human reviewer, 2026-08-17 — Tier J: the keystone, consuming ADR-0038 whole and unweakened under one normative formula — display may preserve source authority; display may not create authority. Eleven mechanism binds (closed vocabulary, class-as-data, the authority-non-increasing derivation rule, wording/stem and visual declaration checks, unweakened accompaniments, honest-state coverage, no write-back, declared non-quality orderings only, no ceremony-bypassing affordance, and no machine verdict structurally — no current W6 source holds a human-authorised verdict); twelve normative firewall rules ending with declarability as the admission condition; deterministic conformance proofs assigned to the artefacts that first bear strings and surfaces; promotes nothing — executed stays not passed, unknowns stay unknown |
| W6-D1 — Honest-State Rendering Law (DR-W6-02) | [`../decisions/0040-honest-state-rendering-law.md`](../decisions/0040-honest-state-rendering-law.md) | Accepted by human reviewer, 2026-08-17 — Tier J: one worked rendering law per honest state inside ADR-0038 Parts A/B/E/F and ADR-0039 whole. Unknown stands as a truth with its basis and display resolves nothing; review-routing names a destination, never a judgement; deferred shows its named dependency, never failure or completion; `behaviourally_executed` means one governed run happened with the executed-is-not-passed accompaniment mandatory and success-grouping barred by name; `evidenced` locates evidence and asserts no requirement satisfied; `not_evidenced` displays honest absence with the same dignity as its siblings; applicability-unresolved stands at full size and no rendering may imply resolution. Six mandatory accompaniments fixed as one table; barred textual transformations named as authority-by-thesaurus; V1–V6 consumed per state with visuals obeying every textual rule; renders nothing, changes no governed state |
| W6-D1 — Human-Routing Presentation Law (DR-W6-03) | [`../decisions/0041-human-routing-presentation-law.md`](../decisions/0041-human-routing-presentation-law.md) | Accepted by human reviewer, 2026-08-17 — Tier J: presentation law for human-routed material beyond ADR-0040's routing boundary. Routed deltas display whole — both variants exactly as recorded, never collapsed into a machine-selected conclusion, no recommendation, score, winner or safer option, and no selected variant exists unless a human record creates one; pending stubs display with identity, owner, dependency and condition, as pending not converted, with no conversion affordance and no bulk path by words, visuals, grouping or affordance; carried open questions display as carried — still alive, never retired — with the three waits kept distinct; the human-attribution chain is law (needed is not reviewed, routed is not reviewed, reviewed is not approved absent a recorded act and a future authorising record); urgency theatre, progress bars over waits, red-as-failure and green-as-cleared barred; five accompaniments complete the ADR-0040 table; renders nothing, performs no review, invents no human act |
| W6-D1 — Catalogue Identity and Catalogue-ID Namespace Law (DR-W6-05) | [`../decisions/0042-catalogue-identity-and-namespace-law.md`](../decisions/0042-catalogue-identity-and-namespace-law.md) | Accepted by human reviewer, 2026-08-17 — Tier J: the record that gates W6-D2 without opening it. The catalogue defined as a governed register of approved display strings and required metadata — never a surface, renderer, glossary, softening layer, or source of authority, because registration lends a string nothing; membership closed to governed strings in the seven ADR-0038 classes with accompaniments as first-class members; exclusions bar sources, evidence, results, layout, glyphs, colours, affordances and model output. A catalogue ID identifies one string record and that is the whole of its meaning — stable, immutable except through visible supersession, encoding and implying nothing, never a verdict, never authorising display by itself; the `CAT-` namespace closed, flat, and meaning-free with identity structurally separated from fields, one illustrative identifier marked non-final and nothing allocated. The thirty-three review-of-record strings held as seed corpus only — per-string admission or rejection through W6-D2's own ceremony, rejection lawful. Thirteen deterministic obligations specified for the dormant catalogue-ID validator class, untouched today and woken only by W6-D2's record; W6-D2's gate stated: declarability, per-entry validation, per-seed decisions, no authority-by-registration |
| W6-D2 — Governed String Catalogue (opening brief) | [`W6-D2-governed-string-catalogue-brief.md`](W6-D2-governed-string-catalogue-brief.md) | Accepted by human reviewer, 2026-08-17 — the W6-D2 opening brief, supplying the second condition of ADR-0042's Part G gate and opening the deliverable for brief-governed work only. Five conservative landings, each behind its own authorisation: artefact shape and home; schema carrying the eleven required fields as data; validator wake implementing the thirteen Part F obligations with same-commit proofs; per-string admission or rejection of the thirty-three seed candidates (11 Wellness · 11 Kitchen · 11 Gym · 0 Meditation, the zero source-derived) with rejection lawful and no bulk or silent admission; and the completion record. The brief itself creates no catalogue, allocates no ID, admits and grades no string, wakes no validator, renders nothing — membership stays necessary and never sufficient for display |
| W6-D2-A — Catalogue Artefact Shape and Home | [`../decisions/0044-catalogue-artefact-shape-and-home.md`](../decisions/0044-catalogue-artefact-shape-and-home.md) | Accepted by human reviewer, 2026-08-17 — Tier J: the first W6-D2 landing, deciding where and in what shape the catalogue will one day live while creating none of it. Home: `governance/string-catalogue.json`, a single artefact beside the registry it structurally mirrors, following the single-governed-JSON-register precedent. Format: JSON under the standing data conventions, chosen for deterministic parse, record-granular diffs, and direct validator consumption — key order, layout, and every schema field remaining W6-D2-B's. Registry posture: registered with content hash under its own entry, created by the same landing that first creates the file, registration providing integrity tracking only — a catalogue file existing, registered, and hashing correctly cannot authorise the display of one word. Declarability: the artefact must satisfy ADR-0039 rule 12 about itself, capability named and fields not created. Markdown-under-docs rejected (copy-deck drift), the split model deferred, the Lane C wrapper pattern rejected for a pure register. No file, schema, entry, or identifier exists after this landing |
| W6-D2-B — Catalogue Schema and Metadata Shape | [`../decisions/0045-catalogue-schema-and-metadata-shape.md`](../decisions/0045-catalogue-schema-and-metadata-shape.md) | Accepted by human reviewer, 2026-08-17 — Tier J: the schema decided in doctrine inside ADR-0044's fixed inputs, creating nothing — the record is the schema's single authoritative statement, a standalone schema file rejected as a second source of truth. Top level: four keys in canonical order (catalogue_version, status born `governed_register_active`, byte-stable governance_note, entries born empty); entry shape: a flat array sorted by id, every entry carrying all eleven fields always — no omitted keys, no hidden defaults, exactly two defined nulls; canonical key order ending with the ceiling; a closed seven-value class set tied one-to-one to ADR-0038; citation-based source authority (no source, no entry), derivation references admitting only registered authority-non-increasing rules, accompaniment emptiness lawful only where no law applies, the prohibition register as the single gate for barred wording, a two-value checked citation requirement, visible-only supersession and retirement with no id reuse, a closed seven-token proof-obligation set where obligations are debts not receipts, and one fixed verbatim ceiling sentence byte-identical in every entry. Grouping by class, room, and normalisation all rejected; complete validation spec handed to W6-D2-C and the admission schema to W6-D2-D — nothing admitted, rejection lawful |
| W6-D2-C+D — Catalogue Validator Wake and Seed Corpus Admission | [`W6-D2-CD-catalogue-admission-record.md`](W6-D2-CD-catalogue-admission-record.md) · catalogue at [`../../governance/string-catalogue.json`](../../governance/string-catalogue.json) | Accepted by human reviewer, 2026-08-17 — the merged C+D landing under the architect's ruling (merge safer than split: the validator wake is same-commit-coupled to the artefact it validates; no intermediate published half-state). The catalogue file born at its decided home and registered with hash in the same commit; all thirty-three seed candidates dispositioned one by one — 33 admitted, 0 rejected, 0 deferred — as `derived_presentation_label` entries carried verbatim from the three sealed contracts' section-5 coverage ledgers, `CAT-0001` through `CAT-0033` allocated in flat meaning-free order; unknown-family and contradicted strings carry their ADR-0040 accompaniment linkages; one named mechanical exception (the W1-D3 temporal collocations for "passed", validator-bounded exactly); the dormant catalogue-ID validator awake at `tests/test_w6_catalogue_validator.py`, green over the populated register in the same commit, the W4 dormancy check's docstring amended by record with the sealed contracts byte-untouched. No string graded, no label rendered, no surface exists; membership stays necessary and never sufficient for display. **W6-D2 remains open pending its completion record — W6-D2-E is next and separate** |
| W6-D2-E — Catalogue Completion Record | [`W6-D2-E-catalogue-completion-record.md`](W6-D2-E-catalogue-completion-record.md) | Accepted by human reviewer, 2026-08-17 — the fifth and final landing: **W6-D2 — the Governed String Catalogue — is complete and closed**, every opening-brief acceptance condition assessed satisfied on the published branch (eight for eight), the authority chain verified whole, the artefact recorded as it stands (33 entries, `CAT-0001`–`CAT-0033`, ceiling byte-identical throughout), the admission pass recorded (33/33 admitted, rejection lawful and unneeded), the validator recorded awake at 22 green proofs with its ceiling stated — green means schema-and-law conformance only. Closure changes nothing and opens nothing: membership stays necessary and never sufficient for display; grading remains W6-D3's behind its own brief. **W6-D3 is next and unopened** |
| W6-D1 — Review-Surface Boundary Contract (DR-W6-06) | [`../decisions/0043-review-surface-boundary-contract.md`](../decisions/0043-review-surface-boundary-contract.md) | Accepted by human reviewer, 2026-08-17 — Tier J: the sixth and final W6-D1 record, gating W6-D4 without opening it and completing the doctrine set. A review surface is a bounded reader over governed sources and catalogue entries — a presentation boundary, never an authority boundary: nothing becomes true by being shown. Sixteen non-identities held structurally; read law bars discovery, merging, and model output in this era; write law: a surface writes nothing and persists nothing; state law: transient render-time state only if later authorised, with the four named drifts barred; channel law: no hidden channels, no fifth channel — a needed channel is new architecture and must stop; declarability law: fifteen declarations as data under one admission rule — no declaration, no pass; the no-bulk-approve obligation a contract term; the human-review boundary carried as four aphorisms — viewing is not reviewing, clicking is not deciding, selecting is not approving, closing a panel is not retiring; twenty proof obligations specified for future W6-D4 surfaces. Creates no surface, cache, store, workflow, or path | No catalogue exists, no catalogue ID is allocated, no label renders, no surface exists, no runtime capability is added, no model is contacted, the nine pending stubs remain visible and unconverted, the twenty-six `unknown-not-absent` records are displayed only and never resolved by anything W6 builds, Tier 3 remains external, applicability remains unresolved, and E10/vendor, Z4, E12/Z5 and the hosted class remain dormant. **W7 and the generative evaluation era remain unopened** — the generative era is assigned to no W6 deliverable and opens, if ever, only by its own future governed record behind ADR-0034's first-contact gate.

## W7 — First-Contact Governance and Synthetic Model Evaluation

**Scope:** The first phase in which generated language may be governed. Public W7 is the governed, demonstrable half of the model era: first-contact doctrine, the synthetic-only public law, the generated-output evaluation record shape, the model boundary and dependency crossing, a synthetic harness bound to the twenty-six recorded unknowns and the synthetic traps, synthetic execution records, human review of textual deltas, and closure. North star: **generated text may enter the public Wing only as synthetic, governed evaluation evidence** — observed, preserved, compared, routed, and reviewed, never authority, advice, truth, safety evidence, approval, or a decision about any person. **Any real-person adoption is a separate governed authority outside this repository.**

**Runway:** [`W7-runway-first-contact-governance-synthetic-evaluation.md`](W7-runway-first-contact-governance-synthetic-evaluation.md)

**Deliverables:**

| Deliverable | Documents | Status |
|---|---|---|
| W7-D1 — First-Contact Doctrine and Synthetic-Only Boundary Brief (opening record) | [`W7-D1-first-contact-doctrine-brief.md`](W7-D1-first-contact-doctrine-brief.md) | Accepted by human reviewer, 2026-08-18 — gate document three; opens W7-D1 only; two decision records planned (DR-W7-01 synthetic-only public law and the external adoption boundary, then DR-W7-02 first-contact doctrine and the named-not-performed gate, in that landing order because boundary-before-contact binds). Defines model contact by **provenance** rather than vendor, medium or transport, separating model contact from generated output from authored synthetic specimen — and binds the governed-handling doctrine on all of them, because the pattern is proven by the handling and not by the provenance of the text handled. Seven conjunctive preconditions for any model contact, tabled with discharge locations: one satisfied, one closed, five outstanding. The one-way property with no retroactive authorisation, no-recirculation (capture is terminal), and the finding-is-an-event inversion replacing silent allowlisting. The architect's ruling carried: **the fixed external-boundary invariant is the clause *a separate governed authority outside this repository***, sentence-position grammar permitted and nothing else, with no erratum required for the runway's §14 variant. Contacts nothing, generates nothing, authors no specimen, defines no record shape, adds no dependency, and settles none of the four model-boundary options |
| W7-D1 — Synthetic-Only Public Law and the External Adoption Boundary (DR-W7-01) | [`../decisions/0046-synthetic-only-public-law-and-adoption-boundary.md`](../decisions/0046-synthetic-only-public-law-and-adoption-boundary.md) | Accepted by human reviewer, 2026-08-18 — Tier J: the first of the two W7-D1 records and the outer fence of the model era, landing before DR-W7-02 because boundary-before-contact binds. Synthetic-only **by construction, never by scrubbing**, with the constructional test that an artefact which *could* carry real-person content is unlawful whether or not it currently does — emptiness is not conformance. **Capture is publication**, ADR-0007 decision 2 carried into the new class: every capture is checked at capture time and no upstream cleanliness transfers. **Three lawful input origins, closed** — authored synthetic, existing fixtures, governed public records — with a fourth **unlawful rather than dispositionable**, and **no-recirculation: capture is terminal**, so generated text may never re-enter as material. The exclusion list is **growable only, never narrowable**, and there is **no exemption for machine production**: a scan finding inside generated prose is a governed event with a disposition path, never a silent allowlisting. The external boundary is fixed at the clause **a separate governed authority outside this repository**, sentence-position grammar permitted and nothing else, under a non-naming law, with **stating the clause creating nothing**. Provenance declarability is the admission condition (ADR-0039 rule 12, completed for provenance), field shape assigned to W7-D2. The non-authority ceiling is carried byte-identically **inside** every record that holds generated or specimen text, with **specimen parity** so the no-contact path cannot buy a weaker ceiling. Six proof obligations: **P1 mechanical and green in this commit** at [`test_w7_boundary_invariant.py`](../../tests/test_w7_boundary_invariant.py), bounded to the clause alone, with the instrument's single disclosed self-exclusion asserted to be exactly its own path; **P2 review-only in full and permanently so** — a mechanical check for a name that must never be written cannot exist, because writing the search term would breach the non-naming law, and rule-explaining prose cannot be told from authority-elaborating prose without semantic judgement; **P3–P6 specified debts** until the artefact class exists. Creates no artefact of that class, defines no record shape, authors no text, adds no dependency, and contacts nothing |
| W7-D1 — First-Contact Doctrine and the Named-Not-Performed Gate (DR-W7-02) | [`../decisions/0047-first-contact-doctrine-and-named-not-performed-gate.md`](../decisions/0047-first-contact-doctrine-and-named-not-performed-gate.md) | Accepted by human reviewer, 2026-08-19 — Tier J: the second and final W7-D1 record, **completing the deliverable's doctrine set**. The crossing, named and not made: it lands after ADR-0046, consumes that fence whole, and performs no contact, authorises none, schedules none, and makes none inevitable. Three closed terms defined by **provenance, never by vendor, medium, or transport**, because a definition keyed to the plumbing is satisfied by re-routing — **model contact** is the act of transmitting material to a model and receiving text back, complete at transmission-and-return whether or not anything is kept; **generated output** is text produced by a model; an **authored synthetic specimen** is human-written, repository-internal, from nothing, deliberately carrying the properties of generated prose. **A specimen is not model contact**, and **provenance does not launder in either direction** — a specimen indistinguishable from generated output is still not generated output, and a generated sentence rewritten by a human is still generated output. **The governed handling binds on both identically**, extending specimen parity from the ceiling to the whole regime, so a weaker regime for specimens is unavailable at any later gate. **Seven conjunctive, closed preconditions** for any contact, tabled with source and discharge location — one satisfied, one law in force, one closed, **four outstanding** — and **landing this record discharges none of the four**. First contact is **one-way**: the gate is checked before and never after, **no retroactive authorisation path exists**, and a generated byte before its gate is a **stop-and-report event**. No-recirculation and finding-is-an-event are consumed from ADR-0046, foreclosing chained exchanges and self-feeding loops. Carries the **fifteen-link anti-collapse chain** whose last link binds both ways: a no-contact outcome proves nothing about a model **and does not mean W7 proved nothing**. Proofs: **Q1 chain integrity and Q2 precondition-set integrity mechanical and green in this commit** at [`test_w7_first_contact_doctrine.py`](../../tests/test_w7_first_contact_doctrine.py), the module holding the chain literally and excluding only itself, disclosed in the record; **Q3 review-only in full**, joining P2 as a standing human-review duty; **Q4 a debt** until the artefact class exists. Q1 and Q2 green prove the chain is intact and the preconditions enumerated, and **prove nothing about whether contact occurred**. Narrows none of W7-D3's four options; contacts nothing |
| W7-D2 — Record-Shape Opening Brief (W7-D2-A) | [`W7-D2-A-record-shape-opening-brief.md`](W7-D2-A-record-shape-opening-brief.md) | Accepted by human reviewer, 2026-08-19 — Tier J: opens **W7-D2 for record-shape work only**, and nothing else. Six standing statements bind at its own landing: W7-D2 creates **a shape, not content**, and authorises **no model contact, no specimen, no generated-output import, and no harness**; it **narrows none of W7-D3's four options**. A container is not an invitation — a defined shape, a landed schema, a green validator and an empty directory authorise nothing. A complete W7-D2 discharges exactly **one** of ADR-0047's seven preconditions (the record shape exists) and **nothing else**: the Tier F crossing, the named first-contact gate — **named nowhere** — and the harness binding all remain outstanding, so **three preconditions still stand and no model may be contacted**. Consumes ADR-0046 and ADR-0047 whole as unreopenable, and names the two assignments W7-D2 receives by record: the field shape for provenance declarability (ADR-0046 d21) and the finding-disposition mechanism (ADR-0047 d17). Fixes the **five-component sequence** — A brief, B record-shape doctrine, C field law, D finding-is-an-event, E proofs last — with merge discipline decided at opening: B+C mergeable only on a record-justified basis, **D not mergeable with C**, **E mergeable with nothing**. Acceptance requires nothing be called mechanical that has not been demonstrated against real bytes, with any scope boundary disclosed in its own record before landing. Creates no record of the class, no directory, no schema, no proof module, and **allocates no ADR number** |
| W7-D2 — Record-Shape Doctrine (W7-D2-B) | [`../decisions/0048-generated-evaluation-record-shape-doctrine.md`](../decisions/0048-generated-evaluation-record-shape-doctrine.md) | Accepted by human reviewer, 2026-08-19 — Tier J: the second of W7-D2's five landings. Decides where a future generated-output or authored-specimen record will live, what it is made of, how coarse it is, how its integrity is proven, and that it may never be rewritten — **and creates none of it.** The W7 record **extends** the existing evaluation-record class rather than forking it, inheriting the W5 spine whole (where the delta has never carried a winner field and routing has always been declared), with **exactly one thing changing: these records may carry text.** **Home: `governance/generated-evaluation/`, reserved and not created** — a **sibling** of `governance/evaluation/`, never shared with it, because the W5 records are content-free by construction while these may hold model-produced text, and a reader, a scan rule and a future contributor should know **from the path alone**; it sits inside an already authorised directory, so it **crosses no top-level fence and moves nothing**. **Format: JSON as material and format class only** — the ADR-0044 formula reused, fixing the material and not the blueprint: **no future W7 key name, order, nesting, nullability, value set or field is fixed here**, existing W5 keys are cited only as live precedent, and **W7-D2-C owns every future field.** **Granularity:** one record per evaluation unit with both paired captures whole, so an omitted variant is a broken record rather than a missing file — *a missing file is the kind of absence that looks like tidiness.* **Integrity: a registered manifest per run, a content hash per record, one registry entry per run**, because ADR-0034 B6's unregistered-with-reproduction precedent **does not transfer** — generated text is not reproducible and a specimen is written, not derived, so **when reproduction proves nothing the hash is the only thing left that proves anything.** **Immutability:** captured text is never edited once landed — **terminal in time as well as in flow** — with correction by erratum or supersession, and human-review fields written later as subsequent governed acts that weaken nothing. **The shape is not permission:** a home, schema, validator, manifest or empty directory authorise no contact, no specimen, no import, no harness. Four obligations specified and **none implemented** — S1 demonstrated, S2 a debt, S3 candidate-mechanical with feasibility undemonstrated, S4 review-only in full |
| W7-D2 — Field Law (W7-D2-C) | [`../decisions/0049-generated-evaluation-field-law.md`](../decisions/0049-generated-evaluation-field-law.md) | Accepted by human reviewer, 2026-08-19 — Tier J: the third of W7-D2's five landings and **the complete blueprint** ADR-0048 chose the ground for — **pouring no concrete.** One wrapper, **15 top-level fields in governed order**, 20 nested positions, 5 capture fields, 5 finding-event fields, **46 schema-property positions**, **4 nullable positions** each with a named lifecycle reason, and **12 fixed object shapes**. **Provenance by `text_class`, never appearance**, with two constraints making a laundered claim *invalid* rather than merely untrue: `generated_output` requires contact to have occurred, and a specimen requires a non-null `authoring_record`. **Pair completeness is structural** — `captures` is keyed by the declared variant labels, with no `paired` boolean and no expected-count field. **Four structural absences**: no winner under any name, no capture-level recirculation authority, no machine-valid human-review path, and **no matched-text field on a finding**, so a finding has nowhere to put what it found. **Input-source non-recirculation** closes the other side: no GER identifier, class path, or capture address may resolve through `inputs` — while a later record *citing* a GER as evidence remains lawful. **Two coherence laws**: `routed-to-review` implies `human_review.routed`, one-way; and per-capture `scan_status` matches its finding references both ways. The **byte-fixed synthetic notice** is authored here on the ADR-0045 precedent because the live fixture marker is false of this class; the **ADR-0046 ceiling is byte-identical and last**; `text_digest` is SHA-256 over decoded UTF-8 with no normalisation, **not the record hash**, which stays manifest-only. Mints the meaning-free **`GER-####`** namespace and **allocates no identifier**. Four vocabularies stop at their owners' doors — W7-D3, W7-D2-D, W7-D4, W7-D6. **Nine proof obligations, none implemented**: T1–T3 mechanical and demonstrated, T4/T5/T7/T8/T9 debts, **T6 review-only in full** |
| W7-D2 — Finding-Is-An-Event and Finding-Disposition Mechanism (W7-D2-D) | [`../decisions/0050-finding-is-an-event-and-finding-disposition-mechanism.md`](../decisions/0050-finding-is-an-event-and-finding-disposition-mechanism.md) | Accepted by human reviewer, 2026-08-20 — Tier J: **the fourth of W7-D2's five landings**, completing the three things ADR-0049 left open inside its five-field finding event and **building none of it.** A finding comes into existence **at capture-time scan**, because capture is publication and no upstream cleanliness transfers. **`finding_id` is record-local and meaning-free** — identity is `(record_id, finding_id)` and **no repository-wide namespace is minted**, since a trackable finding identifier would be authority a finding must never acquire. **`locus = { character_start }`**: one integer, **1-based Unicode code points of the decoded capture text**, match-start only, no end, no length, **no string member at any depth**, so it cannot become a text-bearing channel; reconciled with ADR-0049's digest, which hashes the UTF-8 bytes of the same value. **Three disposition values** — `open`, `routed_for_public_safety_review`, `withheld_from_publication` — **none landable, none meaning the finding stopped mattering**; three packet tokens were re-derived against ADR-0049 and removed. **Part I prohibits any scan-allowlist entry under `governance/generated-evaluation/`**, grounded in the live mechanism being **line-scoped rather than match-scoped**. **The suppression mechanics are category-sensitive:** for a **committed-pattern** finding the machinery offers an allowlist route and **this record deliberately refuses it**; the **`local-wordlist`** branch **consults no allowlist at all**, so there is nothing there to refuse — and in both cases an unsuppressed finding returns 1 while the suite requires 0, so **every finding-bearing GER is non-landable under the resulting law**. A **`local-wordlist` coordinate seam is preserved, not solved**, for a later governed act to resolve. A **19-condition landing predicate** and **stop-and-report** rather than editing, truncating or paraphrasing a capture. **Finding disposition ≠ W7-D6 human-review disposition.** **U1–U6 handed to W7-D2-E, none implemented**, with **U6 review-only in full**. **Part Q remains unresolved:** if this record lands, ADR-0046 decision 13's second branch has no implementation, so a capture producing a scan finding cannot be published as a GER — a real narrowing left visible for its own governed decision. Authorises no implementation, model contact, specimen, generated-output import or harness |
| W7-D2 — Proof Completion Record (W7-D2-E) | [`W7-D2-E-proof-completion-record.md`](W7-D2-E-proof-completion-record.md) | Accepted by human reviewer, 2026-08-20 — Tier J: **the fifth and final of W7-D2's five landings**, proving the record-shape law that ADR-0048, ADR-0049 and ADR-0050 already landed and **creating no artefact of the class.** Nineteen inherited proof obligations are classified and accounted for: **5 LIVE** (S1 T1 T2 T3 U1), **10 READY / DEBT** (S2 T4 T5 T7 T8 T9 U2 U3 U4 U5) and **4 REVIEW-ONLY** (S3 S4 T6 U6) — 5 + 10 + 4 = 19, with no twentieth obligation invented. One **source-derived proof module** of **exactly eighteen test functions across five test classes** parses its expectations from the landed records rather than re-expressing them: the twelve object shapes from ADR-0049's own field tables and `Holds` cells; the counting, vocabulary, canonical-order and nullability statements reconciled against one another through a **single integrity checker** whose mutants prove that **compensating errors cannot preserve a total**; the **integrity graph parsed from decision 41's literal dependency chain** rather than constructed; and **every governed constant — reserved home, non-authority ceiling, disposition set, `locus` field — derived from landed source** and cross-reconciled wherever two records state the same fact. **S3 is resolved REVIEW-ONLY on demonstrated insufficiency, not on preference:** a disposable throwaway repository outside this one showed a **path-scoped history check passing while the law was broken** once a record is renamed and then edited, and an id-scoped check cannot separate an unlawful edit from deletion followed by reappearance of the same identifier — so a complete mechanical proof would need identity or lifecycle semantics ADR-0049 did not state, and **this record invents none and resolves the question in neither direction.** **S2 implements only the set-and-hash relation over neutral inputs and encodes no manifest schema**, because no governed manifest shape exists to bind to. **The ten debts remain debts despite runnable predicates** — the predicates test rule machinery, not an absent subject, and two anti-vacuity tests keep the distinction from being quietly deleted while the suite stays green; **no pending-ledger row is added, converted or touched.** **The four review-only duties remain human**, and are neither tests, skips nor TODOs. **ADR-0050's Part Q publication seam remains unresolved** and **the `local-wordlist` coordinate seam remains unresolved.** **No class artefact is created:** no generated-evaluation home, schema file, manifest, record, `GER-####` identifier, specimen, model contact, generated import, harness or dependency exists, and **ADR-0051 is neither allocated nor reserved.** **W7-D2 shape-completes on publication and remote verification**, discharging ADR-0047 **precondition 6** and leaving **preconditions 2, 3 and 7 outstanding**; **W7-D3 remains unopened and no model may be contacted** |
| W7-D3 — Model Boundary, Adapter and Dependency Crossing: Opening / Design Brief | [`W7-D3-model-boundary-opening-brief.md`](W7-D3-model-boundary-opening-brief.md) | Accepted by human reviewer, 2026-08-21 — Tier J: **opens W7-D3 for brief-governed model-boundary decision work only**, and nothing else. **The later decision is Tier F**; **this brief selects none of A, B, C or D** — repository-managed local dependency, adapter to a local model already running outside the repository, manual governed model-output import, or no public model contact with authored synthetic specimens — and **opening the decision work is not taking the decision.** Carries a neutral four-option frame that supplies dimensions but **no weights and no ordering**, and classifies every material claim as law, live mechanism, precedent or planning inference so no inference can later be cited as doctrine. **The six-seam pre-execution routing register is binding, and split by kind rather than by one blanket rule:** **four are HARD PREREQUISITES** — manifest artefact shape before any run manifest or GER landing · scan-environment representation before any governed run is represented · the W7-D2-E proof succession before the first GER · GER lifecycle and identity law including reuse semantics before the first identifier allocation — and **two are LAWFULLY CARRYABLE LIMITATIONS only on their stated conditions**: Part Q only if its narrowing is explicitly accepted and execution proved lawful under it, and the `local-wordlist` coordinate seam only under the complete per-run posture, where **dormant is never resolved and inactivity may never be manufactured.** Requires **one bit of scan-environment provenance** — the effective branch `active` or `inactive`, and nothing more revealing. **Working state remains outside the repository**: no raw capture, assembling record, finding-bearing payload or withheld text may ever be written into it. **This opening discharges none of ADR-0047's preconditions** — **precondition 2 awaits the later Tier F decision record**, and **preconditions 3 and 7 remain outstanding.** **No model contacted, no generated output, no authored specimen, no generated-evaluation home, manifest, GER or GER identifier, no harness, no dependency, and ADR-0051 neither allocated nor reserved.** **W7-D4 through W7-D7 remain unopened** |
| W7-D3 — Model-Boundary Decision: No Public Model Contact (ADR-0051) | [`../decisions/0051-model-boundary-no-public-contact.md`](../decisions/0051-model-boundary-no-public-contact.md) | Accepted by human reviewer, 2026-08-22 — Tier F: **the W7-D3 model-boundary decision, and the second and final W7-D3 landing.** **OPTION D is selected: PUBLIC W7 WILL NOT CONTACT A MODEL**, using authored synthetic generated-text specimens under the identical governed-handling regime. **This is a decision, not a deferral** — A, B and C are not left open, and each would need its own future governed amendment or crossing; nothing here schedules one. Stage one eliminated no option for inconvenience and produced one finding that reshaped Option C: **ADR-0047 decision 3(a) makes a manual governed import model contact** — *by script or by hand* — so C inherits every precondition A and B do, and is barred outright if its source is hosted. Stage two scored the four against weights fixed before the scores: **A 3.30 · B 2.60 · C 2.65 · D 4.60**, with **four sensitivity re-weightings recorded including the one under which the recommendation loses** — D still wins with law weighted at zero and evidence at fifty, but A wins if boundary expansion, operational complexity and reversibility are all given zero weight. **The applicable Tier F crossing set is proved EMPTY item by item** — no dependency, adapter, manual-import, binary, credential or configuration crossing, and no contact-bearing execution path — so **ADR-0047 precondition 2 is DISCHARGED**, not waived and not deemed inapplicable, while **preconditions 3 and 7 remain OUTSTANDING** and **precondition 6 remains DISCHARGED**. **`contact_class` remains `none`** with no contact-bearing member minted or reserved; **no dependency, credential, configuration, binary, provider, SDK or client is selected** and nothing enters `requirements.txt`. **There is no model runtime and therefore no model timeout, non-response or provider error to govern.** The **four HARD and two CARRYABLE seams remain binding and Option D discharges none of them**; **Part Q is taken exactly as it stands with no reliance on future relaxation** and **remains unresolved**, as does **the `local-wordlist` coordinate seam**. **Of M1–M8, seven are live mechanical proofs in `tests/test_w7_model_boundary_decision.py`** — M1, M2a, M3, M4, M6, M7 and M8, running 8 tests and 53 subtests with **14 of 14 planted mutants detected**; **M5 is already covered by the existing requirements-fence proof** in `tests/test_repo_state.py` and is deliberately not duplicated; **M2b is review-only**, because deciding whether ordinary prose has minted a vocabulary member rather than described a posture is a semantic judgement. **No ownerless mechanical debt remains.** Of the review-only duties **R1–R4 are dispositioned for this record by the Tier F acceptance**, and **R5 — specimen evidence must never become model evidence — is a standing duty, not a W7-D3 debt.** **No model was contacted, no specimen exists, no generated output exists**, and no generated-evaluation home, manifest, GER or identifier exists. **W7-D4 through W7-D7 remain unopened** |
| W7-D4 — Synthetic Harness Binding, Exam Paper and Traps (opening record) | [`W7-D4-synthetic-harness-binding-brief.md`](W7-D4-synthetic-harness-binding-brief.md) | Accepted by human reviewer, 2026-08-22 — Tier J: **opens W7-D4 for synthetic harness binding only**, and nothing beyond it. **It authorises no implementation landing and discharges no precondition** — ADR-0047 **precondition 7 is discharged only by the final published W7-D4 landing.** Introduces a **full-development operating mode** — one accepted architecture, one independent development cycle, one final review — replacing the per-subpart relay, with **final publication still behind explicit human approval**. **ADR-0051 Option D carried unchanged:** no adapter, runtime, provider, SDK, credential, binary or dependency; every specimen an authored synthetic specimen; **manual import is not a substitute, because ADR-0047 defines contact by the act.** **Part Q's current narrowing is ACCEPTED for W7 execution, not relaxed** — a finding-bearing record stays non-landable, no allowlist exception, no suppression route, and an interesting failure may cost the evidence. **The `local-wordlist` seam is bounded, not solved:** one public bit, `active` or `inactive`, nothing more revealing, inactivity never manufactured, and a **run-state stability rule** stopping any run whose branch state changes. **`pairing.variant_labels` closed at `variant_a` / `variant_b`.** The **26 unknowns** are re-derived from the live W5-D4 evaluation records by overt / `unknown-not-absent` / `generative_respondent_required_and_not_authorised`, each row citing its record and `probe_id`, and the **23 existing trap fixtures** are consumed as source and left unchanged; **probe count is derived from the mapping, never pre-decided**, and captures equal twice it. Fixes the specimen contract, exam semantics with **no result, verdict or disposition field**, a seven-part harness that **reuses the live scanner rather than copying it**, the run-manifest shape as an integrity index only, and the manifest validator; **designs the W7-D2-E proof succession for W7-D5 to perform**, recording that S1 reaching its lawful endpoint is state evolution, not a failed proof. Makes the **working-state-outside-repository** invariant mechanical. Harness home is **`tests/`** by W5 precedent; `engine/` and `runtime/` are excluded absent a hard-stop review. Fourteen proof families, a nine-item review-only register including **R5**, and eighteen hard-stop conditions. **No exam, specimen, harness, manifest, generated-evaluation home, GER or GER identifier is created by this opening; no model contacted; no generated output; W7-D5 unopened** |
| W7-D1 — Exclusion-List Conformance (P4) Decidability Correction (ADR-0052) | [`../decisions/0052-exclusion-list-conformance-p4-decomposition.md`](../decisions/0052-exclusion-list-conformance-p4-decomposition.md) | Accepted by human reviewer, 2026-08-22 — Tier M: an **implementation-derived, then review-corrected** amendment to ADR-0046, landing **doctrine first** and reconciling no implementation. W7-D4's attempt at P4 against the first artefact of the specimen class found that **decision 11 is a prohibition over kinds, and no kind in it is decidable as a kind from repository bytes** — family membership turns on provenance, character, context and effect. **P4 is decomposed as ADR-0021 d.3/§57 require**, a compound term being decomposed rather than called partly automated. **P4a — mechanical exclusion surface guards:** the **live family inventory is source-read from decision 11**, while the **guard inventory and its family mapping are governed declarations reconciled to it and never inferred from semantic prose**, each guard carrying governed positive and clean controls, and failing on any of five decidable conditions. **P4a carries four explicit non-claims**, including that it detects no semantic narrowing of a guard beyond its governed controls. **P4b — complete exclusion-list conformance: review-only in full across all eleven families**, failing when provenance, character, context or effect places an artefact inside a prohibited family **whether or not any surface form is present**. **P4 conforms only when P4a is green AND P4b has been reviewed, and P4a green is no evidence for P4b.** The **8+3 partition is withdrawn**: eleven wholly prohibited, eight currently guarded, three currently unguarded, **all eleven inside P4b**. The ordinary scan is **measured and shown insufficient** — five of the eight guarded families receive no category at all. **"P4 green" leaves the vocabulary.** **Decision 11, decision 12 and every prohibition are untouched**; the instrument changes, never the prohibition. ADR-0046 Part H gains a one-line pointer and its hash is recomputed in the same commit, per the ADR-0021 precedent. **No implementation is reconciled here, no model contacted, no generated output, no GER, no generated-evaluation home; W7-D4 stays open and p7 stays outstanding** |
| W7-D4 — Synthetic Harness Binding, Exam Paper and Traps (development and completion record) | [`W7-D4-synthetic-harness-binding-record.md`](W7-D4-synthetic-harness-binding-record.md) | Accepted by human reviewer, 2026-08-23 — Tier J: **the single completion record of W7-D4**, produced under the brief's full-development operating mode and landing the instrument it reports, reconciled onto the ADR-0052 baseline. **Three artefacts and nothing else:** the synthetic exam paper `fixtures/SYNTHETIC-w7-d4-exam.json`, the deterministic standard-library harness `tests/w7_synthetic_evaluation_harness.py`, and the fourteen-family proof module `tests/test_w7_synthetic_harness.py`. **The 26 unknowns are re-derived from the live W5-D4 evaluation records**, never transcribed, and **all 23 live trap fixtures are accounted for and left byte-unchanged** — three carry two unknowns each, which is why 23 traps yield **26 probes and 52 captures**, with **52 distinct authored specimens**. **No non-executable trap**, and executing a trap as a handling probe is **never** a behavioural pass. **Option D is mechanical, not asserted:** contact false / class `none` / authority null, provenance **carried** rather than re-declared, and **no `generated_output` branch exists in the source to flip**. **The ADR-0046 ceiling is carried inside the artefact at 53 points** under specimen parity. **Exclusion-list conformance follows ADR-0052:** **P4a** runs a **governed guard inventory** of eleven rows — eight detectors with governed positive **and clean** controls, three declared unguarded — reconciled to the live decision 11 inventory **by coverage, never by order**, with all **five** failure conditions implemented and controlled and the ordinary scan **demonstrated insufficient**; **P4b** is a **named human review act across all eleven families**, and **P4a green is no evidence for it**. **Part Q's narrowing is ACCEPTED, not relaxed**; the **`local-wordlist`** seam is **bounded, not solved**. **S1 stays green and the successor predicate bites on disposable external homes**; **S2 stays READY-DEBT**. **14 tests / 156 subtests green; 57 of 57 mutants detected** against a disposable clone whose unmutated baseline is verified green. **Inherited P3, P5, P6 and Q4 reach their implementation point and are reconciled on evidence.** **No model contacted, no generated output, no GER, no GER identifier, no generated-evaluation home, manifest or record, no dependency, no scanner or allowlist change, no new suppression; W7-D5 unopened** |
| W7-D5 — Synthetic Execution Records and First Materialisation (opening brief) | [`W7-D5-synthetic-execution-materialisation-brief.md`](W7-D5-synthetic-execution-materialisation-brief.md) | Accepted by human reviewer, 2026-08-23 — Tier J: **opens W7-D5 for brief-governed implementation only**, and nothing beyond it, under the full-development operating mode with a stricter payload boundary — all payload-bearing candidate work in a disposable external clone, one reconciled final packet, Ari engineering review, then **Tara's landing-specific P4b review across all eleven families**, and **P4a green is no evidence for P4b**. Fixes, before any first `GER-####` allocation: **GER lifecycle with a permanent no-reuse law** read from authoritative history, canonical identity paths, and retention with **no automatic expiry and no D5 deletion, archival or withdrawal-from-view mechanism**; **run identity `W7-D5-RUN-01`**, consumed at first capture, with **no silent retry** and a **UTC `as_of`** carrying no location fact; the **authority split** — the frozen D4 harness is invoked with `W7-D4-SHB` as specimen-authoring authority and a pure, write-free binding layer rebinds **only the top-level run authority** to `W7-D5-SEB`; **run-atomic materialisation** of exactly twenty-six GERs plus one registered manifest, or nothing; the exact materialisation order and hash direction; the **D2-E proof succession design, performed only in Landing B** — S1 to its lawful endpoint, S2 bound to real repository bytes, S3 **candidate-mechanical under the new lifecycle law** and reclassified only after its negative controls are demonstrated; the **three-path `W7-D5-RUN-01-STOP` alternate landing** for any capture finding, branch-state change or locus-less local-wordlist finding, the report itself subject to public law and possibly non-landable; and the **advance of the ADR-0049 `GER-####` namespace declaration only at first allocation in Landing B**. ADR-0051 Option D carried unchanged: **no model contact, no generated output**, no dependency, no scanner or allowlist change. **This opening allocates no identifier, creates no home, manifest, GER, run or stop report, and changes no D4 byte** — the existing empty-namespace declaration remains true and untouched. Part Q remains unresolved with its narrowing accepted; the `local-wordlist` seam is bounded, not solved; **ADR-0047 precondition 3 remains outstanding**; D5 acceptance is never a W7-D6 disposition. **W7-D6, W7-D7 and W8 remain unopened** |

**Status:** **W7 — First-Contact Governance and Synthetic Model Evaluation — is open, with W7-D1 through W7-D4 complete, W7-D5 open under its accepted opening brief for brief-governed implementation only, and W7-D6 through W7-D7 unopened behind their own briefs.** The three-document entry gate is closed: the W6 closure record, the runway — accepted 2026-08-17 — and the accepted W7-D1 first-contact doctrine brief ([`W7-D1-first-contact-doctrine-brief.md`](W7-D1-first-contact-doctrine-brief.md)). Two obligations bind at every landing: the **non-goals**, and the **gate statement** — *W7 names model contact; it does not perform, authorise, schedule, or require it.* **W7-D1 is doctrine-complete.** ADR-0046 fixes the outer fence — synthetic-only by construction with the could-carry test, capture-is-publication, three closed input origins with no fourth and no recirculation, the growable-only exclusion list, the boundary invariant with its non-naming law, provenance declarability, and the non-authority ceiling with specimen parity. ADR-0047 names the crossing without making it: contact, generated output and authored synthetic specimen defined by provenance rather than by vendor, medium or transport; **seven conjunctive preconditions**; first contact one-way; and the fifteen-link anti-collapse chain. **Doctrine complete is not capability begun.** Of the ten proof obligations those records carry, **P1, Q1 and Q2 are mechanical and green** and prove nothing about whether the authority was named or whether contact occurred; **P2 and Q3 are permanently review-only**; and **P3, P4, P5, P6 and Q4 reached their implementation point at W7-D4**, the first landing to create artefacts of the specimen class. **They are no longer debts.** **P3** is green over all fifty-two shipped specimens. **P5** is green byte-identically at fifty-three carriage points under specimen parity. **Q4** is green from published history. **P6** is green over the artefact, with ADR-0049's T6 re-authoring residue still human. **P4 was corrected before it could be reported:** ADR-0052 ([`../decisions/0052-exclusion-list-conformance-p4-decomposition.md`](../decisions/0052-exclusion-list-conformance-p4-decomposition.md)) decomposes it into **P4a**, mechanical surface guards from a **governed inventory** reconciled to the live decision 11 inventory, and **P4b**, complete conformance, **review-only in full across all eleven families**. **P4a is necessary and not sufficient, and P4a green is no evidence for P4b.** Eleven families remain wholly prohibited, eight currently have guards, three currently have none, and all eleven sit inside P4b. **W7-D2 is sealed and ADR-0047 precondition 6 is DISCHARGED.** Opened by [`W7-D2-A-record-shape-opening-brief.md`](W7-D2-A-record-shape-opening-brief.md), it designed the container a generated or specimen sentence must arrive in, across five landings. ADR-0048 reserves **`governance/generated-evaluation/`** — reserved, not created — fixes JSON as material, sets one record per evaluation unit with both captures whole, requires a registered manifest per run with a hash per record, and makes captured text never editable once landed. ADR-0049 drew the complete blueprint: 46 schema positions across 12 fixed object shapes, 4 nullable positions, provenance by `text_class` with laundered claims made *invalid*, pair completeness structural, four absences, input-source non-recirculation, two coherence laws, a byte-fixed synthetic notice, the ceiling byte-identical and last, and the **`GER-####`** namespace minted with no identifier allocated. ADR-0050 completed the finding-is-an-event mechanism: a record-local `finding_id`, a one-field **`character_start`** locus, a closed three-value disposition vocabulary with **no landable value**, and a prohibition on any scan-allowlist entry under the generated-evaluation home — so every finding-bearing record is non-landable. [`W7-D2-E-proof-completion-record.md`](W7-D2-E-proof-completion-record.md) closed the deliverable with nineteen inherited obligations classified **5 LIVE / 10 READY-DEBT / 4 REVIEW-ONLY**. **Two statements from that landing are historical and true only as of its date:** S2 was **then** limited to the set-and-hash relation because no manifest shape existed **yet**, and ADR-0051 was **then** neither allocated nor reserved. W7-D4 has since fixed the manifest shape and bound the relation to it, and **ADR-0051 has since landed**. **W7-D3 is sealed and ADR-0047 precondition 2 is DISCHARGED.** Opened by [`W7-D3-model-boundary-opening-brief.md`](W7-D3-model-boundary-opening-brief.md), which framed four options and ranked none, it closes with **ADR-0051, the Tier F model-boundary decision: OPTION D — public W7 will not contact a model**, using authored synthetic generated-text specimens under the identical governed-handling regime. **The model-boundary decision space is closed:** A, B and C are **not** left open, each would need its own future governed amendment or crossing, and **the applicable Tier F crossing set is proved empty**. `contact_class` remains `none`, and no dependency, credential, binary or provider is selected. ADR-0033's dormant hosted class and constructable-only local class remain carried as named constraints, and **neither is opened by any of this.** **W7-D4 is complete**, opened by [`W7-D4-synthetic-harness-binding-brief.md`](W7-D4-synthetic-harness-binding-brief.md) under a full-development operating mode and closed by [`W7-D4-synthetic-harness-binding-record.md`](W7-D4-synthetic-harness-binding-record.md): a synthetic exam of **26 probes and 52 distinct authored specimens** mapping **26 of 26** recorded unknowns and **23 of 23** trap fixtures consumed byte-unchanged; a **deterministic standard-library harness** of seven responsibilities that reuses the live scanner rather than copying it, carries admitted provenance rather than re-declaring it, fences working state outside the repository, and treats the run manifest as a **closed shape** with run membership read from each record's own bytes; and a proof module of **fourteen families, 14 tests and 156 subtests** under a **57 of 57** mutation ceremony. **ADR-0047 precondition 7 is discharged only on publication and remote verification of that landing.** **After it, precondition 3 alone remains outstanding** — **not waived, not inapplicable and not failed**: no contact act occurs to exercise it, which the anti-collapse chain's fifteenth link makes a lawful resting state, and W7-D7 must report it honestly as outstanding. **Part Q remains unresolved**, with its narrowing **accepted for W7 execution** rather than relaxed. **The `local-wordlist` coordinate seam remains unresolved but completely bounded** by one public bit, a run-state stability rule and mandatory stop-and-report. **S2 remains READY-DEBT and W7-D5-owned.** The **four HARD and two CARRYABLE seams remain binding**. **What exists: fifty-two authored synthetic specimens and the W7-D4 harness.** **What does not exist: no model has been contacted, no generated output exists, no `GER-####` identifier is allocated, and there is no generated-evaluation home, no run manifest and no generated-evaluation record.** No dependency was added, the scanner and allowlist are byte-untouched, and no new suppression exists. Gate discipline is fixed and non-negotiable: the record shape lands before any harness produces records; the model boundary lands before any model contact; the harness binding lands before execution; the human-review law lands before any disposition. **W7-D5 is open under accepted `W7-D5-SEB` ([`W7-D5-synthetic-execution-materialisation-brief.md`](W7-D5-synthetic-execution-materialisation-brief.md)) for brief-governed implementation only** — `W7-D5-RUN-01` has not begun, no `GER-####` identifier is allocated, no generated-evaluation home or manifest exists, S2 remains READY-DEBT, S3 remains review-only pending the demonstrated D5 history proof, Part Q remains unresolved, and ADR-0047 precondition 3 remains outstanding. **W7-D6 and W7-D7 remain unopened**, and **W8 remains unopened**, with the generative evaluation era beyond this phase's synthetic scope separately gated.

## W8 and beyond

Not yet defined, and unopened. Any future phase opens only through its own runway and gate, as every phase before it has.
