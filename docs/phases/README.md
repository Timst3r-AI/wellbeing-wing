# Phases

**Build philosophy: capability follows governance, not the other way around.**

A phase is closed when its deliverables are complete, reviewed, and checked against the Constitution. No phase begins before the prior phase closes.

## W0 — Constitution

**Scope:** The governing document — purpose, non-goals, core laws, room boundaries, the Health Vault pattern, privacy-by-design principles, agent boundaries, risks and failure modes, open questions.

**Deliverable:** [`../constitution/W0-wellbeing-wing-constitution.md`](../constitution/W0-wellbeing-wing-constitution.md)

**Status:** Accepted by human reviewer, 2026-06-12 (v0.1.1, W0.1 cleanup pass applied) — the binding governance document for all subsequent phases.

**Closes when:** the Constitution is approved as the binding governance document for all subsequent phases.

## W1 — Governance Architecture and Data Boundary Design

**Scope:** Still no UI and no production code. Per Constitution §13, W1 delivers:

1. **Data boundary map** — every data category, its sensitivity class, its home (Vault / Profile / room records / audit), and every permitted flow, with Laws 4, 8, and 9 expressed as explicit edges that do or do not exist.
2. **Consent and scope model** — the grant object (who, what, why, how long), revocation behaviour, and resolution of Open Question 2 (revocation cascade).
3. **Authority and staleness model** — authority labels, last-reviewed timestamps, re-review triggers, and supersession (Laws 3 and 7), defined before any schema exists.
4. **Safety surfacing decision document** — resolution of Open Question 1 (Law 12 mechanics) as its own reviewed decision record.
5. **Threat model** — Vault-centred security analysis and the local-first vs hosted decision (Open Question 6), which should be decided **first**, since every other W1 deliverable inherits from it. *Keystone resolved: [ADR 0001](../decisions/0001-local-first-user-held-keys.md) (Accepted) settles the local-first vs hosted decision via user-held keys; remaining W1 deliverables inherit from it.*
6. **Evaluation plan skeleton** — which constitutional laws map to deterministic tests vs behavioural evaluation (Open Question 12).

**Status:** Closed — all six deliverables accepted by human reviewer, 2026-06-12 (D1–D3, D5, D6 in [`../architecture/`](../architecture/); D4 landed as [ADR 0002](../decisions/0002-safety-surfacing.md); ADR 0001 resolved the keystone). Closure criteria per W1-D6 §8.

## W2 — Governance Evaluation & Enforcement Foundations

**Scope:** Translate the W1 corpus into enforceable scaffolding and evaluation readiness — verification only, nothing user-facing. The only code W2 may contain is code that checks rules.

**Runway:** [`W2-alignment-report.md`](W2-alignment-report.md)

**Status:** Closed — all deliverables landed and published; sealed by [`W2-closure-record.md`](W2-closure-record.md), accepted by human reviewer, 2026-07-05.

**Deliverables:**

| Deliverable | Document | Status |
|---|---|---|
| W2-D1 — W1 Closure Record | [`W2-D1-w1-closure-record.md`](W2-D1-w1-closure-record.md) | Accepted by human reviewer, 2026-06-13 |
| W2-D2 — Governance Registry | [`W2-D2-governance-registry-brief.md`](W2-D2-governance-registry-brief.md) · registry at [`../governance/registry.md`](../governance/registry.md) | Accepted by human reviewer, 2026-07-05 |
| W2-D3 — Phase Entry Checklist | [`../governance/checklist.md`](../governance/checklist.md) | Accepted by human reviewer, 2026-07-05 |
| W2-D4 — Synthetic Fixture Strategy | [`../governance/fixtures.md`](../governance/fixtures.md) · seed set in [`../../fixtures/`](../../fixtures/) | Stages A and B accepted by human reviewer, 2026-07-05 (Stage B landed after the W2-D6 landing-mode scan) |
| W2-D6 — Public-Safety Scan | [`../../scripts/README.md`](../../scripts/README.md) (tooling; executes before W2-D5, IDs stable) | Accepted by human reviewer, 2026-07-05 |
| W2-D5 — Deterministic Test Skeleton | [`../../tests/README.md`](../../tests/README.md) (tooling; three tiers + skip ledger) | Accepted by human reviewer, 2026-07-05 |
| W2 Closure Record | [`W2-closure-record.md`](W2-closure-record.md) | Accepted by human reviewer, 2026-07-05 |

## W3 — Health Vault and Health Profile Foundations

**Scope:** The first product-spine phase: the Health Vault as evidence layer, the Draft Health Profile as derived review layer, the Approved Health Profile as working context — built on the no-AI authority path, with user review before anything becomes active. No adapters, no models, no clinical logic, no notifications, no hosted anything.

**Runway:** [`W3-runway-health-vault-profile-foundations.md`](W3-runway-health-vault-profile-foundations.md)

**Status:** **W3 — Health Vault and Health Profile Foundations — is complete and closed** (W3-D1 through W3-D6 sealed, W3-D7 phase closure published, 2026-07-06; see [`W3-closure-record.md`](W3-closure-record.md)). **W4 has not started and must open through its own runway/gate.**

**Deliverables:**

| Deliverable | Documents | Status |
|---|---|---|
| W3-D1 — Plaintext Residue and Vault Encryption Doctrine (pair) | [`0004-plaintext-residue-policy.md`](../decisions/0004-plaintext-residue-policy.md) · [`0005-vault-encryption-stack-doctrine.md`](../decisions/0005-vault-encryption-stack-doctrine.md) | Accepted by human reviewer, 2026-07-05 — one atomic landing; neither record authorises implementation on its own |
| W3-D1 — Platform Stack Doctrine and Development-Artifact Policy (pair, Landing A) | [`0006-runtime-platform-stack-doctrine.md`](../decisions/0006-runtime-platform-stack-doctrine.md) · [`0007-development-artifact-policy.md`](../decisions/0007-development-artifact-policy.md) | Accepted by human reviewer, 2026-07-05 — one atomic landing; the evaluation spike becomes runnable only after publication, outside the repo, under the landed rules |
| W3-D1 — Runtime Stack Final Selection | [`0008-runtime-stack-final-selection.md`](../decisions/0008-runtime-stack-final-selection.md) | Accepted by human reviewer, 2026-07-05 — engine-spine selection on first-pass evidence (local-process class); the first binding installation remains a future, separately authorised fence-crossing |
| W3-D1 — Import Boundary and Minimal Review Posture (pair, Landing B) | [`0009-import-file-boundary.md`](../decisions/0009-import-file-boundary.md) · [`0010-minimal-review-posture.md`](../decisions/0010-minimal-review-posture.md) | Accepted by human reviewer, 2026-07-05 — one atomic landing; the engine brief consumes the pair together; neither record authorises implementation on its own |
| W3-D1 — Backup Guidance and Key-Loss Wording (trust-sentences pair, Landing C) | [`0011-local-backup-guidance.md`](../decisions/0011-local-backup-guidance.md) · [`0012-key-loss-onboarding-wording.md`](../decisions/0012-key-loss-onboarding-wording.md) | Accepted by human reviewer, 2026-07-05 — one atomic landing; consequence and remedy in one breath; completes the W3-D1 cluster (8/8); neither record authorises implementation on its own |
| W3-D2 — Engine Milestone 1: store skeleton (first product landing) | [`../../engine/`](../../engine/) · [`../../requirements.txt`](../../requirements.txt) | Accepted by human reviewer, 2026-07-05 — Tier F: one product directory, one exact-pinned dependency set, self-testing fence amendment; test-supplied keys only; import, derivation, custody, and export deferred to their milestones |
| W3-D2 — Engine Milestone 2: import path | [`../../engine/`](../../engine/) · [`0009-import-file-boundary.md`](../decisions/0009-import-file-boundary.md) | Accepted by human reviewer, 2026-07-05 — Tier J: intake operation per ADR 0009 (bytes plus user-supplied provenance in, one sealed record out; type verification capped at magic-number/shape depth; provisional 25 MB operational cap, not doctrine); interpretation of every kind, vault layout/naming/indexing, envelope finalisation, key custody, and export remain deferred to their own decisions |
| W3-D2 — KDF, Custody, and Envelope Selection | [`0013-kdf-custody-envelope-selection.md`](../decisions/0013-kdf-custody-envelope-selection.md) | Accepted by human reviewer, 2026-07-05 — closes ADR 0005's two named opens: moderate KDF profile (evidence-based, micro-spike, review-dated provisional), passphrase-alone custody with no recovery path in any form, two-layer wrap architecture, per-record envelopes; the implementing milestone remains separately authorised |
| W3-D2 — Engine Milestone 3: key custody | [`../../engine/`](../../engine/) · [`0013-kdf-custody-envelope-selection.md`](../decisions/0013-kdf-custody-envelope-selection.md) | Accepted by human reviewer, 2026-07-06 — Tier J: ADR 0013 as code — versioned key envelope (own file, caller-chosen path), two-layer wrap, moderate-profile derivation, passphrase change re-sealing the envelope only; no keyfile, no recovery path in any form; first-run wording, strength posture, vault layout, and backup/export mechanics remain deferred |
| W3-D2 — Engine Milestone 4 (Landing 1): residue at scale and format seam | [`../../tests/`](../../tests/) · [`../../engine/README.md`](../../engine/README.md) | Accepted by human reviewer, 2026-07-06 — Tier J: synthetic vault at scale (all-ciphertext property proven at every checkpoint, including after a killed process), cross-format refusal matrices, and the engine's in-ink documentation (honest residuals, threat-register entries, non-atomic-write finding); atomic write remains a possible future decision; D2 closure follows as its own landing |
| W3-D2 — Closure Record (Landing 2) | [`W3-D2-closure-record.md`](W3-D2-closure-record.md) | Accepted by human reviewer, 2026-07-06 — Tier J: seals the engine spine — published chain cited by commit, verification state at closure, honest findings carried forward, deferred list preserved; authorises nothing to be built; W3-D3 not started |
| W3-D3 — Profile Object Model, Milestone 1: the grammar as shapes | [`../../engine/core/profile.py`](../../engine/core/profile.py) | Accepted by human reviewer, 2026-07-06 — Tier J: W1-D3's authority/staleness grammar as pure data structures — inseparable label pairs, bounded unknowns, contradiction and supersession shapes, the Approved layer as shape only (no instance path exists), ledger event shapes, staleness as a pure function over injected intervals (no clinical defaults); no persistence, no transitions, no review path — those remain W3-D3 M2 and W3-D4 matters under their own gates |
| W3-D3 — Profile Object Model, Milestone 2: sealed profile persistence | [`../../engine/core/profile_records.py`](../../engine/core/profile_records.py) | Accepted by human reviewer, 2026-07-06 — Tier J: profile items and bounded unknowns sealed through the store path unchanged; in-payload typing with evidence-by-absence (no record format v2); truth-label persistence refused at the only write path per the minimal-review-posture record; loading reconstructs through the object-model constructors; contradiction/supersession/ledger-event persistence and the transition engine remain deferred to W3-D4 under its own gates |
| W3-D3 — Closure Record | [`W3-D3-closure-record.md`](W3-D3-closure-record.md) | Accepted by human reviewer, 2026-07-06 — Tier J: seals the Health Profile object model — published chain and the seven planning rulings cited, verification state at closure, honest findings carried forward (including the completeness nuance: transition-era shapes exist but do not persist), deferred list preserved; authorises nothing to be built; W3-D4 not started |
| W3-D4 — Transition Engine, Milestone 1: catalogue, matrix, and validator | [`../../engine/core/transitions.py`](../../engine/core/transitions.py) | Accepted by human reviewer, 2026-07-06 — Tier J: all eight W1-D3 transitions catalogued (understanding, never permission); the runnable matrix as a whitelist with refusal by absence; a pure content-free validator classifying runnable/gated/dormant/illegal; T1 catalogued but dormant until grant machinery exists by its own authority; T2/T4/T7/T8 recognised as gated — no application, no user acts, no events, no persistence change; the applier and its structural guards remain W3-D4 M2 under its own gates |
| W3-D4 — Transition Engine, Milestone 2: applier with user-act gating | [`../../engine/core/transitions.py`](../../engine/core/transitions.py) | Accepted by human reviewer, 2026-07-06 — Tier J: per-transition appliers validating through the classifier, in-memory only, emitting exactly one data-only ledger event per application (emitted, never stored — durable ledger deferred to its own decision); user acts as data, constructible only in the test tree within repository enforcement (AST guard landed in the same commit); truth labels computable in memory during gated synthetic tests and still unpersistable; T7 composite implemented (supersede + mint correction, retained history); T8 supersession only, no reactivation; contradiction/supersession persistence remains deferred |
| W3-D4 — Closure Record | [`W3-D4-closure-record.md`](W3-D4-closure-record.md) | Accepted by human reviewer, 2026-07-06 — Tier J: seals the transition engine — published chain (M1, M2, and the pending-ledger cleanup) cited by commit, the planning-brief and in-flight rulings restated, verification state at closure, honest findings carried forward, deferred list preserved; authorises nothing to be built; W3-D5 and later deliverables not started |
| W3-D5 — Closure Record (mapping-based) | [`W3-D5-closure-record.md`](W3-D5-closure-record.md) | Accepted by human reviewer, 2026-07-06 — Tier J: seals residue rules as a deliverable satisfied by absorption — no separate D5 milestones existed; the expected work landed inside the published D2/D3/D4 evidence discipline, and the record carries the full ADR 0004 clause-by-clause mapping with proof names and commit anchors, two honest notes, and the forward gates; authorises nothing to be built |
| W3 — Durable-Ledger Doctrine (D6 gate) | [`0015-durable-ledger.md`](../decisions/0015-durable-ledger.md) | Accepted by human reviewer, 2026-07-06 — Tier J: the durable ledger exists as doctrine — sealed under the master-key custody boundary, independently sealed append frames, doctrine-wide event scope with v1 limited to existing transition events, backup-unit membership as sealed ciphertext, erasure as the user's knowing act only; the implementation milestone remains unstarted, and D6 remains gated until its sequencing is authorised; authorises no implementation |
| W3 — Durable-Ledger Store (implementation milestone) | [`../../engine/core/ledger_store.py`](../../engine/core/ledger_store.py) | Accepted by human reviewer, 2026-07-06 — Tier J: ADR 0015 as code — the WBWL versioned ledger file of independently sealed append frames under the master key; append order preserved and proven bytewise append-only; torn tail refused with intact history preserved (one-event blast radius); whole-ledger erasure as an explicit act; the appliers untouched (the writer is caller-side); import/custody emission remains a named future extension; backup/export mechanics remain D6 |
| W3-D6 — Milestone 1: backup export and restore symmetry | [`../../engine/core/backup.py`](../../engine/core/backup.py) | Accepted by human reviewer, 2026-07-06 — Tier J: the WBWB portable ciphertext file — public header, reachable key envelope, sealed payload holding manifest, records, and ledger with no structure visible outside it; restore is file-plus-passphrase into an empty target only, five-step validation before any write, no record plaintext decrypted to restore; member sets caller-supplied (the engine never discovers files); single-record export (M2) and the D6 closure (M3) remain under their own gates |
| W3-D6 — Milestone 2: export as a right | [`../../engine/core/export.py`](../../engine/core/export.py) | Accepted by human reviewer, 2026-07-06 — Tier J: single-record plaintext export at the data layer — the imported bytes leave byte-identical under no gate but the owner's act; provenance always returned as structured data, written as a sidecar only when explicitly requested; destinations validated before any write, existing destinations refuse, profile-class records refuse (their export deferred to its own governed path); no batch, no whole-vault plaintext export (own future record), no UserAct or review machinery; the D6 closure (M3) remains under its own gate |
| W3-D6 — Closure Record | [`W3-D6-closure-record.md`](W3-D6-closure-record.md) | Accepted by human reviewer, 2026-07-06 — Tier J: seals export/backup/restore — published chain cited by commit; the seven planning rulings (headlined by the WBWB circularity correction) and two M2 rulings restated; consumed ADR 0015 inputs named; the WBWB container shape, five-step restore validation order, and export-right contract carried in ink; honest findings and deferred list preserved; authorises nothing to be built; W3-D7 not started |
| W3-D7 — W3 Closure Record | [`W3-closure-record.md`](W3-closure-record.md) | Accepted by human reviewer, 2026-07-06 — Tier J: seals the phase — the six deliverable seals cited by commit, criteria assessed against the runway, verification state, the nine future-owned pending stubs named, the incident log (five entries with lessons), the honest-findings roll-up, the W4/W5 gate naming next-era draft material without pre-deciding it, and the deferred list; authorises nothing to be built; W4 not started |

## W4 — Room Contracts

**Scope:** The first jurisdiction phase: four room contracts — Wellness, Kitchen, Gym, Meditation — each a written, testable statement of what a room may read, may write, must never infer, and how it behaves when relied-on context is unknown, stale, or contradicted. Built on the sealed engine spine and the W1 data-boundary map. No adapters, no models, no review surfaces, no UI — the contracts constrain future capability; they are not capability.

**Runway:** [`W4-runway-room-contracts.md`](W4-runway-room-contracts.md)

**Status:** **The three-document W4 entry gate is complete:** the W3 closure record is gate document one, the W4 runway is gate document two, and the accepted DR-W4-01 first-deliverable brief is gate document three. **ADR 0016 (Room Contract Template) is the first landed W4 doctrine record, and W4-D2 (Wellness Room Contract) is the first landed room contract.** **W4 opened for governed doctrine, brief, and room-contract work only, and is now closed:** W4-D1 through W4-D7 are complete, with every runway obligation satisfied, intentionally deferred under named doctrine, or honestly recorded as an external dependency; the closure became effective on publication and authoritative remote verification of the W4-D7 closure record, under the same effective-on-publication rule stated below. Named W5, W6, and external matters remain carried and unresolved, and closure authorises nothing new. That opening authorised no scaffolding, no validator implementation, no adapter work, no review surfaces, no engine changes, and no W5/W6 implementation. Every later W4 record and deliverable still requires its own brief, review, and ceremony. *Opening is effective on publication:* while the ADR-0016 commit remains local it is not yet in force; W4's opening takes effect only once ADR-0016 is published on `origin/main` and verified. The deliverable sequence ran W4-D1 (six room-contract doctrine records) through W4-D7 (closure), with forward dependencies on the W5 adapter layer (isolation enforcement) and the W6 governed string catalogue (wording routing) named and carried, not blocking — and still carried, not resolved, at closure.

**Deliverables:**

| Deliverable | Documents | Status |
|---|---|---|
| W4-D1 — Room Contract Template (DR-W4-01) | [`../decisions/0016-room-contract-template.md`](../decisions/0016-room-contract-template.md) | Accepted by human reviewer, 2026-07-10 — Tier J: the fixed eleven-section room-contract template (fixed order and numbering, every section present and non-blank, "not applicable" written with a reason, a mandatory eleventh open-questions section reading "None at acceptance." when empty, and the Meditation reads-nothing case as the worked N/A example); decides the skeleton only — it fills no room's sections and gates the four room contracts (W4-D2…D5) and the conformance validator (W4-D6); no contract, validator, adapter, surface, or engine code; wording sections route to the future W6 governed string catalogue as review-of-record |
| W4-D1 — Room Isolation Model (DR-W4-02) | [`../decisions/0017-room-isolation-model.md`](../decisions/0017-room-isolation-model.md) | Accepted by human reviewer, 2026-07-10 — Tier J: the architectural room-isolation standard that fills every contract's Processing Boundary section — each room's processing interaction occurs in a context that has never contained another room's content, cross-room content is unreachable (not merely forbidden), one processing grant binds exactly one room, and filtering or output controls alone are insufficient; grant scoping and isolation are separate complementary walls; W5 later realises the property while W4 specifies no mechanism, behavioural evaluations still apply, and any exception needs its own accepted record with no silent downgrade; the display-vs-processing-context split and per-room context-cost evidence remain deferred to W5; no adapter, session, prompt, payload, validator, surface, or engine code — no implementation authorised; the Meditation Room receives the uniform standard, its no-outward-signal property left to DR-W4-04 |
| W4-D1 — Read/Write Scope Confirmation (DR-W4-03) | [`../decisions/0018-read-write-scope-confirmation.md`](../decisions/0018-read-write-scope-confirmation.md) | Accepted by human reviewer, 2026-07-10 — Tier J: W1-D1 is the sole source of truth for every room's read and write scope; contracts carry scope only through the paired form — an exact W1-D1 reference as controlling authority plus a complete verbatim quotation for legibility — and a quotation/reference mismatch fails review; paraphrase is prohibited, and no contract may widen, narrow, reinterpret, simplify, modernise, or clarify a D1 scope; scope amendments land in W1-D1 first and contracts follow, never lead; absence of a D1 edge means absence of authority; rooms write only their own authorised record classes with the D3-T3 label-on-entry by reference, and no room writes into another room's jurisdiction; records that direct reading of live W1-D1 exposed drift in the pack/runway summaries (D1 wins — those are planning materials, not scope authority); confirms and transmits scope, mints none; no adapter, validator, surface, or engine code — no implementation authorised |
| W4-D1 — Cross-Room Inference Prohibition Standard (DR-W4-04) | [`../decisions/0019-cross-room-inference-prohibition-standard.md`](../decisions/0019-cross-room-inference-prohibition-standard.md) | Accepted by human reviewer, 2026-07-11 — Tier J: a room must not derive a health-relevant or room-jurisdiction conclusion outside its granted context — forbidden inference is constitutionally prohibited (even when user-requested), and observable consequence is how a breach becomes testable, never permission to infer silently, with silent behaviour change the primary target; hedging, questions, and user attribution do not launder it; every future room contract requires a room-specific named-bait list (a floor, not a ceiling; each bait mapped one-to-one to a future evaluation fixture) — but ADR-0019 authors no room's bait list; named-bait lists are the highest public-safety surface, canonical grammar placeholders only; user-requested speculation inherits the settled refusal shape and mints no claim, restriction, recommendation, warning, or authority state; room processing has no pattern-flag or "for review" pathway; a confirmed breach is highest-defect with a permanent regression fixture and output filtering alone is never an adequate remedy; complementary to ADR-0017 (isolation) and ADR-0018 (scope), with the Meditation Room on the uniform standard; no fixture, adapter, validator, surface, or engine code — no implementation authorised |
| W4-D1 — Unknown/Stale/Contradicted Behaviour Standard (DR-W4-05) | [`../decisions/0020-unknown-stale-contradicted-behaviour-standard.md`](../decisions/0020-unknown-stale-contradicted-behaviour-standard.md) | Accepted by human reviewer, 2026-07-11 — Tier J: the one shared behaviour table for all four rooms when relied-on context is unknown, stale, expired, or contradicted, with W1-D3 as controlling authority — all six state labels (current, review due, stale, expired, superseded, unknown freshness) carried with their verbatim semantics; every contract carries the table by exact reference plus complete verbatim quotation, and rooms instantiate wording, not rules; unknown means unknown and absence is never confirmation; contradiction is held honestly and newer is not truer until the user says so; the safety-relevant inline uncertainty sentence is always shown, not grace-conditioned; acknowledgement-before-continuing for safety-relevant expired/contradicted/unknown context is a surfacing-order requirement, not a functional block; most-protective framing is surfacing/verbal only and mints nothing; block-versus-warn remains open under W1-D3 §10.6 with the §6.4 floor in every row; final strings and catalogue IDs belong to the future W6 governed string catalogue (review of record until then); no behaviour-table implementation, catalogue, fixture, adapter, validator, surface, or engine code — no implementation authorised |
| W4-D1 — Contract Validator Requirements (DR-W4-06) | [`../decisions/0021-contract-validator-requirements.md`](../decisions/0021-contract-validator-requirements.md) | Accepted by human reviewer, 2026-07-15 — Tier J: the sixth and final W4-D1 doctrine record — decidability, not implementation cost, draws the validator line: a contract term is mechanically checkable if and only if its correctness is decidable from deterministic repository artefacts without interpretation, and every term is mechanically checkable, review-only, or explicitly decomposed — no ambiguous middle; judging validators are prohibited (a judging validator is doctrine minting by machinery); contract validators inspect deterministic repository artefacts while behavioural and runtime checks remain evaluation territory; failure evidence is minimal and public-safe; a mechanical pass does not equal acceptance; validators extend the existing deterministic suite with phase-named tests; DR-W4-06 (doctrine) and W4-D6 (plan/build) remain separate, and validator implementation waits until all four room contracts are accepted; the W6 catalogue-ID class remains dormant and dependency-named; no implementation authorised — with this record's publication the W4-D1 doctrine set is 6 of 6, and W4 remained open at this landing for governed contract work only, not implementation |
| W4-D2 — Wellness Room Contract | [`../rooms/wellness-room-contract.md`](../rooms/wellness-room-contract.md) | Accepted by human reviewer, 2026-07-22 — Tier F: the first of four room contracts (Wellness); all eleven ADR-0016 sections present and non-blank; read and write scope carried only through ADR-0018's paired form (exact W1-D1 reference plus complete verbatim quotation); a five-item named-bait floor (WELL-B1…B5 — a floor, not a ceiling — each declaring exactly one future W4-D6 evaluation fixture that does not yet exist); ADR-0020's one shared behaviour table carried verbatim across all six W1-D3 states with contradicted context; ADR-0017 isolation declared, not operationally enforced (W5 realises it); wording routes to the future W6 governed string catalogue as review-of-record; W5 and W6 remain deferred and unread; no validator, fixture, adapter, surface, or engine code — no implementation authorised |
| W4-D3 — Kitchen Room Contract | [`../rooms/kitchen-room-contract.md`](../rooms/kitchen-room-contract.md) | Accepted by human reviewer, 2026-08-04 — the second of four room contracts (Kitchen); all eleven ADR-0016 sections present and non-blank; E6 read scope (allergies, confirmed dietary requirements only), C1 write scope (recipes/preferences, grocery/staples, meal plans), and the E10 outward vendor boundary carried in ADR-0018's paired form; category #11 embeds E6 under a C3-handling rule while remaining C1; a five-item named-bait floor (KITCH-B1…B5 — a floor, not a ceiling — each declaring exactly one future W4-D6 evaluation fixture that does not yet exist); ADR-0020's shared behaviour table carried verbatim across all six W1-D3 states with contradicted context, allergy the live safety-relevant read; ADR-0017 isolation declared, not operationally enforced (W5); the E10 payload is bare item identity only, declared as a boundary only — the contract establishes no connector or VendorAdapter, claims none has been implemented or verified, and authorises none; wording routes to the future W6 governed string catalogue; no implementation or transmission authorised |
| W4-D4 — Gym Room Contract | [`../rooms/gym-room-contract.md`](../rooms/gym-room-contract.md) | Accepted by human reviewer, 2026-08-09 — the third of four room contracts (Gym); all eleven ADR-0016 sections present and non-blank; E7 read scope (confirmed injury/physical notes only), mixed C1+C2 write scope (Workout plans #12 at C1, Movement/rest/recovery logs #13 at C2), and the User-reported label on entry carried in ADR-0018's paired form; user-reported non-clinical sleep accepted as a category-#13 instance; a six-item named-bait floor (GYM-B1…B6 — a floor, not a ceiling — each declaring exactly one future W4-D6 evaluation fixture that does not yet exist), GYM-B5 (injury-absence-to-all-clear) highest priority; ADR-0020's shared behaviour table carried verbatim across all six W1-D3 states with contradicted context, injury the live safety-relevant read with a retrospective framing (an expired injury note may still apply, never healed); ADR-0017 isolation declared, not operationally enforced (W5); manual-first with current device ingestion, provenance, adapters, and derived scores excluded and no device-integration authority; no outward edge; wording routes to the future W6 governed string catalogue; no implementation or transmission authorised |
| W4-D5 — Meditation Room Contract | [`../rooms/meditation-room-contract.md`](../rooms/meditation-room-contract.md) | Accepted by human reviewer, 2026-08-09 — the fourth and final room contract (Meditation); all eleven ADR-0016 sections present and non-blank; the CM contemplative jurisdiction, a sensitivity peer of C4 behind Law 9's structural wall and never a rung of the health ladder; read scope stated from the complete edge list M1–M2 plus the “no other edges” clause in ADR-0018's paired form, with the template's worked N/A case carried for inbound other-room reads — no inbound Profile or cross-room read edge exists, while M1 (the user and their own Meditation records) remains a right, not a grant; write scope own records only — Meditation practice records #14, Reflections & contemplative notes #15, Contemplative library & annotations #16 (User-curated), all CM, User-reported on entry; silent practice writes nothing by default and an explicitly logged silent sit is a #14 record; breath awareness only, with no wider breathwork authority; persistent preferences out of scope; no movement, rest, recovery, or sleep category acquired; a seven-item named-bait floor (MED-B1…B7 — a floor, not a ceiling — each declaring exactly one future W4-D6 evaluation fixture that does not yet exist), MED-B3 and MED-B7 making the Law 9 wall testable outward and inward, with MED-B7 preserving the lawfulness of the user's own free-text reflection whatever subject it mentions; ADR-0020's shared behaviour table carried verbatim across all six W1-D3 states with a structurally different instantiation stated as such — no source-authorised CM review interval, grace period, or hard limit exists, a date is not a freshness state, and therefore zero current Meditation-specific room-register strings are derived, a present finding and not a permanent claim; no live safety-relevant Profile context can enter, and that absence is not an absence of risk; M2 the only processing edge (user-initiated, nothing persisting outside the room, no output to any other room), ADR-0017 isolation declared, not operationally enforced (W5), and M2 output never represented as source material; the user-curated teaching or scripture library authorised while the content-supply surface remains deferred and no content-rights determination is made; wording routes to the future W6 governed string catalogue with zero currently routed; no implementation or transmission authorised — with this record the four room contracts are complete |
| W4-D6 — Contract Validator M10/M12 Decidability Correction (ADR 0022) | [`../decisions/0022-contract-validator-m10-m12-correction.md`](../decisions/0022-contract-validator-m10-m12-correction.md) | Accepted by human reviewer, 2026-08-09 — Tier J: the Lane A implementation correction to ADR 0021, raised by implementation rather than review. M12's anti-map clause could not be satisfied by any accepted room contract — every accepted section 8 names its granted boundary relationally (“beyond E7”, “beyond the E6 closed list”, “beyond M1 and M2”) rather than enumerating forbidden identifiers, so the empty-intersection pass condition failed all four; telling those apart would require reading prohibition semantics, which the decidability line excludes and R10 already owns. M12 is restated as M12a anti-map identifier fidelity against a frozen per-contract expectation (Wellness, Kitchen, Gym, Meditation) with missing and unexpected identifier as its mismatch classes, plus the unchanged M12b Meditation exact-edge rule; M10 becomes closed-set contract placeholder membership, since the canonical vocabulary is closed and no accepted source enumerates a rejected token — with a governed contract-token recognition grammar explicitly bounded away from fixtures. Corrected Lane A arithmetic: M10 4, M12 5, total 47 mechanical instances (set-level 2, Wellness 10, Kitchen 11, Gym 11, Meditation 13), review surface 37 unchanged. The discovery history is preserved: 46 provisional, 48 source-derived, implementation discovery, 47 corrected — implementation stopped rather than coding around doctrine. ADR 0021 keeps a non-semantic pointer and a logged erratum; no room contract is amended; no implementation is authorised, and Lane A remained suspended until this record was published |
| W4-D6 — Lane A Contract Conformance Validator | [`../../tests/test_w4_contract_conformance.py`](../../tests/test_w4_contract_conformance.py) | Accepted by human reviewer, 2026-08-09 — Tier J: the first W4-D6 build artefact — the Lane A contract-conformance validator and its frozen expectation data (`tests/grammar/w4-contract-expectations.json`, schema version 3), extending the deterministic suite with 47 mechanical check instances against the four accepted room contracts (set-level 2; Wellness 10; Kitchen 11; Gym 11; Meditation 13) under ADR 0021 as corrected by ADR 0022; every expected value frozen from the accepted baseline with dual provenance — immutable acceptance pairs and current governed-baseline pairs, both commit↔hash coherence-proven against repository history — so the validator never learns its expected answer from the artefact under test; M6 enforces strict byte fidelity after removal of blockquote transport syntax only; M10 is closed-set placeholder membership with no rejected-token literal anywhere; M12a is anti-map identifier fidelity and M12b the Meditation exact-edge rule; baseline drift is an orthogonal diagnostic against the governed hash, never a result state; quoted source regions are excluded from every structural extraction; 47 positive assertions, 27 negative mutation scenarios, 6 false-positive guards and 8 separately-counted provenance proofs, all runtime/in-memory with no persistent synthetic contract artefact and no behavioural fixture; execution precondition: Git CLI (already required by the suite's scan invocation) plus full repository history for the provenance proofs; a mechanical pass means document conformance only — human review and ceremony remain required, at Lane A closure no fixture or catalogue identifier existed; fixture identifiers later arrived under W4-D6-BEF, while no catalogue identifier exists, and no implementation or transmission is authorised |
| W4-D6 — Lane C External Assurance Source Register | [`../governance/external-assurance-source-register.md`](../governance/external-assurance-source-register.md) | Accepted by human reviewer, 2026-08-10 — Tier J: the governed provenance-and-currency register for the external authoritative sources consulted by privacy and health-data assurance work — fourteen primary official sources across the constitutionally named alignment families and the voluntary engineering baseline, under a fixed twelve-field schema separating what a source is from whether it currently stands, with immutable allocation identifiers, explicit null/empty semantics, visible supersession routing to dependent-row review, and re-verification-before-use maintenance rules; canonical primary-source locators are individually reviewed and narrowly allowlisted for this register alone, with external links remaining default-denied across the corpus; registration records evidence provenance only — it certifies nothing, determines no applicability, and the Wing makes no legal-certification claim; this row records the register artefact only — it does not claim Lane C closure or W4-D6 closure, and no implementation is authorised |
| W4-D6 — Lane C Privacy & Health-Data Assurance Record | [`../governance/privacy-health-data-assurance-record.md`](../governance/privacy-health-data-assurance-record.md) | Accepted by human reviewer, 2026-08-10 — Tier J: the governed evidence-mapping record for privacy and health-data assurance — sixty-five rows mapping thirteen stable primitives to repository evidence across a framework-neutral core, four alignment overlays (HIPAA-aligned, APP-aligned, GDPR-style, FTC-HBNR) and the voluntary NIST SP 800-122 engineering baseline; exactly sixteen stored fields per row with the assurance vocabulary derived at presentation and never stored; four applicability records all unresolved with no role or basis asserted — applicability unresolved is never not applicable and never a pass; three evidence-timing tiers separating repository evidence now, W5-dependent runtime evidence, and external organisational evidence that no repository artefact can green; honest states preserved — not-evidenced and externally dependent rows are stated as such, never inflated; in the same landing the Source Register's dependent-row fields were populated for the eleven cited sources through governed register maintenance, refreshing its registered content hash; evidence mapping only — the record certifies nothing, determines no applicability, and the Wing makes no legal-certification claim; the record and the External Assurance Source Register are both published and authoritatively remotely verified, the register's dependency rows populated with its registered content hash refreshed in the same commit; Lane C evidence mapping is complete and Lane C is closed under the accepted criterion — complete and honest, not universally green: closure does not mean every row is evidenced, all four regulatory/alignment applicability records remain unresolved with no role or basis asserted, evidence mapping is never certification and asserts no legal requirement as satisfied, and Tier 2 and Tier 3 dependencies remain exactly where recorded — W5 must not collapse Tier 3 evidence into repository evidence; Lane C closure closes Lane C only — Lane B remained separate and declaration-only at this closure (23 declarations, 0 fixtures, 0 fixture IDs) and W4-D6 as a whole was not declared closed at this closure; W5 and W6 remain unopened; no implementation is authorised |
| W4-D6 — Lane B Behavioural Evaluation Fixture Strategy | [`../governance/behavioural-evaluation-fixtures.md`](../governance/behavioural-evaluation-fixtures.md) | Accepted by human reviewer, 2026-08-11 — Tier J: the Lane B Stage A strategy extension that the Synthetic Fixture Strategy (W2-D4) names in its non-scope section — extends W2-D4, never replaces it; twenty-three fixture-reference obligations exist, one per named-bait declaration across the four sealed room contracts, and the accepted cardinality model derives twenty-three future fixture artefacts — derived, never a symmetry target; at this landing no fixture artefact exists, no FIX identifier has been allocated, and the authoritative live bait-to-fixture map is empty by authorised state; the sealed room contracts are never amended to allocate fixture references — this document's map is the authoritative live record of fixture existence; behaviourally_unexecuted is the sole W4 execution status, no runtime behavioural evaluation has occurred, and no behavioural success claim is made; Lane B is not closed by this landing, W4-D6 was not closed at this landing, Stage B (fixture authoring, FIX allocation, map population, corpus validator) remained separately gated on this document's publication and remote verification; W5 and W6 remain unopened; no implementation is authorised |
| W4-D6 — Lane B Behavioural Evaluation Fixture Corpus | [`../governance/behavioural-evaluation-fixtures.md`](../governance/behavioural-evaluation-fixtures.md) | Accepted by human reviewer, 2026-08-11 — Tier J: the Stage B corpus landing — twenty-three SYNTHETIC behavioural-evaluation fixtures, one per named-bait declaration across the four sealed room contracts, landed atomically with the twenty-three-row live bait-to-fixture map and the deterministic corpus validator (`tests/test_w4_lane_b_fixture_corpus.py`); fifty-one local probes at the derived floor; every fixture remains behaviourally_unexecuted with the named W5 execution dependency preserved; the validator proves corpus conformance and referential integrity only — no behavioural evaluation has occurred or passed, and static corpus validation is never behavioural evaluation success; the corpus, its map, and the validator are published and authoritatively remotely verified, and Lane B is closed under the accepted criterion — referenced, validated, and honestly unexecuted: twenty-three declarations resolve through twenty-three map rows to twenty-three published fixtures with bijection proven, the deterministic corpus validator is published and green, and closure means the behavioural-evaluation corpus is complete and statically validated — not that any behavioural evaluation has passed; every fixture remains behaviourally_unexecuted and the named W5 execution dependency is preserved; Lane B closure closes Lane B only — all obligations addressed to W4-D6 are discharged or intentionally deferred under named doctrine, and all three governed lanes are closed; W4-D6 is therefore closed — the validator plan and build delivered and green (Lane A: mechanical document conformance only), the behavioural-evaluation corpus referenced, validated, and honestly unexecuted (Lane B), and the assurance evidence disposition complete and honest, not universally green (Lane C); W4-D6 closure authorises nothing new — no W5 runtime, behavioural-evaluation execution, or downstream implementation, no conversion of deferred or external evidence into repository evidence, and no legal-certification claim; W4 remained open pending W4-D7 at that closure; W5 and W6 remain unopened; no W5 runtime, behavioural-evaluation execution, or downstream implementation is authorised |
| W4-D7 — W4 Closure Record | [`W4-closure-record.md`](W4-closure-record.md) | Accepted by human reviewer, 2026-08-12 — Tier J: the phase closure record, satisfying W4-D7 — the full W4 runway assessed with every obligation classified satisfied, intentionally deferred under named doctrine, or honestly recorded as an external dependency; the deliverable chain cited by commit; the suite measured at 281 total — 272 passed, 9 skipped, 0 failed, 439 subtests — extended by the phase's two phase-named validator surfaces rather than merely failure-free; the pending ledger reviewed with nine future-owned stubs and zero W4-owned items; four incidents closed with dispositions and the landing-hygiene practice carried forward; the W5 gate named with its inherited obligations and not decided; W6 remains unopened; **published and authoritatively remotely verified, and W4 is therefore closed — the closure took effect under the repository's publication rule when this record became authoritative on `origin/main` and that authority was verified; no later ledger entry creates it, this row records it**; nothing new is authorised |

## W5 — Runtime Enforcement and Behavioural Evaluation

**Scope:** The first runtime phase: realising at runtime the boundaries W4 declared — the processing-context boundary, grant machinery, payload assembly and equality, transmission and disclosure mechanics, architectural isolation, and the M2 runtime — and then measuring whether those boundaries hold, including on channels that produce no output. Two governed eras under one phase: adapter/runtime enforcement, and behavioural evaluation. No review surfaces, no governed string catalogue, no UI — those remain the surface era's.

**Runway:** [`W5-runway-runtime-enforcement-evaluation.md`](W5-runway-runtime-enforcement-evaluation.md)

**Status:** **The three-document W5 entry gate is complete:** the W4 closure record is gate document one, the W5 runway is gate document two, and the accepted W5-D1 first-deliverable brief is gate document three. **ADR 0024 (AIAdapter / Processing-Context Boundary) is the first landed W5 doctrine record.** **W5 is open for governed doctrine and brief work only.** This authorises no scaffolding, no runtime or harness implementation, no directory, no dependency, no model contact, no payload construction, no transmission, no fixture execution, no pending-stub conversion, and no W6 work. Every later W5 record and deliverable still requires its own brief, review, and ceremony. *Opening is effective on publication:* while the ADR-0024 commit remains local it is **not yet in force**, and **W5's opening takes effect only once ADR-0024 is published on `origin/main` and that authority is independently verified against the remote.** The W5 runway, accepted and published on 2026-08-13, authorises **W5 briefs only**; capability arrives behind its own gates, as it has in every phase before. **No W5 implementation exists**, no model has been contacted, and all twenty-three behavioural-evaluation fixtures remain unexecuted. **W6 remains unopened**, and nothing here pulls W6 work forward.

**Deliverables:**

| Deliverable | Documents | Status |
|---|---|---|
| W5-D1 — AIAdapter / Processing-Context Boundary (DR-W5-01) | [`../decisions/0024-aiadapter-processing-context-boundary.md`](../decisions/0024-aiadapter-processing-context-boundary.md) | Accepted by human reviewer, 2026-08-13 — Tier J: the first of seven candidate W5-D1 doctrine records and the AIAdapter ADR the sealed corpus has required since W1. Decides the processing boundary as a mechanism-independent property — one governed seam; no content accepted independently of a grant and no free-text interface; the boundary outside the sealed engine with separation both ways, a future external composition root permitted and orchestration left to W5-D2; grant-derived context construction with one-room binding structural and no two-room composition point; no hidden processing-state continuity while lawful retrieval under a fresh grant remains open; context lifetime bounded by the grant with no processing-side state surviving it; display context separated from processing context, answering ADR 0017's first carried question; four-surface observability by construction with propagation claims bounded at the recipient boundary and no claim about a recipient's internals; Z5 kept distinct while W5 activates no Z5 flow; all three E12 prerequisites settled at doctrine level (scoping, authentication, impersonation prevention) with **E12 expressly remaining reserved**; the content-free ledger obligation at the boundary; structural refusal with the audit distinction preserved; and ADR 0017's exception discipline extended to the whole record. Six architecture questions carried with named owners, including the ADR 0004 residue applicability check assigned to a W5-D1 unit before any W5-D2 implementation. Resolves W0 Open Question 10. No implementation, directory, dependency, model contact, payload, transmission, harness or fixture execution is authorised |
| W5-D1 — Runtime Freshness and Unknown/Stale Behaviour (DR-W5-02) | [`../decisions/0029-runtime-freshness-unknown-stale-behaviour.md`](../decisions/0029-runtime-freshness-unknown-stale-behaviour.md) | Accepted by human reviewer, 2026-08-14 — Tier J: the second of the seven W5-D1 doctrine records. Part A fixes freshness semantics — a freshness data type is a **governance policy key**, one key shape only, normative `R`/`G`/`H` with **`S = R + G` derived**, no per-field override, the fixed-duration day of exactly 24 elapsed hours, and **seven governed keys** (medication, allergy, condition, injury, pregnancy status, clinician instructions, preference). Every threshold is a **provisional governance default — not clinically validated**, and the six equal keys carry the **non-equivalence rule**: their equality is a uniform provisional governance simplification, **not evidence of clinical equivalence**. Freshness must be valid at every governed reliance; no background polling; acknowledgement is surfacing only and **never renewal**. Part B fixes runtime consequence — **expired remains the label while the assertion is consumed as unknown**, retaining assertion, provenance and age; bounded unknown never means absence, resolution or reassurance; **safeguard 14 is predicate-based** and stale can meet it; a fail-closed withholds **only the dependent operation or output**, never the user and never the room; three **derived, never-persisted** assertion postures; and ADR-0002 and ADR-0020 apply **independently within their own memberships**, with no precedence and no inferred non-member rule. **Resolves W1-D3 §10.1 and §10.6 in body**; the historical room-contract `Open (§10.6)` text is unchanged. Deterministic obligations are separated from behavioural proof; no implementation, directory, dependency, model contact, payload, transmission, harness or fixture execution is authorised |
| W5-D1 — Grant Machinery, Consent Duration and Re-authentication (DR-W5-03) | [`../decisions/0030-grant-machinery-consent-duration-reauthentication.md`](../decisions/0030-grant-machinery-consent-duration-reauthentication.md) | Accepted by human reviewer, 2026-08-15 — Tier J: the third of the seven W5-D1 doctrine records. Part A makes W1-D2's grant grammar **structurally normative** — all thirteen elements over one already-declared edge, and a missing element, undeclared edge, blanket scope or multiple purposes make a grant **unconstructable**. Lifecycle: **proposed / declined / active / awaiting required re-authentication / review-due / expired / revoked**; **no freshness vocabulary**, `stale` barred, `suspended` omitted, and `review-due` is **surfacing only**. **Re-affirmation creates a successor grant** with new identity, new validity interval and inspectable predecessor lineage — never an in-place extension; **expired and revoked never reactivate**; material change requires a **new grant**. Part B: E2 single-task with **fresh re-authentication at grant time**; profile scoped read standing to a **hard 180-day ceiling** after which it expires and **silence neither renews nor creates a successor**; AI processing single-task default, session maximum; E10 per transmission; E9 single-task; **no unbounded duration**. A session cannot silently revive, and **inactivity-based termination is non-operative** until a later governed decision supplies the threshold — which W5-D2 may not supply by discretion. Re-authentication proves **only present user control for the exact proposal**, in a fixed five-step order, and never becomes a reusable ambient flag. Part C: refusal withholds **only the dependent delegated operation** — never the person, never the room, never a right. **Resolves W1-D2 §8.1 and §8.2 / W1-D3 §10.7 in body.** Authorises no implementation, runtime directory, software/package dependency, UI, authentication mechanism, session-manager implementation, model access, vendor-hosted access, payload construction, transmission, fixture execution, pending-ledger change, Lane C, W6 or W5-D2 milestone |
| W5-D1 — Payload Assembly and Payload Equality (DR-W5-04) | [`../decisions/0031-payload-assembly-and-payload-equality.md`](../decisions/0031-payload-assembly-and-payload-equality.md) | Accepted by human reviewer, 2026-08-15 — Tier J: the fourth of the seven W5-D1 doctrine records. **Permission is not enough — the assembled payload must match the authorised scope.** A payload is lawful only if assembled from the exact authorised scope for the exact grant, matching edge, actor, recipient class, purpose, operation, duration, zones, plaintext status and vendor posture; **the granted scope is the whole payload** (W1-D6 §4.J), and nothing is added for helpfulness, safety, convenience, continuity, model quality or SDK default. **Forbidden:** SDK extras, device identifiers, helpful context, room state, history beyond the grant, cross-room or cached or adjacent context, inferred or derived enrichment, hidden instructions carrying state, and anything included **merely because it is already available**. Metadata is permitted **only where governed**; profiling, scoring and shadow-profile metadata are barred. **Equality** is governed canonical equality — byte-level **where applicable** at the **last controllable point, per boundary edge**; canonical, structural, field-level and deterministic otherwise. Similarity, intention-equivalence, output-equivalence and *close enough* **never** satisfy it, and representation differences may be ignored **only where the governed canonical form says so, never by runtime discretion**. **Three comparisons** are obligations: granted scope — assembled payload — last controllable point, plus pre/post-adapter where a transformation exists. **Z3 is governed now; Z4 remains DORMANT** — the Z4 limb stays **visibly pending**, **W5 may not claim it discharged**, E10 is not activated and no VendorAdapter ADR is authored. Content-free fingerprints permitted under five conditions, with **no hash, schema or storage prescribed**. Where equality cannot be proven the **dependent operation only** fails closed — never the person, never the room, never a right — and **a refusal is never resolved by narrowing the proof**. Prerequisite for the pending payload-equality stub but **does not unblock it**. Authorises no implementation, runtime directory, software/package dependency, payload assembler, serialisation, canonicalisation, hash, comparison mechanism, schema, storage, UI, model access, vendor-hosted access, transmission, disclosure, fixture execution, pending-ledger change, Lane C, W6 or W5-D2 milestone |
| W5-D1 — Transmission and Disclosure Mechanics (DR-W5-05) | [`../decisions/0032-transmission-and-disclosure-mechanics.md`](../decisions/0032-transmission-and-disclosure-mechanics.md) | Accepted by human reviewer, 2026-08-15 — Tier J: the fifth of the seven W5-D1 doctrine records. **Three separate gates** — grant permitted, payload equality proved, transmission authorised — and this record governs the third only: **a lawful payload does not authorise crossing**, and no granted, trusted or session-active flag is transmission authority. **Transmission** is any deliberate movement, handoff, send, export delivery, adapter handoff, model call, vendor call or metadata-bearing transfer out of the controlled processing context; **disclosure** is broader than network send and covers anything that becomes visible, retained or processable outside authorised assembly. Naming a category **does not activate it**. Recipient class must be **nameable, inspectable and matched to the grant**; zone, plaintext status and vendor posture must match at the moment of crossing. **W1-D5 OR-2 is carried unsoftened** — the Wing cannot verify vendor retention behaviour from outside, mitigation is **disclosure honesty, not technical control**, and eight comfort claims are barred by name. **W1-D2 §4's disclosure form is fixed and binding**, restated not redesigned. A **partial crossing is a crossing** and a failed outcome never unmakes an exposure. **Retries** are new attempts unless proven identical on seven attributes; no silent resend, no background retry, none after expiry or revocation. **Telemetry, logging and error reporting are disclosure surfaces and are not exempt because they are operational**; **SDK defaults create no authority**; **metadata-only transfer can be disclosure**. Refusal is structural and content-free, withholding **only the dependent operation** — never the person, never the room, never a right. **Z4 remains dormant by ADR-0031 and is not re-decided**; E10 unopened; no VendorAdapter ADR; model access is **DR-W5-06's**; **E9 export remains a right** and is classified, never gated. Authorises no implementation, network client, library, protocol, SDK integration, retry mechanism, payload assembler, model call, vendor-hosted access, schema, storage, UI, export-warning wording, hosted-mode language, fixture execution, pending-ledger change, Lane C, W6 or W5-D2 milestone |
| W5-D1 — Model Access (DR-W5-06) | [`../decisions/0033-model-access.md`](../decisions/0033-model-access.md) | Accepted by human reviewer, 2026-08-16 — Tier J: the sixth of the seven W5-D1 doctrine records — **the door decision**. **Model access is its own authority question**: not created by a valid grant, a lawful payload, payload equality, transmission authority, model availability, session activity, authentication or evaluation need — passing the ADR-0030/0031/0032 gates is never evidence of it. Model access occurs wherever a grant carries the **local model or vendor-hosted model recipient class**; every access is a **Z3 processing disclosure event through the single AIAdapter seam**, and every model call is a transmission and disclosure surface — including on the user's own device. **The local model class is doctrinally constructable** — the *"a model running on this device"* branch of W1-D2 §4 — lawful only under every prior gate, with no background processing and no ambient model permission; **doctrinally constructable ≠ runtime exists**, and every physical artefact (runtime, SDK, credential, directory, model binary) remains W5-D2's behind its own Tier F crossing. **Vendor-hosted model access is not opened**; the conditions any future opening must meet are stated in ink, with **OR-2 carried unsoftened**. **Z3/Z4 non-conflation is mandatory doctrine**: vendor-hosted model access is a recipient-class question, Z4 stays dormant, E10 unopened, no VendorAdapter ADR, and the Z4 equality limb stays visibly pending. **E12/Z5 remain reserved and unopened.** Model output is **agent-origin by construction — never evidence, never clinical truth, never authority, never ground truth for tests**; a proposal or suggestion only; it reaches stored state only through declared edges and the governed user or review acts they require, and no model-write edge is created. Evaluation-era instrument access is lawful **as a category only** — synthetic, no person, no user grant — under DR-W5-07's architecture. Acceptance satisfies **one conjunct of two pending conditions; both stubs stay pending** — no pending-ledger change, no stub conversion. Refusal is structural, content-free and dependent-operation-only — never the person, never the room, never a right. Authorises no implementation, model contact, provider selection, SDK, credential, runtime directory, transport, prompt, schema, fixture execution, pending-ledger change, Lane C, W6, E10 activation, Z4 discharge, VendorAdapter ADR, or E12/Z5 activation |
| W5 — Provisional Freshness-Governance Qualification (DR-W5-02 gate) | [`../decisions/0025-provisional-freshness-governance-qualification.md`](../decisions/0025-provisional-freshness-governance-qualification.md) | Accepted by human reviewer, 2026-08-13 — Tier J: the qualification that makes DR-W5-02 writable. W1-D3 §10.1 requested clinical-adjacent input for freshness numbers; that input was not available, and this record states so rather than treating three non-clinical inputs as a substitute. Thresholds may be set only as **provisional governance defaults — not clinically validated**, labelled wherever a threshold value is reproduced or summarised, ending only by supersession or explicit re-affirmation and never by silence. Ten binding safeguards constrain DR-W5-02: event-triggered review primary and calendar secondary; expired—unknown preserving assertion, provenance and age; unknown never collapsing into absence, resolution or reassurance; thresholds governed by data type; assertion posture derived and never stored; fail-closed where safe completion would require assuming an uncertain safety-relevant fact absent or resolved — a **partial disposition of W1-D3 §10.6** blocking only the Wing's assertion, never the user; renewal as meaningful review, never a timestamp reset; alert fatigue carried to the surface era; and no safeguard relaxable without its own record. Not a W5 deliverable and creates no eighth W5-D1 identity; sets no threshold value; claims no clinical validation, certification or endorsement; no implementation, directory, dependency, model contact, payload, transmission, harness or fixture execution is authorised |
| W5 — Safety-Relevant Set Reconciliation and Freshness Coverage (DR-W5-02 gate) | [`../decisions/0027-safety-relevant-set-reconciliation.md`](../decisions/0027-safety-relevant-set-reconciliation.md) | Accepted by human reviewer, 2026-08-14 — Tier J: the reconciliation that lifts the block on DR-W5-02. Four accepted sources hold four different safety-relevant / high-stakes memberships; this record fixes that they are **scoped by governance function and non-transferable**, that membership of one never implies membership of another, and that **no canonical universal set exists or is created**. It **adjudicates no membership** — ADR 0002's six are neither validated nor re-justified. It withdraws an earlier reading that ADR 0002's Kitchen/Gym row delegated the whole room layer to W1-D3 §6.3, leaving a **composition question**: ADR 0002's L2/L3 surfacing mechanism over six members and ADR 0020's always-shown mechanism over four both operate at room surfaces, and no accepted record states how they interact for **pregnancy status and clinician instructions**, covered only by the former. **Both records remain simultaneously valid; no precedence, supersession or winning set is created.** Reported and left open: those two items are in no accepted freshness taxonomy and can never age into review-due, stale or expired; and *freshness data type* has no accepted definition. **DR-W5-02 owns the composition question, freshness coverage and that definition.** Not a W5 deliverable and creates no W5 planning slot; amends no law and no record; no implementation, directory, dependency, model contact, payload, transmission, harness or fixture execution is authorised |
| W5-D2 — Runtime implementation | — | Not started. Milestone-based on the W3 precedent, every capability landing with its deterministic proof; four-surface observability designed in, not retrofitted; gated on the W5-D1 records it depends on |
| W5-D3 — Behavioural evaluation harness | — | Not started. Builds and proves the instrument; does not execute the twenty-three-fixture corpus |
| W5-D4 — Fixture execution and evaluation records | — | Not started. Executes only once the W5-D1 to W5-D3 prerequisites hold; results never enter fixture files; every `execution_status` transition follows the W5 doctrine eventually accepted for it |
| W5-D5 — Lane C Tier 2 evidence maintenance | — | Not started. One governed maintenance act, separate from the artefacts that generated the evidence; never converts Tier 3 evidence and never mechanically resolves applicability |
| W5-D6 — W5 closure record | — | Not started. Whole-phase closure assessment against the runway, with the W6 gate named and not opened |

## W6 and beyond

Defined as W5 progresses. Review surfaces, the governed string catalogue, and governance-label rendering are designed only after the runtime they must describe exists — and only through their own runways and gates.
